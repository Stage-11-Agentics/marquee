.DS_Store
node_modules/
.claude/
.env
.env.*
!.env.example
.dev.vars
dist/
.wrangler/
*.log
artifacts/checks/
ac-coverage.json
speed-report.json
playwright-report/
test-results/

# Generated at build time from the Worker and authored agent-facing sources.
src/agent-front-door/llms.txt
src/agent-front-door/llms-full.txt

# Generated from the served OpenAPI document by cli/generate-api-registry.mjs.
# Not tracked: a committed copy conflicts on every concurrent branch.
cli/api-registry.json

# The sbek eval kit is its own git repo, checked out here for eval runs.
.eval-kit-agent/

# A deploy freeze is per-machine and per-moment, never committed.
.deploy-freeze
