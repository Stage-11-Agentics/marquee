# MRQ-209 — Organization Home: the between-conferences view

Your worktree: `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-209-org-home`
(branch `mrq-209-org-home`, cut from `github/main` @ `de189c0e`). **Work only there.**
The primary checkout at `/Users/atin/Projects/Stage11/deployments/Marquee` is the Lattice
board's home and is off limits for code — read it, never write it.

Read `CLAUDE.md` at the repo root first; it is binding. Then read the ticket:

```
lattice show MRQ-209
```

The ticket description is the contract. Follow it; where it is silent, use your judgement
and say in the PR what you decided.

## Design contract

- `sequence/org-settings-design.md` — iteration 3, "Organization Home — green-lit and built".
- The prototype is binding: `http://127.0.0.1:8123/pipeline-v1.1/index.html?v=15#org/home`
  (a python http.server on `prototypes/`, already running). Drive it before writing code.
  `DESIGN.md` (Flight Deck) and `PHILOSOPHY.md` bind visuals and copy.

## Context you need

- **This is a composition, not a system.** Conferences, people counts, participations,
  organizers, audit_log all exist on main. Your job is one page + its queries.
- **Honest empties.** Outreach next-touch (MRQ-205) and stale-seat data (MRQ-212) are being
  built in parallel. If a source is absent when you integrate, its attention line renders an
  honest empty/omitted state — never a fake number. Leave a named seam (one function per
  attention source) so those tickets plug in.
- **Route**: Org Home is a page, not a settings tab — mount it at its own route. The sidebar
  row pointing here is MRQ-203's; if their nav work hasn't merged, add the minimal nav entry
  yourself and note the overlap in the PR (203 will reconcile).
- **Elements never jump** (CLAUDE.md/DESIGN.md rule). KPI numbers are tabular; the attention
  strip has fixed slots.
- Speed is a feature (R7): server-side queries, one round trip for the page.

## Working rules

- Commit early, commit often, on your branch. Never `git stash` anywhere in this repo.
- Other agents work in sibling worktrees right now — MRQ-203 (sidebar), MRQ-205 (outreach),
  MRQ-207 (org settings shell) are in flight. Do not touch their trees or duplicate their scope.
- Update MRQ-209 as you go (`lattice status … --actor agent:delegator-mrq-209`, plan at
  `.lattice/plans/`, comments). Do not create tickets — minting is single-writer.
- Tests: hermetic suite, budgets are objectives (read the status field, `pass-over-budget`
  is a warn). Add tests for your queries.

## Done means

Gate green in your worktree (`node scripts/checks/pr-gate.mjs` or the checks your diff
touches, honestly reported), PR open via
`gh pr create --repo Stage-11-Agentics/marquee --base main`, ticket moved through
`review`/`in_validation` with evidence (drive the real page in a browser), then report.

**Reporting contract (you are suppressed):** report completion and recoverable blockers to
your parent — `c11 send --workspace workspace:6 --surface surface:256 "MRQ-209: <status>"`.
Raise a c11 flag only when operator action is required. Do not merge your own PR; the
orchestrator reviews and merges.
