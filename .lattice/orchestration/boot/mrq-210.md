# MRQ-210 — Server panel: plain-language connections, named Resend account

Your worktree: `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-210-server-panel`
(branch `mrq-210-server-panel`, cut from `github/main` @ `de189c0e`). **Work only there.**
The primary checkout at `/Users/atin/Projects/Stage11/deployments/Marquee` is the Lattice
board's home and is off limits for code — read it, never write it.

Read `CLAUDE.md` at the repo root first; it is binding. Then read the ticket:

```
lattice show MRQ-210
```

## Design contract

- `sequence/org-settings-design.md` — iteration 3, "'Instance' → 'Server', in plain
  organizer language" + the #5 pivot (orgs run their own Resend; name the connected account,
  click out, nothing more).
- The prototype is binding: `http://127.0.0.1:8123/pipeline-v1.1/index.html?v=15#org/server`.
  Drive it first. `DESIGN.md` + `PHILOSOPHY.md` bind visuals and copy.

## Context you need

- The Instance panel and `GET /api/v1/instance/status` exist (cold-start build, SPEC
  Amendment 19; status derives from real bindings, never a stored flag — keep that
  property). Find the panel component (setup dashboard area, `src/ui/setup/`) — this is a
  rename + extension, not a new system.
- Rows lead with the job: **Email sending · File uploads · Spam protection · Web address**;
  the providing service (Resend, Cloudflare R2, Turnstile) is small print. Chips read
  `working` / `not set up`.
- The mail row names the connected Resend **sender and account** — derive from real config
  (env/secrets presence; account name may come from config, never invented) — and offers an
  "Open Resend ↗" link out.
- Steady-state placement: a tab of the org-settings surface MRQ-207 is building in
  parallel. If 207 has merged when you integrate, mount there (route `org/server`, redirect
  `org/instance`); otherwise mount behind your own route and say so in the PR.
- The Recovery card ("no passwords, ever" / re-run the setup command for a fresh claim
  link) per the prototype.

## Working rules

- Commit early, commit often, on your branch. Never `git stash` anywhere in this repo.
- Parallel worktrees are live (MRQ-203/205/207/209/211) — don't touch their trees or scope.
- Update MRQ-210 as you go (`--actor agent:delegator-mrq-210`). Do not create tickets.
- Tests: keep the status-derivation property tested (a stored flag sneaking in is the
  regression this design forbids).

## Done means

Gate honest and green for your diff, PR open via
`gh pr create --repo Stage-11-Agentics/marquee --base main`, ticket through
`review`/`in_validation` with real-browser evidence, then report.

**Reporting contract (you are suppressed):** report completion and recoverable blockers to
your parent — `c11 send --workspace workspace:6 --surface surface:256 "MRQ-210: <status>"`.
Raise a c11 flag only when operator action is required. Do not merge your own PR; the
orchestrator reviews and merges.
