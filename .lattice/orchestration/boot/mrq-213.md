# MRQ-213 — Invite machinery completions: QR code and the committee door

Your worktree: `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-213-invite-completions`
(branch `mrq-213-invite-completions` — the orchestrator cut it from `github/main` AFTER
MRQ-207 merged; verify with `git log --oneline -3` that the scoped-invite mint is under
you). **Work only there.** The primary checkout at
`/Users/atin/Projects/Stage11/deployments/Marquee` is the Lattice board's home and is off
limits for code — read it, never write it.

Read `CLAUDE.md` at the repo root first; it is binding. Then read the ticket:

```
lattice show MRQ-213
```

## Design contract

- `sequence/org-settings-design.md` — iteration 3, "One invite machinery, two doors" +
  ruling O4 (QR + short code, day-of over a phone's own connection).
- Prototype is binding: the invite modal at
  `http://127.0.0.1:8123/pipeline-v1.1/index.html?v=15#org/organizers` (+ Invite) and the
  committee door on `#evaluation` (Program committee card → + Invite reviewer).

## Context you need

- MRQ-207 built the role+scope invite mint and the short join code. You add:
  1. **A real, scannable QR** of the one-time join URL in the invite modal, beside the
     copyable link and short code. Vendor a minimal zero-dependency QR encoder into
     `src/lib/` (public-domain/MIT single-file implementations exist) or generate inline
     SVG server-side. **Flag on the ticket before adding any npm dependency** — the
     default is no new dependency. The QR must actually scan (validate with a phone or
     decoder in validation — "I saw it work").
  2. **The committee door**: Evaluation's Program committee card gains `+ Invite
     reviewer`, opening the SAME invite flow preset to role Reviewer scoped to the current
     conference — same mint path, same expiry, same pending row in Organizers. If you
     find yourself writing a second mint endpoint, stop.
- The role legend line under the picker (MRQ-212) may or may not be merged — do not build
  it; it is theirs.

## Working rules

- Commit early, commit often, on your branch. Never `git stash` anywhere in this repo.
- Sibling worktrees are live. Coordinate via ticket comments, never by editing their trees.
- Update MRQ-213 as you go (`--actor agent:delegator-mrq-213`). Do not create tickets.

## Done means

Gate honest and green for your diff, PR open via
`gh pr create --repo Stage-11-Agentics/marquee --base main`, ticket through
`review`/`in_validation` with evidence including a successful scan/decode of a minted QR,
then report.

**Reporting contract (you are suppressed):** report completion and recoverable blockers to
your parent — `c11 send --workspace workspace:6 --surface surface:256 "MRQ-213: <status>"`.
Raise a c11 flag only when operator action is required. Do not merge your own PR; the
orchestrator reviews and merges.
