# MRQ-212 — Organizer governance: role legend, Owner-only removal, owner transfer, stale seats

Your worktree: `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-212-governance`
(branch `mrq-212-governance` — the orchestrator cut it from `github/main` AFTER MRQ-207
merged; verify with `git log --oneline -3` that the org-settings shell is under you).
**Work only there.** The primary checkout at
`/Users/atin/Projects/Stage11/deployments/Marquee` is the Lattice board's home and is off
limits for code — read it, never write it.

Read `CLAUDE.md` at the repo root first; it is binding. Then read the ticket:

```
lattice show MRQ-212
```

## Design contract

- `sequence/org-settings-design.md` — iteration 3 clarity rulings + "Owner transfer, safe
  and simple" + "Stale conference-scoped seats". The role legend copy in the doc is the
  ruled copy — Atin approved it verbatim ("good as drafted").
- Prototype is binding: `http://127.0.0.1:8123/pipeline-v1.1/index.html?v=15#org/organizers`.
  Drive the whole flow: the legend card, the permission-aware action column, removal's
  two-step dialog (Keep access is the primary default in BOTH stages; the destructive
  final confirm is the product's only filled-red button), Transfer with the
  type-the-org-name gate — and note that after transferring, the ex-owner's controls
  genuinely disappear.

## Context you need

- MRQ-207 built the org-settings shell, scoped invites, and the removal cascade — you
  build ON it: legend, permission gating (server-side first: org-settings routes visible
  to Owner + org-wide Organizers; removal Owner-only; invite Organizer+), owner transfer,
  stale-seat surfacing.
- UI "Organizer" ↔ schema `program_lead`: write the mapping once where roles render, not
  scattered.
- Transfer semantics (ruled): Owner-only; recipient must be an org-wide `program_lead`
  member who has signed in at least once; type-the-org-name confirm; atomic flip (old
  owner → Organizer, `organizations`/membership updated together); both parties notified
  through the outbox; recorded to audit_log (MRQ-211 built the recording seam — use it).
- Stale seats: seats scoped to an event whose end date has passed; banner on the
  Organizers surface + a queryable source the Org Home attention strip (MRQ-209) can read.
  No silent expiry, ever.

## Working rules

- Commit early, commit often, on your branch. Never `git stash` anywhere in this repo.
- Sibling worktrees are live. Coordinate via ticket comments, never by editing their trees.
- Update MRQ-212 as you go (`--actor agent:delegator-mrq-212`). Do not create tickets.
- Auth changes get tests first-class: removal by a non-owner 403s, transfer by a non-owner
  403s, transfer to an event-scoped or never-signed-in member is rejected, the last owner
  is undeletable (existing invariant — keep it green).

## Done means

Gate honest and green for your diff, PR open via
`gh pr create --repo Stage-11-Agentics/marquee --base main`, ticket through
`review`/`in_validation` with real-browser evidence (drive removal AND transfer
end-to-end), then report.

**Reporting contract (you are suppressed):** report completion and recoverable blockers to
your parent — `c11 send --workspace workspace:6 --surface surface:256 "MRQ-212: <status>"`.
Raise a c11 flag only when operator action is required. Do not merge your own PR; the
orchestrator reviews and merges.
