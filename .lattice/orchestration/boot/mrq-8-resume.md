FIRST ACTION, before anything else, run exactly:
`test "$(pwd)" = "/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-8-api" || { echo "FATAL: wrong cwd — actual: $(pwd)"; exit 99; }`
On failure HALT and report the actual pwd to the Orchestrator via c11 send — do not cd, do not improvise.

Read `/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/orchestration/boot/COMMON.md` and follow it — it is the binding delegator contract. Your ticket: **MRQ-8** (BUILDPLAN **M-07** — API core, list contract, OpenAPI assembly). Actor: `agent:delegator-mrq-8`. Worktree: `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-8-api`, branch `mrq-8-api`, cut clean off `forgejo/master`.

## You are RESUMING. Do not re-plan.

Your plan already exists, was plan-reviewed, and is committed at:
`/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/plans/task_01KZJHM831WTD0XSD2Q84NVJC7.md` (288 lines)

It carries a `## Plan-Review Cycle 1 Resolutions (AUTHORITATIVE)` block and a `## Orchestrator Rulings after Cycle 1 (AUTHORITATIVE)` block. Both override any earlier wording in the file.

1. Claim MRQ-8. **Revalidate** the plan against current `forgejo/master` — master has moved since the plan was written and now contains MRQ-1's skeleton, MRQ-2's complete D1 schema (`migrations/0001_init.sql`, `src/db/schema.ts`), and MRQ-6's design system + check harness. Post one short comment: either "plan revalidated; no amendments" or an appended amendment block for genuine drift. Then bump `in_progress` and implement. **Do not run a fresh plan-review.**

## Rulings already binding on this ticket

- **CAS, not transactions.** D1 has no interactive transactions. Compare-and-swap is the pattern, and it must be a **named, reusable primitive** (`compareAndSwapResource` per your plan's endpoint inventory), not ad-hoc at each call site.
- **Bulk writes use the S-3 verdict:** one JSON ID array passed as a single bound parameter, expanded with `json_each(?)` — one write query at both 150 and 1,000 rows. Not ≤90-binding chunking. D1 accepts exactly 100 bound parameters and rejects 101. The helper dedupes, no-ops on empty, stringifies once, runs once. The merged spike lives at `spikes/s3-d1-chunking/VERDICT.md` on master.
- **Estimate is 7h and full scope stays binding** — the agent-native API is a moat feature and every later ticket inherits its contracts. The route manifest is **generated by glob**, never a hand-edited list; `check:api` asserts registry/OpenAPI parity, so a hand-maintained list is a guaranteed gate failure.

## FIRST ACTIONS ON RESUME (you were cut off by a Kimi session cap)

Your worktree still holds **uncommitted work from your last session — it is intact, do not discard it.** Before anything else:
1. `git add -A && git commit -m "MRQ-8: WIP from prior session"` — commit before you read anything.
2. `git fetch forgejo && git rebase forgejo/master` — master has moved and now carries **commit 1507bff**, which closes the adversarial pass and changes rules that bind this ticket.
3. Push immediately (`git push forgejo mrq-8-api`), and keep pushing at every boundary from here — a session cap stops you where you stand, and only pushed work survives cleanly.

## N-7 — binds YOUR ticket directly (contract change, 1507bff)

`check:api` asserts single-source parity across served JSON, rendered docs, and **the CLI registry**. The CLI is M-38 (Tier B, rank 19) and does not exist yet. As originally written, that assertion would fail on **every PR from Wave 0 onward** — including yours — over a registry no ticket has built.

The rule is now: **the CLI-registry half activates only once `cli/` exists and is skipped-with-a-printed-notice before then; the served-JSON / rendered-docs half is live from Wave 0.** Same shape as `trace:ac`'s `--scope` rule. `EVALUATION.md` §1.1 and `BUILDPLAN.md` M-06 carry the wording.

You own the route registry and OpenAPI assembly, so **you implement that activation rule** — `check:api` is currently a stub (`scripts/checks/stub-command.mjs`) and your ticket is where it becomes real. A `check:api` that hard-fails on a missing CLI registry is a broken gate for every ticket after you.

Also from 1507bff: the **three non-`/api/v1` calendar and feed URLs named in `SPEC.md` §4.2 are an allowlist, not drift** — your parity check must not flag them.

## Before your PR

Run `npm run pr-gate -- --ticket MRQ-8` from this worktree and paste the result into your completion comment. Private Forgejo has **no CI runner** — this local gate is the only thing standing between a broken PR and master.

**Headless `lattice code-review` remains SUSPENDED.** Self-review instead: compute your own diff, write the standard-shape review (Verdict PASS / PASS-WITH-NITS / FAIL, findings with file:line), attach it with `--role review` naming your exact HEAD commit, and note "own-reviewer, quota directive" in the completion comment. The Orchestrator independently scans every diff before merging.

Then push, open the PR against `master` (base is clean — you are not stacked on anything), bump `pr_open`, and c11-send the Orchestrator at workspace:9 surface:60. You are on the critical chain and MRQ-9 waits on you, so report the moment you reach `review`.
