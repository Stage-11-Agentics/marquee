{"actor":"agent:eval-triage","data":{"complexity":"low","description":"Round-9 defect, **major**, `speaker-management` (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`).\nNew this round \u2014 it does not appear in round 4. **No rubric item punishes it**; the area\nscored 15/15. It is on the list because a bulk send is irreversible and this one goes to 81\npeople.\n\n## The judge's own words\n\n> **`/communications` \u2014 compose body, preview pane, and queued log entries.** An\n> unsupported merge token is neither rejected nor stripped: `{{portal.link}}` is absent\n> from the MERGE FIELDS palette, yet it renders literally as \"{{portal.link}}\" in the\n> preview pane and, after queueing, in the stored per-recipient message body (\"Your speaker\n> portal is here: {{portal.link}}\"). Nothing warns at save or queue time, so recipients\n> would receive raw template syntax in a bulk send that went to 81 people.\n\n## Read this before you \"fix\" the renderer \u2014 the passthrough is deliberate\n\n`src/jobs/mail/render.ts:22-31`:\n\n```\n * Merge fields are deliberately boring: the template owns the field names,\n * and an absent value stays visible in previews rather than disappearing.\n * This keeps an operator from sending a message that silently lost context.\n```\n\nand `mergeTemplate` returns `` `{{${key}}}` `` when the value is null/undefined.\n\n**That choice is right and must survive.** Silently deleting a token is worse than showing\nit: the operator would send a sentence with a hole in it and never know. Do not change\n`mergeTemplate` to strip unknown tokens.\n\nThe defect is that the deliberate behaviour is doing two different jobs and only one of\nthem is honest:\n\n- **A known field with no value for this recipient** \u2014 showing `{{speaker.company}}` in the\n  *preview* is the designed warning. Correct.\n- **A field name that does not exist at all** \u2014 `{{portal.link}}` is a typo or a guess. It\n  is not a missing value; it can never resolve, for any recipient, ever. Rendering it into\n  the preview and then *queueing it anyway with no warning at save or queue time* turns a\n  typo into 81 emails containing raw template syntax.\n\nThe renderer is the wrong layer. The compose and queue path is the right one.\n\n## What to build\n\n1. **Validate at compose/save and again at queue.** Extract every `{{token}}` from the\n   subject and body with the same regex the renderer uses\n   (`/{{\\s*([a-zA-Z0-9_.-]+)\\s*}}/g`), and compare against the known merge-field set \u2014 the\n   same set the MERGE FIELDS palette renders, read from one place, not a second hardcoded\n   list that can drift from the palette.\n2. **Name the offenders.** The message says which tokens are unknown, verbatim, not \"invalid\n   template\". The operator must be able to fix it without hunting: *\"`{{portal.link}}` is\n   not a merge field. Available fields are listed under MERGE FIELDS.\"*\n3. **A bulk queue with unknown tokens does not proceed.** This is the irreversible step and\n   it is the one that currently has no guard at all. Blocking here is the point of the\n   ticket.\n4. **Ad-hoc and stored templates go through the same check.** `renderAdHocMail` exists\n   precisely so callers \"do not get a second renderer with subtly different merge-field\n   behavior\" \u2014 the validator inherits that rule.\n5. **Do not degrade the preview.** An unknown token still renders literally in the preview,\n   now accompanied by the warning. The preview keeps telling the truth; it simply stops\n   being the only thing that noticed.\n6. **Elements never jump.** If the warning appears above or below the composer, reserve its\n   space so the body textarea does not move under the operator's cursor as they type.\n\n## Acceptance\n\n- Queueing a bulk message whose body contains `{{portal.link}}` is refused, and the refusal\n  names `portal.link`.\n- Queueing the same message with `{{speaker.first_name}}` succeeds.\n- A known field that is null for a given recipient still queues, and still renders its token\n  literally \u2014 the existing designed behaviour, pinned by a test so a later change cannot\n  quietly take it away.\n- Regression test fails on `main`, passes on the branch.\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-173-merge-token -b mrq-173-merge-token main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** This needs none.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","priority":"high","short_id":"MRQ-175","status":"backlog","tags":["auto-eval","speaker-management","round9","craft"],"title":"Bulk send ships raw template syntax: an unknown merge token is never validated","type":"bug","urgency":"high"},"id":"ev_01KZYRCTC0VXM1QMB73EYKQYRN","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:47:52Z","type":"task_created"}
{"actor":"agent:eval-triage","data":{"field":"description","from":"Round-9 defect, **major**, `speaker-management` (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`).\nNew this round \u2014 it does not appear in round 4. **No rubric item punishes it**; the area\nscored 15/15. It is on the list because a bulk send is irreversible and this one goes to 81\npeople.\n\n## The judge's own words\n\n> **`/communications` \u2014 compose body, preview pane, and queued log entries.** An\n> unsupported merge token is neither rejected nor stripped: `{{portal.link}}` is absent\n> from the MERGE FIELDS palette, yet it renders literally as \"{{portal.link}}\" in the\n> preview pane and, after queueing, in the stored per-recipient message body (\"Your speaker\n> portal is here: {{portal.link}}\"). Nothing warns at save or queue time, so recipients\n> would receive raw template syntax in a bulk send that went to 81 people.\n\n## Read this before you \"fix\" the renderer \u2014 the passthrough is deliberate\n\n`src/jobs/mail/render.ts:22-31`:\n\n```\n * Merge fields are deliberately boring: the template owns the field names,\n * and an absent value stays visible in previews rather than disappearing.\n * This keeps an operator from sending a message that silently lost context.\n```\n\nand `mergeTemplate` returns `` `{{${key}}}` `` when the value is null/undefined.\n\n**That choice is right and must survive.** Silently deleting a token is worse than showing\nit: the operator would send a sentence with a hole in it and never know. Do not change\n`mergeTemplate` to strip unknown tokens.\n\nThe defect is that the deliberate behaviour is doing two different jobs and only one of\nthem is honest:\n\n- **A known field with no value for this recipient** \u2014 showing `{{speaker.company}}` in the\n  *preview* is the designed warning. Correct.\n- **A field name that does not exist at all** \u2014 `{{portal.link}}` is a typo or a guess. It\n  is not a missing value; it can never resolve, for any recipient, ever. Rendering it into\n  the preview and then *queueing it anyway with no warning at save or queue time* turns a\n  typo into 81 emails containing raw template syntax.\n\nThe renderer is the wrong layer. The compose and queue path is the right one.\n\n## What to build\n\n1. **Validate at compose/save and again at queue.** Extract every `{{token}}` from the\n   subject and body with the same regex the renderer uses\n   (`/{{\\s*([a-zA-Z0-9_.-]+)\\s*}}/g`), and compare against the known merge-field set \u2014 the\n   same set the MERGE FIELDS palette renders, read from one place, not a second hardcoded\n   list that can drift from the palette.\n2. **Name the offenders.** The message says which tokens are unknown, verbatim, not \"invalid\n   template\". The operator must be able to fix it without hunting: *\"`{{portal.link}}` is\n   not a merge field. Available fields are listed under MERGE FIELDS.\"*\n3. **A bulk queue with unknown tokens does not proceed.** This is the irreversible step and\n   it is the one that currently has no guard at all. Blocking here is the point of the\n   ticket.\n4. **Ad-hoc and stored templates go through the same check.** `renderAdHocMail` exists\n   precisely so callers \"do not get a second renderer with subtly different merge-field\n   behavior\" \u2014 the validator inherits that rule.\n5. **Do not degrade the preview.** An unknown token still renders literally in the preview,\n   now accompanied by the warning. The preview keeps telling the truth; it simply stops\n   being the only thing that noticed.\n6. **Elements never jump.** If the warning appears above or below the composer, reserve its\n   space so the body textarea does not move under the operator's cursor as they type.\n\n## Acceptance\n\n- Queueing a bulk message whose body contains `{{portal.link}}` is refused, and the refusal\n  names `portal.link`.\n- Queueing the same message with `{{speaker.first_name}}` succeeds.\n- A known field that is null for a given recipient still queues, and still renders its token\n  literally \u2014 the existing designed behaviour, pinned by a test so a later change cannot\n  quietly take it away.\n- Regression test fails on `main`, passes on the branch.\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-173-merge-token -b mrq-173-merge-token main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** This needs none.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","to":"Round-9 defect, **major**, `speaker-management` (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`).\nNew this round \u2014 it does not appear in round 4. **No rubric item punishes it**; the area\nscored 15/15. It is on the list because a bulk send is irreversible and this one goes to 81\npeople.\n\n## The judge's own words\n\n> **`/communications` \u2014 compose body, preview pane, and queued log entries.** An\n> unsupported merge token is neither rejected nor stripped: `{{portal.link}}` is absent\n> from the MERGE FIELDS palette, yet it renders literally as \"{{portal.link}}\" in the\n> preview pane and, after queueing, in the stored per-recipient message body (\"Your speaker\n> portal is here: {{portal.link}}\"). Nothing warns at save or queue time, so recipients\n> would receive raw template syntax in a bulk send that went to 81 people.\n\n## Read this before you \"fix\" the renderer \u2014 the passthrough is deliberate\n\n`src/jobs/mail/render.ts:22-31`:\n\n```\n * Merge fields are deliberately boring: the template owns the field names,\n * and an absent value stays visible in previews rather than disappearing.\n * This keeps an operator from sending a message that silently lost context.\n```\n\nand `mergeTemplate` returns `` `{{${key}}}` `` when the value is null/undefined.\n\n**That choice is right and must survive.** Silently deleting a token is worse than showing\nit: the operator would send a sentence with a hole in it and never know. Do not change\n`mergeTemplate` to strip unknown tokens.\n\nThe defect is that the deliberate behaviour is doing two different jobs and only one of\nthem is honest:\n\n- **A known field with no value for this recipient** \u2014 showing `{{speaker.company}}` in the\n  *preview* is the designed warning. Correct.\n- **A field name that does not exist at all** \u2014 `{{portal.link}}` is a typo or a guess. It\n  is not a missing value; it can never resolve, for any recipient, ever. Rendering it into\n  the preview and then *queueing it anyway with no warning at save or queue time* turns a\n  typo into 81 emails containing raw template syntax.\n\nThe renderer is the wrong layer. The compose and queue path is the right one.\n\n## What to build\n\n1. **Validate at compose/save and again at queue.** Extract every `{{token}}` from the\n   subject and body with the same regex the renderer uses\n   (`/{{\\s*([a-zA-Z0-9_.-]+)\\s*}}/g`), and compare against the known merge-field set \u2014 the\n   same set the MERGE FIELDS palette renders, read from one place, not a second hardcoded\n   list that can drift from the palette.\n2. **Name the offenders.** The message says which tokens are unknown, verbatim, not \"invalid\n   template\". The operator must be able to fix it without hunting: *\"`{{portal.link}}` is\n   not a merge field. Available fields are listed under MERGE FIELDS.\"*\n3. **A bulk queue with unknown tokens does not proceed.** This is the irreversible step and\n   it is the one that currently has no guard at all. Blocking here is the point of the\n   ticket.\n4. **Ad-hoc and stored templates go through the same check.** `renderAdHocMail` exists\n   precisely so callers \"do not get a second renderer with subtly different merge-field\n   behavior\" \u2014 the validator inherits that rule.\n5. **Do not degrade the preview.** An unknown token still renders literally in the preview,\n   now accompanied by the warning. The preview keeps telling the truth; it simply stops\n   being the only thing that noticed.\n6. **Elements never jump.** If the warning appears above or below the composer, reserve its\n   space so the body textarea does not move under the operator's cursor as they type.\n\n## Acceptance\n\n- Queueing a bulk message whose body contains `{{portal.link}}` is refused, and the refusal\n  names `portal.link`.\n- Queueing the same message with `{{speaker.first_name}}` succeeds.\n- A known field that is null for a given recipient still queues, and still renders its token\n  literally \u2014 the existing designed behaviour, pinned by a test so a later change cannot\n  quietly take it away.\n- Regression test fails on `main`, passes on the branch.\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-175-merge-token -b mrq-175-merge-token main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** This needs none.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`."},"id":"ev_01KZYRD12PDT9QMVCAKWTVDG1D","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:47:59Z","type":"field_updated"}
{"actor":"agent:codex-cli","data":{"from":null,"to":"agent:codex-cli"},"id":"ev_01KZYRGKKWTJRB8Z803EG5BV7N","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:49:56Z","type":"assignment_changed"}
{"actor":"agent:codex-cli","data":{"from":"backlog","to":"in_planning"},"id":"ev_01KZYRGKPTS8CYC5M8EN9ZPDWB","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:49:56Z","type":"status_changed"}
{"actor":"agent:codex-cli","data":{"body":"Plan: trace the communications compose/save and bulk queue paths to the shared merge-field palette and renderer; add a single validator over the renderer's token regex and known field set; wire it into both save and queue/ad-hoc paths while preserving literal known-null and unknown preview rendering; reserve stable UI warning space; prove the regression on main and this branch, then run the serialized gate and open one PR. No migration or deployment."},"id":"ev_01KZYRGKSQW7X45HYD7A66Y67R","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:49:56Z","type":"comment_added"}
{"actor":"agent:codex-cli","data":{"from":"in_planning","to":"planned"},"id":"ev_01KZYRGKWMY598VE31GAQZ3EPA","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:49:56Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-plan-review-task_01KZYRCTBZ1FQV48TAAPHY5HD6.log","mode":"single","pid":16468,"review_type":"plan-review","spawned_at":"2026-08-13T23:49:56Z","trigger_status_event_id":"ev_01KZYRGKWMY598VE31GAQZ3EPA"},"id":"ev_01KZYRGKWSBK01QM03ZFM3D9RG","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:49:56Z","type":"auto_review_spawned"}
{"actor":"agent:codex-cli","data":{"from":"planned","to":"in_progress"},"id":"ev_01KZYRGVG8Z3676MPFAQR8W8Q8","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:50:04Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"artifact_id":"art_01KZYRNC2S5SVE06NVDET8D4HH","role":"plan-review"},"id":"ev_01KZYRNC2WT5KB72RGBZSKPWY0","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:52:32Z","type":"artifact_attached"}
{"actor":"agent:codex-cli","data":{"body":"Root cause confirmed: mergeTemplate intentionally preserves unresolved tokens, but the UI palette is a private constant and template create/update plus enqueueBulkReminder perform no token-set validation. Added focused MRQ-175 tests; before implementation, npx vitest run tests/integration/mail.test.ts -t 'MRQ-175' failed as expected: unknown queue returned 202 (not 400) and unknown template save returned 201 (not 400); preview remained 200/literal."},"id":"ev_01KZYRYGS3FYMCW88MWTE0GZB5","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-13T23:57:32Z","type":"comment_added"}
{"actor":"agent:codex-cli","data":{"from":"in_progress","to":"review"},"id":"ev_01KZYSRFGBEF2TQR92TH06H58K","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:11:42Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-code-review-task_01KZYRCTBZ1FQV48TAAPHY5HD6.log","mode":"single","pid":82979,"review_type":"code-review","spawned_at":"2026-08-14T00:11:42Z","trigger_status_event_id":"ev_01KZYSRFGBEF2TQR92TH06H58K"},"id":"ev_01KZYSRFGHQTG22Z5X8XD306HK","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:11:42Z","type":"auto_review_spawned"}
{"actor":"agent:codex-cli","data":{"body":"Root cause: `mergeTemplate` deliberately preserves unresolved values, but no shared MERGE FIELDS vocabulary or validator guarded template save or bulk queue, so an unknown token reached the outbox. Verified A/B with `npx vitest run tests/integration/mail.test.ts -t MRQ-175`: base main returned 202 for the unknown queue and 201 for the unknown template save, while this branch passes all 4 MRQ-175 tests; final `npm test` passed 1,061/1,061 and the serialized `npm run pr-gate` passed in 57.178s. PR: #200. No migration or deployment.","role":"validation"},"id":"ev_01KZYSRS9NKJQYHZWZ31P60H10","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:11:53Z","type":"comment_added"}
{"actor":"agent:codex-cli","data":{"from":"review","to":"in_validation"},"id":"ev_01KZYSSB30ANX2XJ66VB83FHPA","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:12:11Z","type":"status_changed"}
{"actor":"agent:codex-cli","data":{"from":"in_validation","to":"pr_open"},"id":"ev_01KZYSSS44YTTB4P7D6AKY7W75","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:12:25Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"artifact_id":"art_01KZYT6MDYZZ60PYQXK1AWM36X","role":"review"},"id":"ev_01KZYT6ME4BETHGFW2PD6HXJZE","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:19:26Z","type":"artifact_attached"}
{"actor":"agent:eval-triage","data":{"field":"description","from":"Round-9 defect, **major**, `speaker-management` (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`).\nNew this round \u2014 it does not appear in round 4. **No rubric item punishes it**; the area\nscored 15/15. It is on the list because a bulk send is irreversible and this one goes to 81\npeople.\n\n## The judge's own words\n\n> **`/communications` \u2014 compose body, preview pane, and queued log entries.** An\n> unsupported merge token is neither rejected nor stripped: `{{portal.link}}` is absent\n> from the MERGE FIELDS palette, yet it renders literally as \"{{portal.link}}\" in the\n> preview pane and, after queueing, in the stored per-recipient message body (\"Your speaker\n> portal is here: {{portal.link}}\"). Nothing warns at save or queue time, so recipients\n> would receive raw template syntax in a bulk send that went to 81 people.\n\n## Read this before you \"fix\" the renderer \u2014 the passthrough is deliberate\n\n`src/jobs/mail/render.ts:22-31`:\n\n```\n * Merge fields are deliberately boring: the template owns the field names,\n * and an absent value stays visible in previews rather than disappearing.\n * This keeps an operator from sending a message that silently lost context.\n```\n\nand `mergeTemplate` returns `` `{{${key}}}` `` when the value is null/undefined.\n\n**That choice is right and must survive.** Silently deleting a token is worse than showing\nit: the operator would send a sentence with a hole in it and never know. Do not change\n`mergeTemplate` to strip unknown tokens.\n\nThe defect is that the deliberate behaviour is doing two different jobs and only one of\nthem is honest:\n\n- **A known field with no value for this recipient** \u2014 showing `{{speaker.company}}` in the\n  *preview* is the designed warning. Correct.\n- **A field name that does not exist at all** \u2014 `{{portal.link}}` is a typo or a guess. It\n  is not a missing value; it can never resolve, for any recipient, ever. Rendering it into\n  the preview and then *queueing it anyway with no warning at save or queue time* turns a\n  typo into 81 emails containing raw template syntax.\n\nThe renderer is the wrong layer. The compose and queue path is the right one.\n\n## What to build\n\n1. **Validate at compose/save and again at queue.** Extract every `{{token}}` from the\n   subject and body with the same regex the renderer uses\n   (`/{{\\s*([a-zA-Z0-9_.-]+)\\s*}}/g`), and compare against the known merge-field set \u2014 the\n   same set the MERGE FIELDS palette renders, read from one place, not a second hardcoded\n   list that can drift from the palette.\n2. **Name the offenders.** The message says which tokens are unknown, verbatim, not \"invalid\n   template\". The operator must be able to fix it without hunting: *\"`{{portal.link}}` is\n   not a merge field. Available fields are listed under MERGE FIELDS.\"*\n3. **A bulk queue with unknown tokens does not proceed.** This is the irreversible step and\n   it is the one that currently has no guard at all. Blocking here is the point of the\n   ticket.\n4. **Ad-hoc and stored templates go through the same check.** `renderAdHocMail` exists\n   precisely so callers \"do not get a second renderer with subtly different merge-field\n   behavior\" \u2014 the validator inherits that rule.\n5. **Do not degrade the preview.** An unknown token still renders literally in the preview,\n   now accompanied by the warning. The preview keeps telling the truth; it simply stops\n   being the only thing that noticed.\n6. **Elements never jump.** If the warning appears above or below the composer, reserve its\n   space so the body textarea does not move under the operator's cursor as they type.\n\n## Acceptance\n\n- Queueing a bulk message whose body contains `{{portal.link}}` is refused, and the refusal\n  names `portal.link`.\n- Queueing the same message with `{{speaker.first_name}}` succeeds.\n- A known field that is null for a given recipient still queues, and still renders its token\n  literally \u2014 the existing designed behaviour, pinned by a test so a later change cannot\n  quietly take it away.\n- Regression test fails on `main`, passes on the branch.\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-175-merge-token -b mrq-175-merge-token main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** This needs none.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","to":"Round-9 defect, **major**, `speaker-management` (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`).\nNew this round \u2014 it does not appear in round 4. **No rubric item punishes it**; the area\nscored 15/15. It is on the list because a bulk send is irreversible and this one goes to 81\npeople.\n\n## The judge's own words\n\n> **`/communications` \u2014 compose body, preview pane, and queued log entries.** An\n> unsupported merge token is neither rejected nor stripped: `{{portal.link}}` is absent\n> from the MERGE FIELDS palette, yet it renders literally as \"{{portal.link}}\" in the\n> preview pane and, after queueing, in the stored per-recipient message body (\"Your speaker\n> portal is here: {{portal.link}}\"). Nothing warns at save or queue time, so recipients\n> would receive raw template syntax in a bulk send that went to 81 people.\n\n## Read this before you \"fix\" the renderer \u2014 the passthrough is deliberate\n\n`src/jobs/mail/render.ts:22-31`:\n\n```\n * Merge fields are deliberately boring: the template owns the field names,\n * and an absent value stays visible in previews rather than disappearing.\n * This keeps an operator from sending a message that silently lost context.\n```\n\nand `mergeTemplate` returns `` `{{${key}}}` `` when the value is null/undefined.\n\n**That choice is right and must survive.** Silently deleting a token is worse than showing\nit: the operator would send a sentence with a hole in it and never know. Do not change\n`mergeTemplate` to strip unknown tokens.\n\nThe defect is that the deliberate behaviour is doing two different jobs and only one of\nthem is honest:\n\n- **A known field with no value for this recipient** \u2014 showing `{{speaker.company}}` in the\n  *preview* is the designed warning. Correct.\n- **A field name that does not exist at all** \u2014 `{{portal.link}}` is a typo or a guess. It\n  is not a missing value; it can never resolve, for any recipient, ever. Rendering it into\n  the preview and then *queueing it anyway with no warning at save or queue time* turns a\n  typo into 81 emails containing raw template syntax.\n\nThe renderer is the wrong layer. The compose and queue path is the right one.\n\n## What to build\n\n1. **Validate at compose/save and again at queue.** Extract every `{{token}}` from the\n   subject and body with the same regex the renderer uses\n   (`/{{\\s*([a-zA-Z0-9_.-]+)\\s*}}/g`), and compare against the known merge-field set \u2014 the\n   same set the MERGE FIELDS palette renders, read from one place, not a second hardcoded\n   list that can drift from the palette.\n2. **Name the offenders.** The message says which tokens are unknown, verbatim, not \"invalid\n   template\". The operator must be able to fix it without hunting: *\"`{{portal.link}}` is\n   not a merge field. Available fields are listed under MERGE FIELDS.\"*\n3. **A bulk queue with unknown tokens does not proceed.** This is the irreversible step and\n   it is the one that currently has no guard at all. Blocking here is the point of the\n   ticket.\n4. **Ad-hoc and stored templates go through the same check.** `renderAdHocMail` exists\n   precisely so callers \"do not get a second renderer with subtly different merge-field\n   behavior\" \u2014 the validator inherits that rule.\n5. **Do not degrade the preview.** An unknown token still renders literally in the preview,\n   now accompanied by the warning. The preview keeps telling the truth; it simply stops\n   being the only thing that noticed.\n6. **Elements never jump.** If the warning appears above or below the composer, reserve its\n   space so the body textarea does not move under the operator's cursor as they type.\n\n## Acceptance\n\n- Queueing a bulk message whose body contains `{{portal.link}}` is refused, and the refusal\n  names `portal.link`.\n- Queueing the same message with `{{speaker.first_name}}` succeeds.\n- A known field that is null for a given recipient still queues, and still renders its token\n  literally \u2014 the existing designed behaviour, pinned by a test so a later change cannot\n  quietly take it away.\n- Regression test fails on `main`, passes on the branch.\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-175-merge-token -b mrq-175-merge-token main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** This needs none.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Gate serialization is a fleet rule, not an option.** Route EVERY `npm run pr-gate` and EVERY full\n  `npm test` through `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/.gate-lock/gate-lock.sh`,\n  including one-off \"just checking\" runs \u2014 a lock only serializes its participants, so a single\n  unlocked run lands on top of a lock-holder and inflates it. Cheap scoped things (tsc, one test\n  file) stay unlocked.\n- **A timeout and a red are indistinguishable in an unserialized run.** Check `uptime` before you\n  believe a red; load has ranged 14 to 164 tonight and the same branch measured 78s and 276s\n  against a 120s budget. If your only evidence for a defect is \"it timed out\", you have no\n  evidence. CI fast-gate runs on a quiet box and is tonight's oracle.\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`."},"id":"ev_01KZYTGTWF261S219Y2KMSPTEF","provenance":{"reason":"fleet gate-lock rule"},"schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:25:01Z","type":"field_updated"}
{"actor":"agent:codex-cli","data":{"body":"Root cause: compose/save and bulk enqueue lacked a shared unknown-token validation boundary, so an unsupported token such as {{portal.link}} could be queued literally. Fix: PR #200 centralizes the merge vocabulary/validator, validates before save and queue, preserves literal preview behavior, and adds the exhaustive DEFAULT_TEMPLATES-to-MERGE_FIELDS drift test; the four shipped template tokens were verified in merged main. Verification: npx vitest run tests/unit/mail-merge-fields.MRQ-175.test.ts (1/1), npx vitest run tests/integration/mail.test.ts (23/23), npx vitest run tests/integration/api/evaluation.test.ts (31/31), plus the previously reported locked npm test (1061/1061) and locked pr-gate pass in 57s. PR #200 merged as 3a5314dc743d4a456a7f4be6f70e9cdc7198764d; no migration or deploy performed.","role":"review"},"id":"ev_01KZYVG3YF91YFKP5THF378R7N","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:42:06Z","type":"comment_added"}
{"actor":"agent:codex-cli","data":{"from":"pr_open","to":"review"},"id":"ev_01KZYVG3YF91YFKP5THF378R7P","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:42:06Z","type":"status_changed"}
{"actor":"agent:codex-cli","data":{"artifact_id":"art_01KZYVG3YF91YFKP5THF378R7M","role":"review"},"id":"ev_01KZYVG3YF91YFKP5THF378R7Q","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:42:06Z","type":"artifact_attached"}
{"actor":"agent:codex-cli","data":{"from":"review","to":"done"},"id":"ev_01KZYVG3YF91YFKP5THF378R7R","schema_version":1,"task_id":"task_01KZYRCTBZ1FQV48TAAPHY5HD6","ts":"2026-08-14T00:42:06Z","type":"status_changed"}
