{"actor":"agent:eval-triage","data":{"complexity":"medium","description":"Two round-9 defects (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`), one\nshape: **a count on the page contradicts what the page itself is showing.** Grouped because\nthe fix is the same discipline in two places, not because the code is shared.\n\n**No rubric item punishes either \u2014 speaker-management scored 15/15 this round.** They are on\nthe list because an organizer who catches the roster lying about its own size stops trusting\nevery other number in the product, and this one repeated and got worse.\n\n---\n\n## Part 1 \u2014 a hand-added speaker is not on the roster (major, escalated from round 4)\n\n### The judge's own words\n\n> **`/roster` \u2014 speaker count header and status tabs after \"Add speaker\".** The roster's\n> counts do not reflect a speaker added by hand. Before Marcus Okafor was created the tabs\n> read All 509 / Pending 468 / Invited 4 / Confirmed 36 / Declined 1; after he was saved\n> (his record opening with CONFERENCE STATUS = Invited) and after a subsequent full page\n> reload the tabs still read All 509 with Invited unchanged at 4, and the total only moved\n> to 510 when the CSV import created Dana Kowalski (Pending 468\u2192+1). The header \"N speakers\n> on the roster for this conference \u2014 everyone who submitted, was accepted, was imported, or\n> was added by hand\" therefore undercounts, and **the hand-added speaker's row was never\n> observed in the roster table** even though his record is reachable by URL, appears in the\n> task-assignment picker and received the bulk email.\n\nRound 4 recorded the same thing as a **minor** (\"Roster totals do not visibly account for\nnewly added or imported speakers\"). Round 9 is sharper and worse: this is not a stale\ncounter, the row is **absent from the list**. The person exists everywhere else in the\nproduct and is missing from the one page whose entire job is to list him.\n\n### What I checked, so you do not repeat it\n\nThe obvious cause is not the cause. `createSpeaker` (`src/routes/speakers.routes.ts:238`)\npushes `speakerMembershipStatement(...)` into its batch **unconditionally**\n(`:324-331`), with `invitedAt` set when `body.invited` \u2014 which matches the observed\n\"CONFERENCE STATUS = Invited\" on his record. The membership write is not skipped.\n\nSo look downstream of the write:\n\n- `src/lib/roster-source.ts` \u2014 `SPEAKER_ROSTER_PERSON_SOURCE` is\n  `memberships WHERE event_id = ? AND role = 'speaker'` UNION participations on submissions\n  whose status is in `ROSTER_SUBMISSION_STATUSES`. A membership row alone is supposed to be\n  sufficient. Confirm it actually is, for this row, with the values `createSpeaker` writes.\n- `speakerMembershipStatement` (`src/lib/speaker-membership.ts:42`) is idempotent via a\n  conflict clause. Check what happens when a row for that (event, person) already exists\n  with a **different role** \u2014 `DO NOTHING` would leave no `speaker` row and produce exactly\n  this symptom.\n- The counts and the list may not read the same source. The header, the status tabs, and the\n  table rows must all resolve from `SPEAKER_ROSTER_PERSON_SOURCE`; if any of them has its\n  own query, that divergence is the bug and consolidating it is the fix.\n- The contrast is the strongest clue you have: **the CSV import path did move the count**\n  (Dana Kowalski, Pending 468\u2192+1) and the hand-add path did not. Diff those two writes\n  against each other before anything else.\n\n### Acceptance\n\n1. A speaker added by hand appears in the roster table **and** in the header count **and**\n   in the correct status tab, on the response to the very next read \u2014 no manual refresh\n   beyond normal navigation.\n2. The status tab he lands in matches the CONFERENCE STATUS his own record shows. Today the\n   record says Invited while the Invited tab does not move; those are the same fact and must\n   never disagree.\n3. Adding the same person twice does not double-count him.\n4. A regression test that fails on `main` and passes on the branch, driving the real\n   create-speaker route and then reading the roster list and counts \u2014 not a unit test of the\n   membership helper, which already passes today.\n\n---\n\n## Part 2 \u2014 the FILES panel contradicts itself (minor)\n\n### The judge's own words\n\n> **`/roster` speaker record \u2192 FILES panel.** The FILES panel on a speaker record reads\n> \"0 of 1 requested file received.\" immediately above a listed, speaker-provided file\n> (headshot.png, V1 OF 1, 569 B). The two statements read as contradictory to an organizer\n> scanning the record for what has arrived.\n\nAlmost certainly correct-but-unreadable rather than wrong: the headshot is a\n`person_headshot` attachment while the \"1 requested file\" is a `task_upload` deliverable, so\n\"0 of 1 received\" is true of the deliverable and says nothing about the headshot listed\nunder it. That is a defensible model and an indefensible sentence \u2014 the organizer reads one\npanel, not two data types.\n\n### Acceptance\n\nThe panel never states a received-count that its own visible contents contradict. Either\ncount what is listed, or separate the two groups so each carries its own honest line (a\nprofile photo is not a requested deliverable and should not be filed as if it were).\nWhichever you choose, an organizer scanning the panel must not have to know the schema to\nread it.\n\n---\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-174-roster-truth -b mrq-174-roster-truth main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** If Part 1 turns out to need a schema change, stop\n  and say so on this ticket rather than adding one.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Elements never jump** \u2014 if a count or a line swaps, reserve its space.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","priority":"high","short_id":"MRQ-176","status":"backlog","tags":["auto-eval","speaker-management","round9","craft"],"title":"The roster does not list a speaker added by hand, and the FILES panel contradicts itself","type":"bug","urgency":"high"},"id":"ev_01KZYRCTF1QR86KC8WS0F2SX9Q","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:47:52Z","type":"task_created"}
{"actor":"agent:eval-triage","data":{"field":"description","from":"Two round-9 defects (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`), one\nshape: **a count on the page contradicts what the page itself is showing.** Grouped because\nthe fix is the same discipline in two places, not because the code is shared.\n\n**No rubric item punishes either \u2014 speaker-management scored 15/15 this round.** They are on\nthe list because an organizer who catches the roster lying about its own size stops trusting\nevery other number in the product, and this one repeated and got worse.\n\n---\n\n## Part 1 \u2014 a hand-added speaker is not on the roster (major, escalated from round 4)\n\n### The judge's own words\n\n> **`/roster` \u2014 speaker count header and status tabs after \"Add speaker\".** The roster's\n> counts do not reflect a speaker added by hand. Before Marcus Okafor was created the tabs\n> read All 509 / Pending 468 / Invited 4 / Confirmed 36 / Declined 1; after he was saved\n> (his record opening with CONFERENCE STATUS = Invited) and after a subsequent full page\n> reload the tabs still read All 509 with Invited unchanged at 4, and the total only moved\n> to 510 when the CSV import created Dana Kowalski (Pending 468\u2192+1). The header \"N speakers\n> on the roster for this conference \u2014 everyone who submitted, was accepted, was imported, or\n> was added by hand\" therefore undercounts, and **the hand-added speaker's row was never\n> observed in the roster table** even though his record is reachable by URL, appears in the\n> task-assignment picker and received the bulk email.\n\nRound 4 recorded the same thing as a **minor** (\"Roster totals do not visibly account for\nnewly added or imported speakers\"). Round 9 is sharper and worse: this is not a stale\ncounter, the row is **absent from the list**. The person exists everywhere else in the\nproduct and is missing from the one page whose entire job is to list him.\n\n### What I checked, so you do not repeat it\n\nThe obvious cause is not the cause. `createSpeaker` (`src/routes/speakers.routes.ts:238`)\npushes `speakerMembershipStatement(...)` into its batch **unconditionally**\n(`:324-331`), with `invitedAt` set when `body.invited` \u2014 which matches the observed\n\"CONFERENCE STATUS = Invited\" on his record. The membership write is not skipped.\n\nSo look downstream of the write:\n\n- `src/lib/roster-source.ts` \u2014 `SPEAKER_ROSTER_PERSON_SOURCE` is\n  `memberships WHERE event_id = ? AND role = 'speaker'` UNION participations on submissions\n  whose status is in `ROSTER_SUBMISSION_STATUSES`. A membership row alone is supposed to be\n  sufficient. Confirm it actually is, for this row, with the values `createSpeaker` writes.\n- `speakerMembershipStatement` (`src/lib/speaker-membership.ts:42`) is idempotent via a\n  conflict clause. Check what happens when a row for that (event, person) already exists\n  with a **different role** \u2014 `DO NOTHING` would leave no `speaker` row and produce exactly\n  this symptom.\n- The counts and the list may not read the same source. The header, the status tabs, and the\n  table rows must all resolve from `SPEAKER_ROSTER_PERSON_SOURCE`; if any of them has its\n  own query, that divergence is the bug and consolidating it is the fix.\n- The contrast is the strongest clue you have: **the CSV import path did move the count**\n  (Dana Kowalski, Pending 468\u2192+1) and the hand-add path did not. Diff those two writes\n  against each other before anything else.\n\n### Acceptance\n\n1. A speaker added by hand appears in the roster table **and** in the header count **and**\n   in the correct status tab, on the response to the very next read \u2014 no manual refresh\n   beyond normal navigation.\n2. The status tab he lands in matches the CONFERENCE STATUS his own record shows. Today the\n   record says Invited while the Invited tab does not move; those are the same fact and must\n   never disagree.\n3. Adding the same person twice does not double-count him.\n4. A regression test that fails on `main` and passes on the branch, driving the real\n   create-speaker route and then reading the roster list and counts \u2014 not a unit test of the\n   membership helper, which already passes today.\n\n---\n\n## Part 2 \u2014 the FILES panel contradicts itself (minor)\n\n### The judge's own words\n\n> **`/roster` speaker record \u2192 FILES panel.** The FILES panel on a speaker record reads\n> \"0 of 1 requested file received.\" immediately above a listed, speaker-provided file\n> (headshot.png, V1 OF 1, 569 B). The two statements read as contradictory to an organizer\n> scanning the record for what has arrived.\n\nAlmost certainly correct-but-unreadable rather than wrong: the headshot is a\n`person_headshot` attachment while the \"1 requested file\" is a `task_upload` deliverable, so\n\"0 of 1 received\" is true of the deliverable and says nothing about the headshot listed\nunder it. That is a defensible model and an indefensible sentence \u2014 the organizer reads one\npanel, not two data types.\n\n### Acceptance\n\nThe panel never states a received-count that its own visible contents contradict. Either\ncount what is listed, or separate the two groups so each carries its own honest line (a\nprofile photo is not a requested deliverable and should not be filed as if it were).\nWhichever you choose, an organizer scanning the panel must not have to know the schema to\nread it.\n\n---\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-174-roster-truth -b mrq-174-roster-truth main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** If Part 1 turns out to need a schema change, stop\n  and say so on this ticket rather than adding one.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Elements never jump** \u2014 if a count or a line swaps, reserve its space.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","to":"Two round-9 defects (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`), one\nshape: **a count on the page contradicts what the page itself is showing.** Grouped because\nthe fix is the same discipline in two places, not because the code is shared.\n\n**No rubric item punishes either \u2014 speaker-management scored 15/15 this round.** They are on\nthe list because an organizer who catches the roster lying about its own size stops trusting\nevery other number in the product, and this one repeated and got worse.\n\n---\n\n## Part 1 \u2014 a hand-added speaker is not on the roster (major, escalated from round 4)\n\n### The judge's own words\n\n> **`/roster` \u2014 speaker count header and status tabs after \"Add speaker\".** The roster's\n> counts do not reflect a speaker added by hand. Before Marcus Okafor was created the tabs\n> read All 509 / Pending 468 / Invited 4 / Confirmed 36 / Declined 1; after he was saved\n> (his record opening with CONFERENCE STATUS = Invited) and after a subsequent full page\n> reload the tabs still read All 509 with Invited unchanged at 4, and the total only moved\n> to 510 when the CSV import created Dana Kowalski (Pending 468\u2192+1). The header \"N speakers\n> on the roster for this conference \u2014 everyone who submitted, was accepted, was imported, or\n> was added by hand\" therefore undercounts, and **the hand-added speaker's row was never\n> observed in the roster table** even though his record is reachable by URL, appears in the\n> task-assignment picker and received the bulk email.\n\nRound 4 recorded the same thing as a **minor** (\"Roster totals do not visibly account for\nnewly added or imported speakers\"). Round 9 is sharper and worse: this is not a stale\ncounter, the row is **absent from the list**. The person exists everywhere else in the\nproduct and is missing from the one page whose entire job is to list him.\n\n### What I checked, so you do not repeat it\n\nThe obvious cause is not the cause. `createSpeaker` (`src/routes/speakers.routes.ts:238`)\npushes `speakerMembershipStatement(...)` into its batch **unconditionally**\n(`:324-331`), with `invitedAt` set when `body.invited` \u2014 which matches the observed\n\"CONFERENCE STATUS = Invited\" on his record. The membership write is not skipped.\n\nSo look downstream of the write:\n\n- `src/lib/roster-source.ts` \u2014 `SPEAKER_ROSTER_PERSON_SOURCE` is\n  `memberships WHERE event_id = ? AND role = 'speaker'` UNION participations on submissions\n  whose status is in `ROSTER_SUBMISSION_STATUSES`. A membership row alone is supposed to be\n  sufficient. Confirm it actually is, for this row, with the values `createSpeaker` writes.\n- `speakerMembershipStatement` (`src/lib/speaker-membership.ts:42`) is idempotent via a\n  conflict clause. Check what happens when a row for that (event, person) already exists\n  with a **different role** \u2014 `DO NOTHING` would leave no `speaker` row and produce exactly\n  this symptom.\n- The counts and the list may not read the same source. The header, the status tabs, and the\n  table rows must all resolve from `SPEAKER_ROSTER_PERSON_SOURCE`; if any of them has its\n  own query, that divergence is the bug and consolidating it is the fix.\n- The contrast is the strongest clue you have: **the CSV import path did move the count**\n  (Dana Kowalski, Pending 468\u2192+1) and the hand-add path did not. Diff those two writes\n  against each other before anything else.\n\n### Acceptance\n\n1. A speaker added by hand appears in the roster table **and** in the header count **and**\n   in the correct status tab, on the response to the very next read \u2014 no manual refresh\n   beyond normal navigation.\n2. The status tab he lands in matches the CONFERENCE STATUS his own record shows. Today the\n   record says Invited while the Invited tab does not move; those are the same fact and must\n   never disagree.\n3. Adding the same person twice does not double-count him.\n4. A regression test that fails on `main` and passes on the branch, driving the real\n   create-speaker route and then reading the roster list and counts \u2014 not a unit test of the\n   membership helper, which already passes today.\n\n---\n\n## Part 2 \u2014 the FILES panel contradicts itself (minor)\n\n### The judge's own words\n\n> **`/roster` speaker record \u2192 FILES panel.** The FILES panel on a speaker record reads\n> \"0 of 1 requested file received.\" immediately above a listed, speaker-provided file\n> (headshot.png, V1 OF 1, 569 B). The two statements read as contradictory to an organizer\n> scanning the record for what has arrived.\n\nAlmost certainly correct-but-unreadable rather than wrong: the headshot is a\n`person_headshot` attachment while the \"1 requested file\" is a `task_upload` deliverable, so\n\"0 of 1 received\" is true of the deliverable and says nothing about the headshot listed\nunder it. That is a defensible model and an indefensible sentence \u2014 the organizer reads one\npanel, not two data types.\n\n### Acceptance\n\nThe panel never states a received-count that its own visible contents contradict. Either\ncount what is listed, or separate the two groups so each carries its own honest line (a\nprofile photo is not a requested deliverable and should not be filed as if it were).\nWhichever you choose, an organizer scanning the panel must not have to know the schema to\nread it.\n\n---\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-176-roster-truth -b mrq-176-roster-truth main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** If Part 1 turns out to need a schema change, stop\n  and say so on this ticket rather than adding one.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Elements never jump** \u2014 if a count or a line swaps, reserve its space.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`."},"id":"ev_01KZYRD15SSSH3K13MT8B9CGRD","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:47:59Z","type":"field_updated"}
{"actor":"agent:codex-mrq-176","data":{"from":null,"to":"agent:codex-mrq-176"},"id":"ev_01KZYRGD4D38RJ6RY98W5AP37Q","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:49:49Z","type":"assignment_changed"}
{"actor":"agent:codex-mrq-176","data":{"from":"backlog","to":"in_planning"},"id":"ev_01KZYRGD4D38RJ6RY98W5AP37R","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:49:49Z","type":"status_changed"}
{"actor":"agent:codex-mrq-176","data":{"from":"in_planning","to":"planned"},"id":"ev_01KZYRGSGV0GHPDDEWG17WG201","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:50:02Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-plan-review-task_01KZYRCTEZ59PJH2A3FBE1EKCX.log","mode":"single","pid":17542,"review_type":"plan-review","spawned_at":"2026-08-13T23:50:02Z","trigger_status_event_id":"ev_01KZYRGSGV0GHPDDEWG17WG201"},"id":"ev_01KZYRGSH0SQ7B1KB8W81N3GZK","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:50:02Z","type":"auto_review_spawned"}
{"actor":"agent:codex-mrq-176","data":{"from":"planned","to":"in_progress"},"id":"ev_01KZYRGSM2BB4PE25R4K29HF2N","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:50:02Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"artifact_id":"art_01KZYRN4VPRTN4NHRHPP5ASC6Y","role":"plan-review"},"id":"ev_01KZYRN4VTEZ9MF5KSR08AQMEK","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-13T23:52:25Z","type":"artifact_attached"}
{"actor":"agent:eval-triage","data":{"body":"STRONGER EVIDENCE from the ai-agenda judgement, same run \u2014 this is very likely one root cause with two symptoms, and the second one is on a public surface.\n\nThe ai-agenda judge recorded (minor, /agenda?event=aie-ny-2026, Mon Oct 12 09:00 card):\n\n  'On the published public agenda, the card for \"Lightning: Agents in Production Q&A\" shows an em dash where the speaker line belongs, although the same session lists \"Marcus Okafor\" as its speaker in the builder tile and in the publish review list. Sibling cards on the same page (e.g. \"Advanced AI Engineering with AI SDK\" \u2014 Nico Albanese) render speaker name, title and company correctly, so the speaker data is dropped for this record rather than absent by design.'\n\nMarcus Okafor is the same hand-added speaker this ticket is about. Trace the pattern before you fix either half:\n\n- present: the agenda builder tile, the publish review list, the task-assignment picker, the bulk email audience, his own record by URL\n- absent: the /roster table and its counts, and now the speaker line on the public agenda card\n\nEverything that finds him reads from the submission/participation side. Everything that loses him looks like it reads through the speaker-roster/membership path. Sibling cards render their speakers correctly, so the public projection works in general \u2014 it drops this person specifically.\n\nIf that holds, fixing the membership/roster resolution fixes the public agenda card for free, and a fix that only patches the /roster list would leave a speaker missing from the page the public reads. Please check the public agenda projection's speaker source as part of this ticket and say on the ticket whether it shares the cause. If it turns out to be independent, say so and I will mint it separately rather than widening this."},"id":"ev_01KZYSY1CVGSD2S4AAQ95WVPSV","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:14:45Z","type":"comment_added"}
{"actor":"agent:codex-mrq-176","data":{"body":"Root cause: speakerMembershipStatement used ON CONFLICT DO NOTHING, so re-adding an existing pending speaker with invited:true discarded invited_at and left the record and roster status tab pending; the FILES summary also counted task uploads without naming that scope. Verified by A/B with npx vitest run tests/integration/api/speakers-roster.MRQ-111.test.ts -t MRQ-176: main behavior fails Expected invited / Received pending, while this branch passes; focused roster/files/UI tests, TypeScript, and production build also pass. PR #203, head f7d27701; no migration or deployment performed."},"id":"ev_01KZYTAPFJ8AW1VWM374K3W61V","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:21:39Z","type":"comment_added"}
{"actor":"agent:codex-mrq-176","data":{"from":"in_progress","to":"review"},"id":"ev_01KZYTAYKB4F19QT2RNAS55J9E","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:21:48Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-code-review-task_01KZYRCTEZ59PJH2A3FBE1EKCX.log","mode":"single","pid":8913,"review_type":"code-review","spawned_at":"2026-08-14T00:21:48Z","trigger_status_event_id":"ev_01KZYTAYKB4F19QT2RNAS55J9E"},"id":"ev_01KZYTAYKMD6GPMEGVVSYFTB8T","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:21:48Z","type":"auto_review_spawned"}
{"actor":"agent:eval-triage","data":{"field":"description","from":"Two round-9 defects (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`), one\nshape: **a count on the page contradicts what the page itself is showing.** Grouped because\nthe fix is the same discipline in two places, not because the code is shared.\n\n**No rubric item punishes either \u2014 speaker-management scored 15/15 this round.** They are on\nthe list because an organizer who catches the roster lying about its own size stops trusting\nevery other number in the product, and this one repeated and got worse.\n\n---\n\n## Part 1 \u2014 a hand-added speaker is not on the roster (major, escalated from round 4)\n\n### The judge's own words\n\n> **`/roster` \u2014 speaker count header and status tabs after \"Add speaker\".** The roster's\n> counts do not reflect a speaker added by hand. Before Marcus Okafor was created the tabs\n> read All 509 / Pending 468 / Invited 4 / Confirmed 36 / Declined 1; after he was saved\n> (his record opening with CONFERENCE STATUS = Invited) and after a subsequent full page\n> reload the tabs still read All 509 with Invited unchanged at 4, and the total only moved\n> to 510 when the CSV import created Dana Kowalski (Pending 468\u2192+1). The header \"N speakers\n> on the roster for this conference \u2014 everyone who submitted, was accepted, was imported, or\n> was added by hand\" therefore undercounts, and **the hand-added speaker's row was never\n> observed in the roster table** even though his record is reachable by URL, appears in the\n> task-assignment picker and received the bulk email.\n\nRound 4 recorded the same thing as a **minor** (\"Roster totals do not visibly account for\nnewly added or imported speakers\"). Round 9 is sharper and worse: this is not a stale\ncounter, the row is **absent from the list**. The person exists everywhere else in the\nproduct and is missing from the one page whose entire job is to list him.\n\n### What I checked, so you do not repeat it\n\nThe obvious cause is not the cause. `createSpeaker` (`src/routes/speakers.routes.ts:238`)\npushes `speakerMembershipStatement(...)` into its batch **unconditionally**\n(`:324-331`), with `invitedAt` set when `body.invited` \u2014 which matches the observed\n\"CONFERENCE STATUS = Invited\" on his record. The membership write is not skipped.\n\nSo look downstream of the write:\n\n- `src/lib/roster-source.ts` \u2014 `SPEAKER_ROSTER_PERSON_SOURCE` is\n  `memberships WHERE event_id = ? AND role = 'speaker'` UNION participations on submissions\n  whose status is in `ROSTER_SUBMISSION_STATUSES`. A membership row alone is supposed to be\n  sufficient. Confirm it actually is, for this row, with the values `createSpeaker` writes.\n- `speakerMembershipStatement` (`src/lib/speaker-membership.ts:42`) is idempotent via a\n  conflict clause. Check what happens when a row for that (event, person) already exists\n  with a **different role** \u2014 `DO NOTHING` would leave no `speaker` row and produce exactly\n  this symptom.\n- The counts and the list may not read the same source. The header, the status tabs, and the\n  table rows must all resolve from `SPEAKER_ROSTER_PERSON_SOURCE`; if any of them has its\n  own query, that divergence is the bug and consolidating it is the fix.\n- The contrast is the strongest clue you have: **the CSV import path did move the count**\n  (Dana Kowalski, Pending 468\u2192+1) and the hand-add path did not. Diff those two writes\n  against each other before anything else.\n\n### Acceptance\n\n1. A speaker added by hand appears in the roster table **and** in the header count **and**\n   in the correct status tab, on the response to the very next read \u2014 no manual refresh\n   beyond normal navigation.\n2. The status tab he lands in matches the CONFERENCE STATUS his own record shows. Today the\n   record says Invited while the Invited tab does not move; those are the same fact and must\n   never disagree.\n3. Adding the same person twice does not double-count him.\n4. A regression test that fails on `main` and passes on the branch, driving the real\n   create-speaker route and then reading the roster list and counts \u2014 not a unit test of the\n   membership helper, which already passes today.\n\n---\n\n## Part 2 \u2014 the FILES panel contradicts itself (minor)\n\n### The judge's own words\n\n> **`/roster` speaker record \u2192 FILES panel.** The FILES panel on a speaker record reads\n> \"0 of 1 requested file received.\" immediately above a listed, speaker-provided file\n> (headshot.png, V1 OF 1, 569 B). The two statements read as contradictory to an organizer\n> scanning the record for what has arrived.\n\nAlmost certainly correct-but-unreadable rather than wrong: the headshot is a\n`person_headshot` attachment while the \"1 requested file\" is a `task_upload` deliverable, so\n\"0 of 1 received\" is true of the deliverable and says nothing about the headshot listed\nunder it. That is a defensible model and an indefensible sentence \u2014 the organizer reads one\npanel, not two data types.\n\n### Acceptance\n\nThe panel never states a received-count that its own visible contents contradict. Either\ncount what is listed, or separate the two groups so each carries its own honest line (a\nprofile photo is not a requested deliverable and should not be filed as if it were).\nWhichever you choose, an organizer scanning the panel must not have to know the schema to\nread it.\n\n---\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-176-roster-truth -b mrq-176-roster-truth main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** If Part 1 turns out to need a schema change, stop\n  and say so on this ticket rather than adding one.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Elements never jump** \u2014 if a count or a line swaps, reserve its space.\n- Gate serialized. macOS has no `flock(1)`; wrap it, e.g.\n  `python3 -c 'import fcntl,subprocess,sys; f=open(\"/tmp/marquee-gate.lock\",\"w\"); fcntl.flock(f, fcntl.LOCK_EX); sys.exit(subprocess.run([\"npm\",\"run\",\"pr-gate\"]).returncode)'`\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`.","to":"Two round-9 defects (`runs/2026-08-13T22-25-26/judgements/speaker-management.json`), one\nshape: **a count on the page contradicts what the page itself is showing.** Grouped because\nthe fix is the same discipline in two places, not because the code is shared.\n\n**No rubric item punishes either \u2014 speaker-management scored 15/15 this round.** They are on\nthe list because an organizer who catches the roster lying about its own size stops trusting\nevery other number in the product, and this one repeated and got worse.\n\n---\n\n## Part 1 \u2014 a hand-added speaker is not on the roster (major, escalated from round 4)\n\n### The judge's own words\n\n> **`/roster` \u2014 speaker count header and status tabs after \"Add speaker\".** The roster's\n> counts do not reflect a speaker added by hand. Before Marcus Okafor was created the tabs\n> read All 509 / Pending 468 / Invited 4 / Confirmed 36 / Declined 1; after he was saved\n> (his record opening with CONFERENCE STATUS = Invited) and after a subsequent full page\n> reload the tabs still read All 509 with Invited unchanged at 4, and the total only moved\n> to 510 when the CSV import created Dana Kowalski (Pending 468\u2192+1). The header \"N speakers\n> on the roster for this conference \u2014 everyone who submitted, was accepted, was imported, or\n> was added by hand\" therefore undercounts, and **the hand-added speaker's row was never\n> observed in the roster table** even though his record is reachable by URL, appears in the\n> task-assignment picker and received the bulk email.\n\nRound 4 recorded the same thing as a **minor** (\"Roster totals do not visibly account for\nnewly added or imported speakers\"). Round 9 is sharper and worse: this is not a stale\ncounter, the row is **absent from the list**. The person exists everywhere else in the\nproduct and is missing from the one page whose entire job is to list him.\n\n### What I checked, so you do not repeat it\n\nThe obvious cause is not the cause. `createSpeaker` (`src/routes/speakers.routes.ts:238`)\npushes `speakerMembershipStatement(...)` into its batch **unconditionally**\n(`:324-331`), with `invitedAt` set when `body.invited` \u2014 which matches the observed\n\"CONFERENCE STATUS = Invited\" on his record. The membership write is not skipped.\n\nSo look downstream of the write:\n\n- `src/lib/roster-source.ts` \u2014 `SPEAKER_ROSTER_PERSON_SOURCE` is\n  `memberships WHERE event_id = ? AND role = 'speaker'` UNION participations on submissions\n  whose status is in `ROSTER_SUBMISSION_STATUSES`. A membership row alone is supposed to be\n  sufficient. Confirm it actually is, for this row, with the values `createSpeaker` writes.\n- `speakerMembershipStatement` (`src/lib/speaker-membership.ts:42`) is idempotent via a\n  conflict clause. Check what happens when a row for that (event, person) already exists\n  with a **different role** \u2014 `DO NOTHING` would leave no `speaker` row and produce exactly\n  this symptom.\n- The counts and the list may not read the same source. The header, the status tabs, and the\n  table rows must all resolve from `SPEAKER_ROSTER_PERSON_SOURCE`; if any of them has its\n  own query, that divergence is the bug and consolidating it is the fix.\n- The contrast is the strongest clue you have: **the CSV import path did move the count**\n  (Dana Kowalski, Pending 468\u2192+1) and the hand-add path did not. Diff those two writes\n  against each other before anything else.\n\n### Acceptance\n\n1. A speaker added by hand appears in the roster table **and** in the header count **and**\n   in the correct status tab, on the response to the very next read \u2014 no manual refresh\n   beyond normal navigation.\n2. The status tab he lands in matches the CONFERENCE STATUS his own record shows. Today the\n   record says Invited while the Invited tab does not move; those are the same fact and must\n   never disagree.\n3. Adding the same person twice does not double-count him.\n4. A regression test that fails on `main` and passes on the branch, driving the real\n   create-speaker route and then reading the roster list and counts \u2014 not a unit test of the\n   membership helper, which already passes today.\n\n---\n\n## Part 2 \u2014 the FILES panel contradicts itself (minor)\n\n### The judge's own words\n\n> **`/roster` speaker record \u2192 FILES panel.** The FILES panel on a speaker record reads\n> \"0 of 1 requested file received.\" immediately above a listed, speaker-provided file\n> (headshot.png, V1 OF 1, 569 B). The two statements read as contradictory to an organizer\n> scanning the record for what has arrived.\n\nAlmost certainly correct-but-unreadable rather than wrong: the headshot is a\n`person_headshot` attachment while the \"1 requested file\" is a `task_upload` deliverable, so\n\"0 of 1 received\" is true of the deliverable and says nothing about the headshot listed\nunder it. That is a defensible model and an indefensible sentence \u2014 the organizer reads one\npanel, not two data types.\n\n### Acceptance\n\nThe panel never states a received-count that its own visible contents contradict. Either\ncount what is listed, or separate the two groups so each carries its own honest line (a\nprofile photo is not a requested deliverable and should not be filed as if it were).\nWhichever you choose, an organizer scanning the panel must not have to know the schema to\nread it.\n\n---\n\n## Constraints\n\n- Your **own linked worktree**, created first:\n  `git worktree add ../Marquee-worktrees/mrq-176-roster-truth -b mrq-176-roster-truth main`.\n  Verify with `pwd` and `git branch --show-current`. Never the primary checkout (it is the\n  Lattice board's home); never `mrq-auto-eval*`.\n- **Never `git stash` anywhere in this repo** \u2014 the stash stack is shared across worktrees.\n- **No migration without the operator.** If Part 1 turns out to need a schema change, stop\n  and say so on this ticket rather than adding one.\n- **Do not deploy.** An eval round is running; a `.deploy-freeze` marker sits at the primary\n  checkout. Merging is wanted; deploying is not, and is not yours.\n- **Elements never jump** \u2014 if a count or a line swaps, reserve its space.\n- **Gate serialization is a fleet rule, not an option.** Route EVERY `npm run pr-gate` and EVERY full\n  `npm test` through `/Users/atin/Projects/Stage11/deployments/Marquee-worktrees/.gate-lock/gate-lock.sh`,\n  including one-off \"just checking\" runs \u2014 a lock only serializes its participants, so a single\n  unlocked run lands on top of a lock-holder and inflates it. Cheap scoped things (tsc, one test\n  file) stay unlocked.\n- **A timeout and a red are indistinguishable in an unserialized run.** Check `uptime` before you\n  believe a red; load has ranged 14 to 164 tonight and the same branch measured 78s and 276s\n  against a 120s budget. If your only evidence for a defect is \"it timed out\", you have no\n  evidence. CI fast-gate runs on a quiet box and is tonight's oracle.\n- PR: `gh pr create --repo Stage-11-Agentics/marquee --base main`."},"id":"ev_01KZYTGV8V7CBJ2WVCKK8ZKB7Z","provenance":{"reason":"fleet gate-lock rule"},"schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:25:01Z","type":"field_updated"}
{"actor":"agent:codex-mrq-176","data":{"body":"Gate correction: checked uptime first (load 63.17 / 47.75 / 34.79), then ran /Users/atin/Projects/Stage11/deployments/Marquee-worktrees/.gate-lock/gate-lock.sh npm run pr-gate -- --ticket MRQ-176. The shared lock waited 240s; at acquisition load was 37.52 / 47.92 / 38.84. Types, build, shell/design/API/routes/clocks/schema passed; the hermetic suite reproduced 980 passing and the same 22 missing-credential auth failures in 63.9s, so this is not a timing-only red. CI fast-gate remains the quiet-box oracle. No deployment, migration, or freeze change."},"id":"ev_01KZYTRJMXKKRVR40P6HWBGTVA","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:29:14Z","type":"comment_added"}
{"actor":"agent:eval-triage","data":{"body":"MY CROSS-REFERENCE HYPOTHESIS WAS WRONG \u2014 retracting it so it does not widen your scope or mislead your review.\n\nI told you the public-agenda em dash on Marcus Okafor's card was probably one root cause with your roster defect. Having traced it, it is not, and your ON CONFLICT DO NOTHING / invited_at diagnosis looks right and complete for THIS ticket. Your account also explains the judge's evidence better than mine did: if Marcus already existed as a pending speaker, All correctly stays 509 and Pending correctly stays 468, while Invited should have gone 4 -> 5 and did not. That is exactly the invited_at discard, and 'his row was never observed in the roster table' resolves to the judge looking in the Invited tab where he was not.\n\nThe public-agenda symptom has a different cause, in src/lib/participants.ts:40-44. roleFilterSql applies NO role filter for audience 'program' and restricts audience 'public' to SPEAKING_PARTICIPATION_ROLES (speaker, co_speaker, moderator, chairperson). The agenda builder tile and the publish review list read 'program'; the public agenda card reads 'public'. So a participation in a non-speaking role \u2014 submitter, most likely \u2014 shows in the builder and is correctly filtered out of the public card, which then renders a bare em dash. Different mechanism, different file, and the filtering itself is right.\n\nSo: do not widen this PR. I am minting the public-agenda em dash separately. Nothing you need to do \u2014 this note exists because I put the hypothesis on your ticket and it would have cost you time."},"id":"ev_01KZYV6EJB3HYHRY2RT9XAC77N","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:36:49Z","type":"comment_added"}
{"actor":"agent:eval-triage","data":{"body":"Reviewed as a non-author and merged as PR #203.\n\nRoot cause accepted and it is better than the one I hypothesised. ON CONFLICT DO NOTHING discarded invited_at when re-adding a person who already held a pending speaker membership, so the record read Invited while the roster's Invited tab did not move. That also explains the judge's counts exactly \u2014 All correctly stays 509 and Pending correctly stays 468 for someone already on the roster, while Invited should have gone 4 -> 5 and did not, and 'his row was never observed in the roster table' resolves to the judge looking in the Invited tab where he was not. My reading of the evidence was worse than yours.\n\nVerified the one thing that could fail at runtime rather than in tests: the new conflict target (org_id, event_id, person_id, role) WHERE event_id IS NOT NULL matches uq_memberships_event at migrations/0001_init.sql:755-757 exactly, including the partial predicate \u2014 without it SQLite would not match the partial index at all. COALESCE(memberships.invited_at, excluded.invited_at) keeps the first invitation rather than overwriting it, and confirmation_status and confirmed_at are untouched, so the docstring's promise that a re-add cannot clear a decline still holds.\n\nPart 2 does what was asked: the FILES panel now states requested deliverables and the profile photo as separate lines with their own counts, so the '0 of 1 received' sentence can no longer contradict a headshot listed underneath it, and .speaker-files-summary reserves min-heights so the block does not reflow as the values swap.\n\nTwo CONTRACT tests, no migration, CI fast-gate green. Retracted separately: the public-agenda em dash is participants.ts audience filtering, not this bug, and is now MRQ-185.","role":"review"},"id":"ev_01KZYVMYTQTYR9KSASNYM89R3D","schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:44:44Z","type":"comment_added"}
{"actor":"agent:eval-triage","data":{"from":"review","to":"done"},"id":"ev_01KZYVNK1ZK6JN8GKTRSFDJGC4","provenance":{"reason":"PR #203 merged at 00:44Z after non-author review"},"schema_version":1,"task_id":"task_01KZYRCTEZ59PJH2A3FBE1EKCX","ts":"2026-08-14T00:45:05Z","type":"status_changed"}
