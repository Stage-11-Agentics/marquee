{"actor":"agent:claude","data":{"complexity":"high","description":"WALKTHROUGH-BLOCKER. The app shows conflicting pipeline counts for the same event across four surfaces, and a judge notices inside two clicks. Full evidence: sequence/UX-SWEEP-FINDINGS.md rows 1-2 and section 4b (counts re-verified directly against the API, so these are backend defects, not rendering artifacts).\n\n## Why this exists\n\nThe pipeline IS the product. SPEC.md section 1 makes it the organizer's home: seven stages, each a count, each clickable into the work behind it. When the hero says 960 Submitted and the sidebar's Submitted filter opens an EMPTY LIST, the core promise of the screen is broken in the most visible way available. Right now four of the seven stages disagree, one by more than 10x.\n\n## The observed disagreement\n\n| Stage | Landing `/` | `/dashboard` | `/board` | `/submissions?status=` | True |\n|---|---|---|---|---|---|\n| Submitted | 960 | 0 | 0 | 0 | no rows have status='submitted' |\n| Waved | \u2014 | 60 | **620** | 60 | no rows have status='waved' |\n| Onboarding | 153 | 58 | \u2014 | \u2014 | 153 is people, 58 is submissions |\n| Scheduled | 24 | 1 | \u2014 | 1 | 1 |\n| In review | 280 | 280 | 280 | 280 | 280 (agrees) |\n| Published | 23 | 23 | 23 | 23 | 23 (agrees) |\n\nTrue status distribution across all 1,000 seeded submissions: rejected 550, in_review 280, waitlisted 70, draft 40, accepted 36, published 23, scheduled 1. There is no `submitted` status and no `waved` status in the data at all.\n\n## Root cause \u2014 THREE competing derivations, two of them rogue\n\n`src/routes/submissions.queries.ts:79-97` declares the intended contract in its own doc comment: \"One status vocabulary powers list filtering and dashboard instruments. A dashboard tile therefore cannot count a different set than its destination.\" `submissionStatusPredicate` is that vocabulary, and `/dashboard` correctly consumes it (`dashboardStageSql`, dashboard.routes.ts:77-81). Two surfaces bypass it entirely:\n\n1. **The landing page rolls its own SQL.** `src/routes/landing.route.tsx:56-101` is one hand-written D1 read: `submitted_count` is `status <> 'draft'` (cumulative, 960), `scheduled_count` is every agenda_item (24, includes published), `onboarding_count` is `COUNT(DISTINCT person_id)` (people, 153) where the dashboard counts submissions (58). Every one of these is a different question than the sidebar filter answers under the same label.\n\n2. **The board rolls its own CASE.** `BOARD_STAGE_SQL` at `src/api/board.ts:143-157` maps `s.status IN ('waitlisted','rejected','withdrawn') THEN 'waved'` \u2014 550 + 70 = **620**. Its doc comment is explicit that this is deliberate (\"A rejected or waitlisted record remains visible in Waved: the board has no destructive terminal column of its own\"), but it directly contradicts SPEC.md section 1, which specs the board as a read-only Kanban across THE SEVEN LIFECYCLE STAGES \u2014 the same seven, not a private vocabulary. The board needed a bucket for decided-and-not-accepted records and took the Waved label instead of getting a ruling.\n\n3. **`waved` is broken inside the shared vocabulary too.** `submissions.queries.ts:95` \u2014 `wave_id IS NOT NULL AND status = 'accepted'`. Every accepted record in the seed has a wave_id, so `?status=waved` and `?status=accepted` return the IDENTICAL 60 records. The Waved filter cannot show a single actually-waved record. `dashboard.routes.ts:64` labels this stage \"Decision wave prepared\", which is a THIRD meaning again.\n\n## The semantic ruling this ticket must make first\n\nDo not patch counts surface by surface \u2014 that is how this was introduced and it will regress. Rule on the semantics, then make every surface read one derivation.\n\n- **Is a stage cumulative or literal?** Landing reads cumulative (960 = everything past draft), the filter reads literal (0 = status='submitted'). Recommendation: STAGES ARE LITERAL \u2014 a pipeline where a record sits in exactly one stage is the one an organizer can act on, and it is the only reading where a count and the list behind it can agree. If the landing hero wants the cumulative headline, it must be RELABELED (e.g. \"960 submissions received\"), never labeled with a stage name.\n- **What does Waved mean?** Recommendation: records assigned to a decision wave whose decision has not yet been sent \u2014 the SPEC ordering `In Review -> Waved -> Accepted` only makes sense as a staging area BEFORE the decision lands. Under that reading `/board`'s decided-not-accepted bucket needs its own honest column (Declined / Not advancing), which is a SPEC-visible change: state it in the PR.\n- **Onboarding: people or submissions?** Pick one and use it in both places. The `/onboarding` screen's own copy says \"153 accepted speakers still owe something\" \u2014 people. Recommendation: make the noun explicit in the label wherever the count appears.\n\nIf your investigation contradicts a recommendation above, take your reading and say why in the PR. What is NOT acceptable is four surfaces continuing to answer four different questions under one label.\n\n## Also in scope \u2014 same root cause, do not split\n\n`src/routes/submission-record.routes.ts:406` \u2014 `can_decide: [\"submitted\",\"in_review\",\"accepted\",\"waitlisted\"].includes(row.status)`. Scheduled and published records still carry `status='accepted'` in D1 (their stage is DERIVED from agenda_items), so an already-scheduled talk renders live Accept / Maybe / Reject buttons with no indication a decision was made and acted on. Reproduce at `/submissions/sub_what-rl-means-for-agents` \u2014 \"Scheduled\" pill top-right, decision buttons below. This is the SAME defect class: reading the raw `status` column where the derived stage is what the reader means. Fix it with the derivation, not with a special case.\n\n## Scope\n\n- One shared stage derivation. `submissionStatusPredicate` is the natural home; landing and board consume it rather than hand-rolling SQL. Where a surface genuinely needs a different question (a cumulative hero number), it gets a DIFFERENT LABEL, not a different definition of the same word.\n- Fix `waved` so it selects a set that is not identical to `accepted`.\n- Reconcile `/board`'s stage vocabulary with SPEC.md section 1.\n- Gate decision affordances on the derived stage.\n- Keep the landing page's one-D1-read property (landing.route.tsx:52-55 documents it deliberately) \u2014 share the derivation, do not add round trips.\n\n## Constraints\n\n- DESIGN.md / Flight Deck tokens; the build reproduces the binding prototype one-to-one. Prototype-to-product fidelity is a taste rule (SPEC.md preamble), so a label change must be justified against the prototype, not invented.\n- ELEMENTS NEVER JUMP. These counts change under the reader \u2014 tabular numerals, reserved space, constant row counts.\n- Speed is a graded feature (R7). The landing read is on the public path; do not regress it. `npm run check:speed` must stay green.\n- No new D1 table and no migration. This is a derivation defect over columns that already exist.\n- If you add or change an API route, `npx vite build && node cli/generate-api-registry.mjs` \u2014 `check:api` asserts exact registry parity.\n- The fast suite has a 29s hard kill and a 30s budget. Worker-free unit tests in `tests/unit`, Worker-backed in `tests/integration`.\n\n## Verification\n\n1. **NO TEST CURRENTLY PINS ANY OF THIS** \u2014 `grep -rl waved tests/` returns nothing. The regression net is part of the deliverable, not optional. Add a test that asserts, per stage, that the dashboard tile count EQUALS the row count of the list its href opens. That single invariant is what was missing and is what makes this bug unrepeatable.\n2. Unit tests on the derivation itself, fixture-driven, no Worker: each stage's predicate against a known distribution; `waved` and `accepted` must return DIFFERENT sets.\n3. Integration test: landing counts, dashboard counts, board column counts, and filtered list totals agree stage by stage for one seeded event.\n4. `npm run pr-gate -- --ticket <this ticket>` \u2014 all checks green, including `check:seed` and `check:speed`.\n5. REAL-ARTIFACT SMOKE, non-negotiable. `wrangler dev` against the full seed. Walk `/` -> `/dashboard` -> `/board` -> each `/submissions?status=` filter and confirm every stage agrees across all four, that clicking any count opens a list whose \"N matching records\" matches the count clicked, and that NO pipeline stage links to an empty list. Then open `/submissions/sub_what-rl-means-for-agents` and confirm the decision buttons are gone or visibly resolved. Numbers agreeing in a unit test proves nothing here \u2014 the defect is cross-surface and only the walkthrough shows it.\n\n## Delivery\n\nOwn git worktree, branch `mrq-75-pipeline-stage-derivation`. PR via `gh pr create --repo Stage-11-Agentics/marquee --base main`.\n\n## File ownership (MRQ-75 / MRQ-76 / MRQ-77 run in parallel)\n\nMRQ-75 OWNS: `src/routes/submissions.queries.ts`, `src/routes/landing.route.tsx`, `src/api/board.ts`, `src/routes/board.routes.ts`, `src/routes/dashboard.routes.ts`, `src/api/dashboard.ts`, `src/routes/submission-record.routes.ts`, its own tests.\nMRQ-75 MUST NOT TOUCH: `src/ui/evaluation/*`, `src/ui/submissions/record.css`, `src/ui/submissions/SubmissionRecordPage.tsx` (MRQ-76), `src/routes/tokens.routes.ts`, `scripts/seed/evaluations.ts` (MRQ-77), `package.json`.","priority":"critical","short_id":"MRQ-76","status":"backlog","title":"Pipeline stage counts disagree across four surfaces \u2014 one derivation, one vocabulary","type":"bug"},"id":"ev_01KZS27XAW2GCCAYRCY85QE00Q","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T18:44:30Z","type":"task_created"}
{"actor":"agent:claude","data":{"field":"description","from":"WALKTHROUGH-BLOCKER. The app shows conflicting pipeline counts for the same event across four surfaces, and a judge notices inside two clicks. Full evidence: sequence/UX-SWEEP-FINDINGS.md rows 1-2 and section 4b (counts re-verified directly against the API, so these are backend defects, not rendering artifacts).\n\n## Why this exists\n\nThe pipeline IS the product. SPEC.md section 1 makes it the organizer's home: seven stages, each a count, each clickable into the work behind it. When the hero says 960 Submitted and the sidebar's Submitted filter opens an EMPTY LIST, the core promise of the screen is broken in the most visible way available. Right now four of the seven stages disagree, one by more than 10x.\n\n## The observed disagreement\n\n| Stage | Landing `/` | `/dashboard` | `/board` | `/submissions?status=` | True |\n|---|---|---|---|---|---|\n| Submitted | 960 | 0 | 0 | 0 | no rows have status='submitted' |\n| Waved | \u2014 | 60 | **620** | 60 | no rows have status='waved' |\n| Onboarding | 153 | 58 | \u2014 | \u2014 | 153 is people, 58 is submissions |\n| Scheduled | 24 | 1 | \u2014 | 1 | 1 |\n| In review | 280 | 280 | 280 | 280 | 280 (agrees) |\n| Published | 23 | 23 | 23 | 23 | 23 (agrees) |\n\nTrue status distribution across all 1,000 seeded submissions: rejected 550, in_review 280, waitlisted 70, draft 40, accepted 36, published 23, scheduled 1. There is no `submitted` status and no `waved` status in the data at all.\n\n## Root cause \u2014 THREE competing derivations, two of them rogue\n\n`src/routes/submissions.queries.ts:79-97` declares the intended contract in its own doc comment: \"One status vocabulary powers list filtering and dashboard instruments. A dashboard tile therefore cannot count a different set than its destination.\" `submissionStatusPredicate` is that vocabulary, and `/dashboard` correctly consumes it (`dashboardStageSql`, dashboard.routes.ts:77-81). Two surfaces bypass it entirely:\n\n1. **The landing page rolls its own SQL.** `src/routes/landing.route.tsx:56-101` is one hand-written D1 read: `submitted_count` is `status <> 'draft'` (cumulative, 960), `scheduled_count` is every agenda_item (24, includes published), `onboarding_count` is `COUNT(DISTINCT person_id)` (people, 153) where the dashboard counts submissions (58). Every one of these is a different question than the sidebar filter answers under the same label.\n\n2. **The board rolls its own CASE.** `BOARD_STAGE_SQL` at `src/api/board.ts:143-157` maps `s.status IN ('waitlisted','rejected','withdrawn') THEN 'waved'` \u2014 550 + 70 = **620**. Its doc comment is explicit that this is deliberate (\"A rejected or waitlisted record remains visible in Waved: the board has no destructive terminal column of its own\"), but it directly contradicts SPEC.md section 1, which specs the board as a read-only Kanban across THE SEVEN LIFECYCLE STAGES \u2014 the same seven, not a private vocabulary. The board needed a bucket for decided-and-not-accepted records and took the Waved label instead of getting a ruling.\n\n3. **`waved` is broken inside the shared vocabulary too.** `submissions.queries.ts:95` \u2014 `wave_id IS NOT NULL AND status = 'accepted'`. Every accepted record in the seed has a wave_id, so `?status=waved` and `?status=accepted` return the IDENTICAL 60 records. The Waved filter cannot show a single actually-waved record. `dashboard.routes.ts:64` labels this stage \"Decision wave prepared\", which is a THIRD meaning again.\n\n## The semantic ruling this ticket must make first\n\nDo not patch counts surface by surface \u2014 that is how this was introduced and it will regress. Rule on the semantics, then make every surface read one derivation.\n\n- **Is a stage cumulative or literal?** Landing reads cumulative (960 = everything past draft), the filter reads literal (0 = status='submitted'). Recommendation: STAGES ARE LITERAL \u2014 a pipeline where a record sits in exactly one stage is the one an organizer can act on, and it is the only reading where a count and the list behind it can agree. If the landing hero wants the cumulative headline, it must be RELABELED (e.g. \"960 submissions received\"), never labeled with a stage name.\n- **What does Waved mean?** Recommendation: records assigned to a decision wave whose decision has not yet been sent \u2014 the SPEC ordering `In Review -> Waved -> Accepted` only makes sense as a staging area BEFORE the decision lands. Under that reading `/board`'s decided-not-accepted bucket needs its own honest column (Declined / Not advancing), which is a SPEC-visible change: state it in the PR.\n- **Onboarding: people or submissions?** Pick one and use it in both places. The `/onboarding` screen's own copy says \"153 accepted speakers still owe something\" \u2014 people. Recommendation: make the noun explicit in the label wherever the count appears.\n\nIf your investigation contradicts a recommendation above, take your reading and say why in the PR. What is NOT acceptable is four surfaces continuing to answer four different questions under one label.\n\n## Also in scope \u2014 same root cause, do not split\n\n`src/routes/submission-record.routes.ts:406` \u2014 `can_decide: [\"submitted\",\"in_review\",\"accepted\",\"waitlisted\"].includes(row.status)`. Scheduled and published records still carry `status='accepted'` in D1 (their stage is DERIVED from agenda_items), so an already-scheduled talk renders live Accept / Maybe / Reject buttons with no indication a decision was made and acted on. Reproduce at `/submissions/sub_what-rl-means-for-agents` \u2014 \"Scheduled\" pill top-right, decision buttons below. This is the SAME defect class: reading the raw `status` column where the derived stage is what the reader means. Fix it with the derivation, not with a special case.\n\n## Scope\n\n- One shared stage derivation. `submissionStatusPredicate` is the natural home; landing and board consume it rather than hand-rolling SQL. Where a surface genuinely needs a different question (a cumulative hero number), it gets a DIFFERENT LABEL, not a different definition of the same word.\n- Fix `waved` so it selects a set that is not identical to `accepted`.\n- Reconcile `/board`'s stage vocabulary with SPEC.md section 1.\n- Gate decision affordances on the derived stage.\n- Keep the landing page's one-D1-read property (landing.route.tsx:52-55 documents it deliberately) \u2014 share the derivation, do not add round trips.\n\n## Constraints\n\n- DESIGN.md / Flight Deck tokens; the build reproduces the binding prototype one-to-one. Prototype-to-product fidelity is a taste rule (SPEC.md preamble), so a label change must be justified against the prototype, not invented.\n- ELEMENTS NEVER JUMP. These counts change under the reader \u2014 tabular numerals, reserved space, constant row counts.\n- Speed is a graded feature (R7). The landing read is on the public path; do not regress it. `npm run check:speed` must stay green.\n- No new D1 table and no migration. This is a derivation defect over columns that already exist.\n- If you add or change an API route, `npx vite build && node cli/generate-api-registry.mjs` \u2014 `check:api` asserts exact registry parity.\n- The fast suite has a 29s hard kill and a 30s budget. Worker-free unit tests in `tests/unit`, Worker-backed in `tests/integration`.\n\n## Verification\n\n1. **NO TEST CURRENTLY PINS ANY OF THIS** \u2014 `grep -rl waved tests/` returns nothing. The regression net is part of the deliverable, not optional. Add a test that asserts, per stage, that the dashboard tile count EQUALS the row count of the list its href opens. That single invariant is what was missing and is what makes this bug unrepeatable.\n2. Unit tests on the derivation itself, fixture-driven, no Worker: each stage's predicate against a known distribution; `waved` and `accepted` must return DIFFERENT sets.\n3. Integration test: landing counts, dashboard counts, board column counts, and filtered list totals agree stage by stage for one seeded event.\n4. `npm run pr-gate -- --ticket <this ticket>` \u2014 all checks green, including `check:seed` and `check:speed`.\n5. REAL-ARTIFACT SMOKE, non-negotiable. `wrangler dev` against the full seed. Walk `/` -> `/dashboard` -> `/board` -> each `/submissions?status=` filter and confirm every stage agrees across all four, that clicking any count opens a list whose \"N matching records\" matches the count clicked, and that NO pipeline stage links to an empty list. Then open `/submissions/sub_what-rl-means-for-agents` and confirm the decision buttons are gone or visibly resolved. Numbers agreeing in a unit test proves nothing here \u2014 the defect is cross-surface and only the walkthrough shows it.\n\n## Delivery\n\nOwn git worktree, branch `mrq-75-pipeline-stage-derivation`. PR via `gh pr create --repo Stage-11-Agentics/marquee --base main`.\n\n## File ownership (MRQ-75 / MRQ-76 / MRQ-77 run in parallel)\n\nMRQ-75 OWNS: `src/routes/submissions.queries.ts`, `src/routes/landing.route.tsx`, `src/api/board.ts`, `src/routes/board.routes.ts`, `src/routes/dashboard.routes.ts`, `src/api/dashboard.ts`, `src/routes/submission-record.routes.ts`, its own tests.\nMRQ-75 MUST NOT TOUCH: `src/ui/evaluation/*`, `src/ui/submissions/record.css`, `src/ui/submissions/SubmissionRecordPage.tsx` (MRQ-76), `src/routes/tokens.routes.ts`, `scripts/seed/evaluations.ts` (MRQ-77), `package.json`.","to":"WALKTHROUGH-BLOCKER. The app shows conflicting pipeline counts for the same event across four surfaces, and a judge notices inside two clicks. Full evidence: sequence/UX-SWEEP-FINDINGS.md rows 1-2 and section 4b (counts re-verified directly against the API, so these are backend defects, not rendering artifacts).\n\n## Why this exists\n\nThe pipeline IS the product. SPEC.md section 1 makes it the organizer's home: seven stages, each a count, each clickable into the work behind it. When the hero says 960 Submitted and the sidebar's Submitted filter opens an EMPTY LIST, the core promise of the screen is broken in the most visible way available. Right now four of the seven stages disagree, one by more than 10x.\n\n## The observed disagreement\n\n| Stage | Landing `/` | `/dashboard` | `/board` | `/submissions?status=` | True |\n|---|---|---|---|---|---|\n| Submitted | 960 | 0 | 0 | 0 | no rows have status='submitted' |\n| Waved | \u2014 | 60 | **620** | 60 | no rows have status='waved' |\n| Onboarding | 153 | 58 | \u2014 | \u2014 | 153 is people, 58 is submissions |\n| Scheduled | 24 | 1 | \u2014 | 1 | 1 |\n| In review | 280 | 280 | 280 | 280 | 280 (agrees) |\n| Published | 23 | 23 | 23 | 23 | 23 (agrees) |\n\nTrue status distribution across all 1,000 seeded submissions: rejected 550, in_review 280, waitlisted 70, draft 40, accepted 36, published 23, scheduled 1. There is no `submitted` status and no `waved` status in the data at all.\n\n## Root cause \u2014 THREE competing derivations, two of them rogue\n\n`src/routes/submissions.queries.ts:79-97` declares the intended contract in its own doc comment: \"One status vocabulary powers list filtering and dashboard instruments. A dashboard tile therefore cannot count a different set than its destination.\" `submissionStatusPredicate` is that vocabulary, and `/dashboard` correctly consumes it (`dashboardStageSql`, dashboard.routes.ts:77-81). Two surfaces bypass it entirely:\n\n1. **The landing page rolls its own SQL.** `src/routes/landing.route.tsx:56-101` is one hand-written D1 read: `submitted_count` is `status <> 'draft'` (cumulative, 960), `scheduled_count` is every agenda_item (24, includes published), `onboarding_count` is `COUNT(DISTINCT person_id)` (people, 153) where the dashboard counts submissions (58). Every one of these is a different question than the sidebar filter answers under the same label.\n\n2. **The board rolls its own CASE.** `BOARD_STAGE_SQL` at `src/api/board.ts:143-157` maps `s.status IN ('waitlisted','rejected','withdrawn') THEN 'waved'` \u2014 550 + 70 = **620**. Its doc comment is explicit that this is deliberate (\"A rejected or waitlisted record remains visible in Waved: the board has no destructive terminal column of its own\"), but it directly contradicts SPEC.md section 1, which specs the board as a read-only Kanban across THE SEVEN LIFECYCLE STAGES \u2014 the same seven, not a private vocabulary. The board needed a bucket for decided-and-not-accepted records and took the Waved label instead of getting a ruling.\n\n3. **`waved` is broken inside the shared vocabulary too.** `submissions.queries.ts:95` \u2014 `wave_id IS NOT NULL AND status = 'accepted'`. Every accepted record in the seed has a wave_id, so `?status=waved` and `?status=accepted` return the IDENTICAL 60 records. The Waved filter cannot show a single actually-waved record. `dashboard.routes.ts:64` labels this stage \"Decision wave prepared\", which is a THIRD meaning again.\n\n## The semantic ruling this ticket must make first\n\nDo not patch counts surface by surface \u2014 that is how this was introduced and it will regress. Rule on the semantics, then make every surface read one derivation.\n\n- **Is a stage cumulative or literal?** Landing reads cumulative (960 = everything past draft), the filter reads literal (0 = status='submitted'). Recommendation: STAGES ARE LITERAL \u2014 a pipeline where a record sits in exactly one stage is the one an organizer can act on, and it is the only reading where a count and the list behind it can agree. If the landing hero wants the cumulative headline, it must be RELABELED (e.g. \"960 submissions received\"), never labeled with a stage name.\n- **What does Waved mean?** Recommendation: records assigned to a decision wave whose decision has not yet been sent \u2014 the SPEC ordering `In Review -> Waved -> Accepted` only makes sense as a staging area BEFORE the decision lands. Under that reading `/board`'s decided-not-accepted bucket needs its own honest column (Declined / Not advancing), which is a SPEC-visible change: state it in the PR.\n- **Onboarding: people or submissions?** Pick one and use it in both places. The `/onboarding` screen's own copy says \"153 accepted speakers still owe something\" \u2014 people. Recommendation: make the noun explicit in the label wherever the count appears.\n\nIf your investigation contradicts a recommendation above, take your reading and say why in the PR. What is NOT acceptable is four surfaces continuing to answer four different questions under one label.\n\n## Also in scope \u2014 same root cause, do not split\n\n`src/routes/submission-record.routes.ts:406` \u2014 `can_decide: [\"submitted\",\"in_review\",\"accepted\",\"waitlisted\"].includes(row.status)`. Scheduled and published records still carry `status='accepted'` in D1 (their stage is DERIVED from agenda_items), so an already-scheduled talk renders live Accept / Maybe / Reject buttons with no indication a decision was made and acted on. Reproduce at `/submissions/sub_what-rl-means-for-agents` \u2014 \"Scheduled\" pill top-right, decision buttons below. This is the SAME defect class: reading the raw `status` column where the derived stage is what the reader means. Fix it with the derivation, not with a special case.\n\n## Scope\n\n- One shared stage derivation. `submissionStatusPredicate` is the natural home; landing and board consume it rather than hand-rolling SQL. Where a surface genuinely needs a different question (a cumulative hero number), it gets a DIFFERENT LABEL, not a different definition of the same word.\n- Fix `waved` so it selects a set that is not identical to `accepted`.\n- Reconcile `/board`'s stage vocabulary with SPEC.md section 1.\n- Gate decision affordances on the derived stage.\n- Keep the landing page's one-D1-read property (landing.route.tsx:52-55 documents it deliberately) \u2014 share the derivation, do not add round trips.\n\n## Constraints\n\n- DESIGN.md / Flight Deck tokens; the build reproduces the binding prototype one-to-one. Prototype-to-product fidelity is a taste rule (SPEC.md preamble), so a label change must be justified against the prototype, not invented.\n- ELEMENTS NEVER JUMP. These counts change under the reader \u2014 tabular numerals, reserved space, constant row counts.\n- Speed is a graded feature (R7). The landing read is on the public path; do not regress it. `npm run check:speed` must stay green.\n- No new D1 table and no migration. This is a derivation defect over columns that already exist.\n- If you add or change an API route, `npx vite build && node cli/generate-api-registry.mjs` \u2014 `check:api` asserts exact registry parity.\n- The fast suite has a 29s hard kill and a 30s budget. Worker-free unit tests in `tests/unit`, Worker-backed in `tests/integration`.\n\n## Verification\n\n1. **NO TEST CURRENTLY PINS ANY OF THIS** \u2014 `grep -rl waved tests/` returns nothing. The regression net is part of the deliverable, not optional. Add a test that asserts, per stage, that the dashboard tile count EQUALS the row count of the list its href opens. That single invariant is what was missing and is what makes this bug unrepeatable.\n2. Unit tests on the derivation itself, fixture-driven, no Worker: each stage's predicate against a known distribution; `waved` and `accepted` must return DIFFERENT sets.\n3. Integration test: landing counts, dashboard counts, board column counts, and filtered list totals agree stage by stage for one seeded event.\n4. `npm run pr-gate -- --ticket <this ticket>` \u2014 all checks green, including `check:seed` and `check:speed`.\n5. REAL-ARTIFACT SMOKE, non-negotiable. `wrangler dev` against the full seed. Walk `/` -> `/dashboard` -> `/board` -> each `/submissions?status=` filter and confirm every stage agrees across all four, that clicking any count opens a list whose \"N matching records\" matches the count clicked, and that NO pipeline stage links to an empty list. Then open `/submissions/sub_what-rl-means-for-agents` and confirm the decision buttons are gone or visibly resolved. Numbers agreeing in a unit test proves nothing here \u2014 the defect is cross-surface and only the walkthrough shows it.\n\n## Delivery\n\nOwn git worktree, branch `mrq-76-pipeline-stage-derivation`. PR via `gh pr create --repo Stage-11-Agentics/marquee --base main`.\n\n## File ownership (MRQ-76 / MRQ-77 / MRQ-78 run in parallel)\n\nMRQ-76 OWNS: `src/routes/submissions.queries.ts`, `src/routes/landing.route.tsx`, `src/api/board.ts`, `src/routes/board.routes.ts`, `src/routes/dashboard.routes.ts`, `src/api/dashboard.ts`, `src/routes/submission-record.routes.ts`, its own tests.\nMRQ-76 MUST NOT TOUCH: `src/ui/evaluation/*`, `src/ui/submissions/record.css`, `src/ui/submissions/SubmissionRecordPage.tsx` (MRQ-77), `src/routes/tokens.routes.ts`, `scripts/seed/evaluations.ts` (MRQ-78), `package.json`."},"id":"ev_01KZS2BFNQTTBWJZQ7G59A08XR","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T18:46:27Z","type":"field_updated"}
{"actor":"agent:delegator-mrq-76","data":{"surface":"surface:250","workspace":"FF5A5A52-3AD9-4735-AD2E-84E3C0270209"},"id":"ev_01KZS38WXZ1WJF40G6GG5CQMSX","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:02:31Z","type":"surface_bound"}
{"actor":"agent:delegator-mrq-76","data":{"from":null,"to":"agent:delegator-mrq-76"},"id":"ev_01KZS3AD48V7Y10AT6Q32PR3CY","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:03:20Z","type":"assignment_changed"}
{"actor":"agent:delegator-mrq-76","data":{"from":"backlog","to":"in_planning"},"id":"ev_01KZS3AD4CM062QTQ09Q5ZAWMB","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:03:20Z","type":"status_changed"}
{"actor":"agent:delegator-mrq-76","data":{"from":"in_planning","to":"planned"},"id":"ev_01KZS3VNW1VZNM6KYX8Y3QWXXB","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:12:46Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-plan-review-task_01KZS27XASDA1FBT30ZNVY3MMT.log","mode":"single","pid":77869,"review_type":"plan-review","spawned_at":"2026-08-11T19:12:47Z","trigger_status_event_id":"ev_01KZS3VNW1VZNM6KYX8Y3QWXXB"},"id":"ev_01KZS3VPBSFZY7E02MWQ957KVY","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:12:47Z","type":"auto_review_spawned"}
{"actor":"agent:delegator-mrq-76","data":{"from":"planned","to":"in_progress"},"id":"ev_01KZS3WT8D9TXGQQBWCY9XBQFZ","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:13:24Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"artifact_id":"art_01KZS44Q4WA95VNR54FFKETBBN","role":"plan-review"},"id":"ev_01KZS44Q5CEN9TJZHBWM3V88P8","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:17:43Z","type":"artifact_attached"}
{"actor":"agent:delegator-mrq-76","data":{"from":"in_progress","to":"review"},"id":"ev_01KZS5FJJ0YVSC9FXWMSEY9H8F","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:41:07Z","type":"status_changed"}
{"actor":"agent:lattice-auto-review","data":{"log_path":"/Users/atin/Projects/Stage11/deployments/Marquee/.lattice/.daemon/auto-code-review-task_01KZS27XASDA1FBT30ZNVY3MMT.log","mode":"single","pid":57515,"review_type":"code-review","spawned_at":"2026-08-11T19:41:08Z","trigger_status_event_id":"ev_01KZS5FJJ0YVSC9FXWMSEY9H8F"},"id":"ev_01KZS5FKS2J7YGR72G5B2STW1X","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:41:08Z","type":"auto_review_spawned"}
{"actor":"agent:delegator-mrq-76","data":{"artifact_id":"art_01KZS66T28X79XEXWD0239H12F","role":"validation"},"id":"ev_01KZS66T290KDG8NFZ8B0ZQAZS","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:53:48Z","type":"artifact_attached"}
{"actor":"agent:delegator-mrq-76","data":{"artifact_id":"art_01KZS67CZ6DTERMZBK1VT2YFZA","role":"validation"},"id":"ev_01KZS67CZEPJCQVFCZ7R87TMW9","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:54:08Z","type":"artifact_attached"}
{"actor":"agent:delegator-mrq-76","data":{"artifact_id":"art_01KZS67SKERMSS8VJTTKMK2YEK","role":"review"},"id":"ev_01KZS67SKGWJM57A6V2GJDWF39","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:54:21Z","type":"artifact_attached"}
{"actor":"agent:delegator-mrq-76","data":{"body":"PR #17 opened: https://github.com/Stage-11-Agentics/marquee/pull/17. Exact HEAD 76fff163cbeb97927fafd18958e6e1cd36b99b55 rebased onto github/main 9fa278ddd61f9fc4433217a59016df91b5aed0bf. Validation: npm ci passed; tsconfig, tsconfig.client, and tsconfig.test noEmit passes; vite build, check:design, check:api (129 operations, no findings), and trace:ac (212 live criteria, 102 test files, 0 errors) passed; focused unit 1 test and Worker 6 files / 31 tests passed. Full npm test and pr-gate intentionally skipped under the merge-driver directive; GitHub CI is the runner-backed full validation. Protected MRQ-74 UI label files remain untouched."},"id":"ev_01KZS684GQXW218DPSG789MVVZ","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:54:32Z","type":"comment_added"}
{"actor":"agent:delegator-mrq-76","data":{"from":"review","to":"pr_open"},"id":"ev_01KZS689GR5MPGCFXY3Q2CDDSG","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T19:54:37Z","type":"status_changed"}
{"actor":"agent:delegator-mrq-76","data":{"body":"PR #17 follow-up fix pushed at HEAD 2e9018e, rebased onto github/main fc44cd8. CI regression root cause was communications audience status=accepted reusing the new derived pipeline stage: sub_mail_2 has an open onboarding task, so it was excluded despite stored status accepted. Added explicit stored-status semantics only for MRQ-8 audience selection; pipeline list and bulk selectors remain derived. Targeted mail audience test passed, MRQ-76 unit and cross-surface tests passed, and all three tsc passes passed. PR description updated; GitHub CI rerun is in progress."},"id":"ev_01KZS6RCFAES6FHD4T5PG9T3MM","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T20:03:24Z","type":"comment_added"}
{"actor":"agent:delegator-mrq-76","data":{"body":"Second CI fix pushed at HEAD 7f5857d, rebased onto github/main 54d8e43. AC-265 was a test-shape regression only: board SQL delegates onboarding to submissionStatusPredicate with includeCancelledAt=true, and that shared helper emits filtered_task.status = open plus filtered_task.cancelled_at IS NULL. Updated the AC-265 static guard to assert the delegation and retained the helper tombstone assertion; no filter was removed. AC-264 through AC-267 pass locally, and the mail audience regression passes. CI rerun is pending."},"id":"ev_01KZS73CDG9QH1P31SPQ3FT66Z","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T20:09:25Z","type":"comment_added"}
{"actor":"agent:claude","data":{"body":"Pass B (write-flow sweep) independently reproduced this stage-pill defect TWICE more, on two different terminal states \u2014 adding repro cases to the set already in this ticket. (1) sub_synthetic-pool-0001, accepted then acceptance-reversed to WITHDRAWN: status badge and /submissions STATUS column both correctly read 'Withdrawn', but the record page's own top-right stage pill still reads 'Waved'. (2) sub_synthetic-pool-0280, plainly REJECTED with feedback (no reversal involved): status badge correctly 'Rejected', same top-right pill reads 'Waved'. Two terminal states, same wrong pill, so this is not reversal-specific \u2014 consistent with BOARD_STAGE_SQL (src/api/board.ts:155) mapping waitlisted|rejected|withdrawn -> 'waved', now visibly leaking onto the record page. Full write-up: sequence/UX-SWEEP-FINDINGS-PASSB.md Flow 2 + Flow 3; screenshots B-organizer-submission-reversed-DEADEND.png and B-organizer-submission-rejected-WAVED-STAGE-BUG.png. Worth adding the record-page pill to this ticket's smoke walk \u2014 it is a fifth surface reading the same derivation."},"id":"ev_01KZS7AXBF3N32E1CD0ETQJFDY","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T20:13:31Z","type":"comment_added"}
{"actor":"agent:claude","data":{"body":"Follow-up from the merge of #17 (551407c): the stage derivation this ticket shipped is correct, but the can_decide list at submission-record.routes.ts:414 was carried over from the old status vocabulary and never gained a 'declined' arm. Waitlisted/rejected/withdrawn all fall to ELSE 'declined' in BOARD_STAGE_SQL, so they lost their Accept/Maybe/Reject card. Tracked and fully diagnosed in MRQ-83 (critical) \u2014 no action needed here, this ticket stays closed."},"id":"ev_01KZS7FH85YR604ZM8VGDG5TV7","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T20:16:03Z","type":"comment_added"}
{"actor":"agent:merge-driver","data":{"from":"pr_open","to":"done"},"id":"ev_01KZS9472QVASEZVXCERMSWHGP","schema_version":1,"task_id":"task_01KZS27XASDA1FBT30ZNVY3MMT","ts":"2026-08-11T20:44:49Z","type":"status_changed"}
