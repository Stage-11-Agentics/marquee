# MRQ-248: The mirror builds its own tables: empty-base provisioning and template adoption

WHY. An organizer pointing Marquee at a fresh Airtable base hits a dead end: connect refuses with "Airtable returned no tables for this base." (src/jobs/mirror/actions.ts:134-136) — they must hand-build three correctly-shaped tables first, with no stated shape. The sibling failure: a base made from an Airtable template, mapped via the three dropdowns, passes mapping cleanly and then fails hours later as Stuck: N with an opaque provider error in mirror_state.last_error (consumer.ts:97-111) — a clear form-time error deferred into an unreadable drain-time one. Making the base shaped right is chase work the system should do. PHILOSOPHY 2/5 (the mirror is a genuine surface; their base stays theirs).

SCOPE. All on the existing Settings → Airtable door (src/ui/settings/AirtablePage.tsx) — no new screen. (a) EMPTY BASE: the refusal becomes the offer — a primary "Create the three tables for me" provisions Submissions, Speaker tasks, People with the declared columns, re-reads the schema, lands on mapping with all three pre-selected, and reports per table ("Created Submissions · 28 columns"). The empty base becomes the easiest path. (b) TEMPLATE/PARTIAL BASE: mapping keeps the dropdowns (the name heuristic at AirtablePage.tsx:69-79 becomes a pre-selection fallback); on confirm an adopt pass adds ONLY missing columns and reports honestly ("Adopted Speakers · added marquee_id, title · kept your 4 extra columns"). Organizer-added columns are never touched. (c) WRONG-SHAPED TABLE: the failure moves from runtime to the form — a column whose existing type conflicts with what the mirror writes is named at mapping time, field-level, in plain language; no webhook registers and the mirror stays off. CRITICAL: because writes set typecast: false (transport.ts:202), the diff MUST compare types as well as names — a name-only diff adopts a base that then hard-fails every PATCH. (d) READ-ONLY TOKEN (no schema.bases:write): connect still succeeds against an already-correct base; against one needing changes it refuses with a sentence naming the scope — raw provider text never reaches the organizer. (e) AGENT PATH: cli/marquee.mjs mirror verbs (:402-413) gain --provision + the same adopt report, or the agent-native path is strictly worse than the UI (PHILOSOPHY 3).

DATA MODEL. No migration — mirror_state.airtable_table_id already persists per-table ids (keep id-addressing; it survives renames). Code: new src/jobs/mirror/schema.ts holding MIRROR_TABLE_SCHEMA (name AND Airtable type per column, derived from the three field maps in records.ts:68-158 — the declaration that has no home today) + ensureMirrorSchema(transport, tables) → {createdTables, adoptedColumns, conflicts}. AirtableTransport (transport.ts:31-56) grows createTable + createField — implemented in the fetch transport, in FakeAirtableTransport (mutating its in-memory tables so tests assert WHICH columns were added), AND in rateLimitedAirtableTransport (transport.ts:78-112 — a hand-written passthrough where a forgotten method silently escapes the base-wide 4 req/s budget with capacity 1; the easiest mistake in this work, and it gets its own AC). connectMirror/mapMirror grow the provision branch and the pre-webhook column diff; response schemas widen (watch check:api parity). docs/GETTING-STARTED.md:174-200 must start naming token scopes at all (today it names none); schema.bases:write documented as required-for-provisioning, optional otherwise.

CONTRACT FOLD (executing agent): SPEC §3.9 amendment + US/AC mint at consolidation; re-check next-mint (AC-314 as of 2026-08-15).

AC DRAFTS. Empty base offers provisioning; accepting creates exactly the three tables per MIRROR_TABLE_SCHEMA and lands on mapping pre-selected, report naming each table. Adopting adds only missing columns (incl. marquee_id), organizer columns untouched — asserted via the fake transport's exact createField calls. Type-conflict refuses at mapping, field named; no webhook, mirror stays off. schema.bases:write absent: connects against a correct base; refuses with the scope named otherwise. Rate wrapper covers both new methods (a provision through the wrapper consumes the serialized budget). AC-310's discipline extends to the new branches: no test inserts mirror_state as a fixture — the connect flow is the only on-switch. CLI provisions an empty base end to end.

VALIDATION. Hermetic against the fake transport (the MRQ-223 discipline — no credential, no network); one live smoke against a real scratch base when available.

COORDINATION. Disjoint from MRQ-239 (inbound hardening: inbound.ts + cascade/decisions.ts) except the AirtableTransport interface and the MRQ-223 test fixture (tests/integration/mirror-connect-inbound.MRQ-223.test.ts:34-38 — this ticket adds empty-base and template-base fixtures beside it). Whichever merges second rebases over a small interface diff; noted in both.

CUT LINE. Smallest: empty-base provisioning at connect + column-presence validation at mapping (deletes both dead ends). Complete: type-aware adopt pass with per-column report, scope degradation, CLI parity, GETTING-STARTED scopes.

## Durable implementation plan (MRQ-248; pre-feature hold)

### Hold, authority, and boundaries

This is the plan-only checkpoint. Do not write feature code, mint a US/AC, amend SPEC or another contract artifact, add a migration, launch a subagent/reviewer, contact Airtable, run a live smoke, open a browser/computer-use flow, deploy, merge, or publish until the Adoption Orchestrator reviews and releases this plan.

D1 remains the source of truth. The existing base-wide Airtable limiter remains the only provider budget; every new provider operation must pass through it. Airtable table IDs remain the durable address stored in mirror_state, so renames do not require a migration. The work stays on the existing Settings → Airtable door and the existing organization-scoped CLI/API surface.

The plan uses only the ticket's draft acceptance language and existing mirror criteria as test labels. It does not mint or consolidate US/AC IDs. The contract fold and shared consolidation order remain owned by the Adoption Orchestrator.

### Current seam and collision baseline

- Worktree: /Users/atin/Projects/Stage11/deployments/Marquee-worktrees/mrq-248-mirror-schema.
- Branch HEAD and github/main are both 52bb485f at planning time; the worktree is clean apart from pre-existing Lattice runtime state, which is not owned by this ticket.
- MRQ-239 is merged on main. Its inbound/cascade changes are disjoint from this work except for the shared AirtableTransport interface and the existing MRQ-223 integration fixture.
- No migration is planned or permitted. Before final review, fetch github/main again, compare the exact interface and migration heads, and recheck that MRQ-239 or the consolidation tickets did not alter the touched seams. If main advanced, rebase the branch, take upstream .lattice state mechanically, run npm ci, and regenerate focused evidence on the post-rebase HEAD.

### Design and implementation sequence

1. Establish one schema authority in src/jobs/mirror/schema.ts. Declare the three provider table names and every field written by records.ts, including marquee_id, with explicit Airtable field names and Airtable types. Keep the declaration deterministic and separate from D1 column inference. Expose ensureMirrorSchema(transport, tables) with a result containing createdTables, adoptedColumns, and field-level conflicts. It must first inspect all selected tables, compare both field names and types, and refuse type conflicts before any webhook registration. A correct existing field is a no-op; a missing field is adoptable; an existing same-name wrong-type field is a named conflict.

2. Extend AirtableTransport with createTable and createField. Implement the metadata requests in createFetchAirtableTransport, record exact create_table/create_field mutations in FakeAirtableTransport, and add both methods to rateLimitedAirtableTransport. The fake's tables must be mutated with the returned/declared fields so assertions can inspect exact table and field calls, including organizer columns that must survive byte-for-byte. Cover the Airtable metadata request bodies and paths in the transport unit tests.

3. Make schema mutation explicit at both lifecycle boundaries. Add a provision intent to connect/map requests and CLI flags. On an empty base, an explicit provision request creates exactly Submissions, Speaker tasks, and People from MIRROR_TABLE_SCHEMA, re-reads the schema, and returns the per-table creation report plus the selected tables. On a non-empty base, mapping's explicit adopt/provision action runs the same two-phase schema check and creates only missing fields, including marquee_id. A mapping against an already-correct base performs no schema write and succeeds without schema.bases:write. A mapping that needs mutation without that Airtable scope returns a safe sentence naming schema.bases:write; provider response text never reaches the organizer.

   The UI's first connection attempt remains a verification/read path. When the base is empty it exposes the primary Create the three tables for me action, which retries with the explicit provision intent. Mapping confirmation is the explicit adoption action and reports Created/Adopted per table, added fields, and kept organizer columns. The CLI mirrors this shape with mirror connect --provision for empty-base creation and mirror map --provision for explicit adoption; without the flag, correct bases still work and bases needing changes receive the same safe scope refusal. If the orchestrator review selects a different flag placement, amend this plan before implementation.

4. Keep persistence and the on-switch ordering safe. connectMirror may persist the encrypted credential only after verification/provisioning has completed; it must not create mirror_state merely because a provider was connected. mapMirror must finish schema validation/adoption before createWebhook and must write mirror_state only after webhook registration returns a signature secret. Any type conflict, missing table, or schema permission refusal leaves the mirror off and registers no webhook. Do not insert mirror_state in a fixture to make these tests pass; the connect → map flow is the only on-switch.

5. Widen the route response/request schemas without leaking provider details. Add provision to the connect and mapping inputs and return structured, stable creation/adoption reports and the re-read table schema. Represent type conflicts as safe field-level details (table, field, expected type, actual type, and organizer-facing recovery) so the UI can name the exact field. Preserve the existing auth scopes and D1 ownership. Update the generated/public API parity through the canonical mirror.routes.ts module and its route tests.

6. Bring Settings → Airtable to parity without adding a screen. Add the empty-base offer, preselection after provisioning, per-table Created/Adopted reporting, and the partial/template adoption state. Render type conflicts at mapping time with the table and field named, preserve the selected mapping on recoverable errors, and keep the mirror health/on state absent until the route succeeds. Read-only tokens must see a correct-base success and a scope-named refusal only when a schema write is required.

7. Bring the agent path and docs to parity. Extend cli/registry.mjs, cli/marquee.mjs, the flag parser, tests/node/airtable-agent.AC-312.test.mjs, SKILL.md, and docs/GETTING-STARTED.md. Document the Marquee mirror:write/program:read credentials as applicable, the Airtable personal access token's schema.bases:write requirement only for provisioning/adoption, and the fact that schema.bases:write is optional for an already-correct base. JSON output must preserve the same per-table report and conflict details as the API; no token or raw provider error may appear in output.

### Test and evidence matrix

- Unit transport tests: exact create-table/create-field HTTP method, path, auth, JSON body, type values, and error normalization. Add wrapper tests proving createTable/createField consume the same serialized base-wide limiter and cannot bypass it.
- Hermetic fake integration tests in a MRQ-248-focused integration file or the existing MRQ-223 fixture: empty-base provision creates exactly the three declared tables and all declared fields; a template/partial base creates only missing fields; organizer-added fields remain untouched; field type mismatches fail during mapping with the field named; no webhook or mirror_state is created on failure; and a correct base succeeds without a schema write.
- Permission and lifecycle tests: a schema.bases:write refusal is safe and names the scope without provider text; provision/adoption does not store a raw provider message; successful connect followed by failed map leaves the feed off; and no test inserts mirror_state as a shortcut.
- Route/UI/CLI parity tests: response schemas accept the reports and conflict details, check:api remains aligned, the Settings UI renders the offer/reports/conflicts, and the CLI sends the provision intent and returns the same report. Preserve existing AC-308–AC-312/AC-228 mirror regression controls.
- Positive controls: existing record patch/delete behavior, webhook registration after a correct mapping, inbound/outbound mirror behavior, and organizer columns remain unchanged.

### Verification and handoff

After the Adoption Orchestrator releases the plan, move the task to in_progress, implement in small commits, and push every meaningful commit. Run focused mirror/route/UI/CLI tests after each relevant increment. Before any full npm test or npm run pr-gate, request a serialized gate slot from mailbox merge-captain and wait for the slot; a slow pass is not a failure, while fail or timeout is blocking evidence.

At the final review boundary, re-fetch github/main and record the exact base/head, inspect migration/interface collisions (especially MRQ-239 and the occupied consolidation tickets), rebase if required, run npm ci after a rebase, and review only the exact post-rebase HEAD. No live Airtable smoke, browser/computer-use validation, credentials, network writes, deployment, merge, or publication is authorized by this brief. The handoff stops at the orchestrator-owned review/validation boundary, with static and hermetic evidence clearly separated from runtime inference.

## Plan-Review Cycle 1 Resolutions (AUTHORITATIVE)

Reference: FAIL artifact art_01M04YKCYKP716NXD1EQT5X7Q4. These resolutions supersede any earlier wording in this plan that is less specific or conflicts with them. This is still a plan-only amendment: remain planned, do not write feature code, change status, launch a reviewer/subagent, or run the gate before the Adoption Orchestrator releases Cycle 2.

1. Provider schema is a write-contract, not a name/type transcription. MIRROR_TABLE_SCHEMA must describe each field with name, type, options when the Airtable metadata API requires them, acceptedTypes for write-compatible existing fields, a representative outbound JavaScript value, and legal primary-field ordering. Every table creation payload puts marquee_id first as the legal primary field. The declaration must cover every value currentAirtableRecord can emit, not merely the D1 column name.

   Bind the outbound shapes in records.ts to the provider schema: response_json, social_links, and custom_fields become stable JSON.stringify strings stored in multilineText; attachments retain the current object-array payload and are declared multipleAttachments; epoch-millisecond created_at, updated_at, due_at, and decided_at values are converted in records.ts to ISO-8601 strings and declared dateTime with explicit legal dateFormat/timeZone options; booleans use checkbox; evolving status and kind vocabularies use singleLineText rather than brittle singleSelect choices; long prose uses multilineText; and IDs, names, emails, and URLs use appropriate writable scalar types. D1 remains unchanged. The inbound allowlists do not ingest the JSON-backed or timestamp fields.

   Add a hermetic conformance table test that feeds representative values from currentAirtableRecord through a type-to-accepted-JS-shape validator maintained beside the declaration. The fake provider's acceptance alone is not proof of provider legality. A live scratch-base smoke is an ACCEPTED DEFERRED RISK because credentials and network writes are not approved: record it as an operator-validation candidate, not a merge prerequisite.

2. The connect/provision seam must use the real limiter. connectMirror constructs MirrorTokenBucket at the actual connect/provision boundary and passes it through providerFor, with injectable MirrorClockOptions for deterministic tests. The transport wrapper and fetch beforeRequest path must cover every connect-time schema read, createTable, and re-read. The implementation and plan use the actual rate of 4 req/s, capacity 1, with 250 ms minimum spacing; the earlier stale limiter wording is superseded. Fake call timestamps must prove serialized spacing across the full connect/provision sequence, not just wrapper method presence.

3. Unknown field metadata refuses safely. AirtableTable.fields === undefined means the schema is unknown, not an empty field list and not a permission to skip validation. ensureMirrorSchema refuses with the base and selected table named, and never silently passes to webhook registration. Upgrade the shared MRQ-223 TABLES fixture in tests/integration/mirror-connect-inbound.MRQ-223.test.ts to full field metadata for all three tables; this expected fixture diff is part of MRQ-248 and must retain the existing MRQ-223 coverage.

4. Compare write compatibility, not strict type equality. Each field declaration carries acceptedTypes derived from its representative outbound value and safe Airtable writable alternates. Existing multilineText or richText must pass where the mirror writes legal strings, including the abstract multilineText-compatible case; compatible email, URL, phone, and scalar text types may pass when their payload shape is legal. Computed/read-only types and shape-incompatible types conflict. Add explicit proofs for the abstract multilineText pass and for checkbox-versus-text and computed-field conflicts, with the conflict table/field named and no webhook or mirror_state on failure.

5. Provisioning returns authoritative IDs and deterministic preselection. The provision result carries created/adopted table IDs keyed by mirrored table name. The UI sets mapping from those exact keyed IDs and asserts all three IDs are distinct; it does not re-run a heuristic over the response to select them. The template/partial fallback heuristic becomes order-independent: normalized exact-name priority wins before broader matching, and the People matcher explicitly excludes Speaker tasks so Speaker tasks can never be selected as People. Add tests with reordered tables and similarly named candidates.

6. Provisioning is idempotent across partial failure. Before createTable, match an existing table by normalized exact name and adopt it rather than attempting a duplicate. Create only missing tables; never delete organizer tables, roll back a created table, or remove organizer columns. createTable sends all declared fields in one provider call. Inject a failure on the second table call, retry the same client request, and prove the retry reports the first table as Adopted, creates only missing tables, and eventually returns a mixed Created/Adopted per-table report with distinct IDs. The same no-delete rule applies to field adoption.

7. Bound existing-table adoption with client-driven continuation. Existing-table createField work is partitioned by mirrored table across at most three client-driven map/adopt requests. Each request re-reads the provider schema, handles exactly one table in stable submissions → speaker_tasks → people order, and returns progress containing the completed table keys, current/next table, remaining tables, and per-table Created/Adopted field report. The next request carries the returned continuation table; no hidden state or single request may issue a 70-call all-table adoption. A retry re-reads the table and remains idempotent after any partial field creation.

   The webhook and mirror_state on-switch is legal only after all three tables are conformant. The UI loops the same continuation, shows honest per-table progress while pending, and preserves mapping/conflict details. The CLI loop uses the same route response and continuation until mapped, returning the same report in JSON. Correct bases may advance without schema writes; bases needing mutation without explicit provision/adopt intent receive the scope refusal before any webhook.

8. Gate the new integration file explicitly. Add tests/integration/mirror-schema.MRQ-248.test.ts as the dedicated MRQ-248 hermetic integration file and add that exact path to testFiles in scripts/checks/check-mirror.mjs. Retain the upgraded MRQ-223 fixture in the mirror check rather than replacing it. The focused test file must cover schema conformance, limiter timestamps, unknown fields, compatibility, exact IDs, partial retry, continuation/on-switch ordering, organizer-column preservation, and safe permission errors.

9. Attribute schema.bases:write by operation, never by raw status alone. A 403 raised by createTable or createField is mapped to a safe sentence naming schema.bases:write and the affected provision/adopt operation, table, and field when applicable. A 401/403 from a schema read retains the existing token/base-access copy. ensureMirrorSchema and its provider error boundary thread operation, table, and field metadata into the route-safe result; raw Airtable provider text never reaches the API, UI, CLI, persisted error, or report.

10. Keep CLI, generated docs, and route/UI boundaries explicit. Edit cli/registry.mjs and run node cli/generate-skill.mjs to regenerate SKILL.md; do not hand-edit generated SKILL.md. The CLI exposes the same explicit provision/adopt intent and loops the same per-table continuation, returning route conflict/progress details. Route responses carry stable conflict and progress structures; AirtablePage.tsx renders them without importing src/jobs/mirror/schema.ts or any job module.

   Claim-manifest work stays deferred to consolidation. Do not add tests/ac-claims/MRQ-248.json in this ticket; the expected gate output is the named missing-current-ticket-manifest warning from trace:ac, not an unexplained failure.

### Cycle 1 evidence additions and hold

The focused evidence must include: full provider-legal schema declarations and representative values; JSON/timestamp conversion and inbound allowlist non-ingestion; exact fake createTable/createField calls and organizer-column preservation; 4 req/s capacity-1 timestamp serialization through connect; undefined-field refusal; accepted-type compatibility and true conflicts; distinct keyed provision IDs and name fallback safety; partial-failure retry convergence; at-most-three-table continuation with no early webhook/state; safe operation-aware scope sentences; check-mirror explicit test registration; generated SKILL parity; route conflict/progress parity; and no browser/live/credential proof under this hold.

Cycle 1 is complete only when this authoritative plan and its branch copy are byte-identical, plan-only, pushed, and locally/remote clean. Leave MRQ-248 planned and hold for Cycle 2 plan review.
## Plan-Review Cycle 2 Resolutions (AUTHORITATIVE)

Reference: FAIL artifact art_01M04Z9M1GJ7A84TKTECBZSDPK. These resolutions amend the earlier plan without changing the Lattice lifecycle state: MRQ-248 remains planned, this cycle is plan-only, and no feature code, contract fold, reviewer launch, gate, browser/live/credential validation, PR, merge, or deploy is authorized. Cycle 2 supersedes any earlier provisioning predicate based only on a zero-table response and any assumption that one fixed outbound encoder may be silently applied to an existing connected mapping; Cycle 1 remains binding everywhere it is not expressly superseded.

1. Make provisioning a mirror-readiness decision, not a table-count decision. The server computes the set of distinct mirrored roles that can be filled by conformant tables: submissions, speaker_tasks, and people each require one distinct provider table whose schema passes the complete declared field contract. It sets needs_provisioning to true whenever that set contains fewer than three roles, including an empty provider response, a realistic fresh Airtable base containing its undeletable/default Table 1, and a partial template containing only one or two conformant mirrored roles. The provision/adopt action is a full three-role reconciliation: adopt conformant matches and create only the missing canonical roles, so the operator is offered Create the three tables for every incomplete readiness state while existing provider tables and organizer data remain untouched. Table 1 is never renamed, repurposed, deleted, or used as a reason to skip provisioning.

   The route returns a stable machine-readable readiness object, independent of human message text. Its contract includes needs_provisioning, provisionable, missing_roles, and a per-role readiness/progress record with the normalized provider table name, table ID when known, conformance state, and Created or Adopted result when the action runs. needs_provisioning is the readiness predicate; provisionable reports whether the requested create/adopt operation is currently executable under the provider scope and conflict state. UI and CLI branch only on those fields and structured conflict/progress codes: they show the Create action for incomplete readiness, surface a scope refusal or named recovery conflict as structured state, and never match an error string. Add fake-transport proofs for zero tables, the one-Table-1 fresh base, and a partial template, asserting exact create/adopt calls, distinct final IDs, no Table 1 mutation, and no organizer-row or organizer-column loss.

2. Make MIRROR_TABLE_SCHEMA exhaustive by rule and by proof. For each table, the declared key set must be exactly equal to the keys that currentAirtableRecord can emit, including marquee_id from the shared fieldRecord wrapper, with no omitted or speculative fields. The conformance fixture and test must assert these exact sets:

   - submissions: marquee_id, event_id, form_id, kind, bypass_evaluation, title, abstract, status, format_id, primary_track_id, tracks, origin, vendor_affiliation, wave_id, submitter_person_id, decided_at, decided_by_person_id, submitted_at, last_saved_at, is_published, external_ref, applied_rule_id, last_write_source, created_at, updated_at, attachments.
   - speaker_tasks: marquee_id, event_id, person_id, submission_id, template_id, title, kind, description, due_at, status, completed_at, completed_by_person_id, response_json, cancelled_at, sponsorship_id, last_write_source, created_at, updated_at, attachments.
   - people: marquee_id, org_id, email, name, title, company, company_id, bio, social_links, custom_fields, do_not_contact, is_demo, kind, last_write_source, created_at, updated_at, headshot_url.

   The test uses representative currentAirtableRecord values and fails on both a schema-only field and a record-only field; the three role sets are also checked against the explicit MIRRORED_TABLES set. Every emitted D1 epoch field is converted before provider validation to an ISO-8601 string, with dateTime declarations carrying explicit legal date and time options. The complete emitted epoch list is decided_at, submitted_at, last_saved_at, created_at, updated_at, due_at, completed_at, and cancelled_at; null remains a legal nullable value where D1 has no timestamp. D1 0/1 values are converted to real booleans for bypass_evaluation, is_published, is_demo, and do_not_contact. The representative value passed to a checkbox validator is true or false, never 0 or 1, and a numeric representative is a failing conformance proof. response_json, social_links, and custom_fields use deterministic JSON.stringify output with canonical object-key ordering and preserved array order, and remain absent from inbound allowlists. Long prose stays multilineText; evolving status/kind values stay writable singleLineText; attachments stay multipleAttachments with the provider-safe object hedge below. D1 remains authoritative.

3. Use a no-migration, schema-aware outbound encoder that preserves every connected base. At the start of each drain operation and each reconcile operation, call readBaseSchema exactly once through the same shared base-wide MirrorTokenBucket, whose real limit remains 4 req/s with capacity 1. Resolve the three mapped table IDs from the existing mapping/state rows and build a server-owned field-type profile from that one schema response. Do not add an encoder-version column, marker, control table, or migration: the inspected mirror_state seam carries table IDs and sync/webhook state, while mirror_credentials carries the base and credential state, and neither needs to remember a version when the provider schema is the authority.

   Keep currentAirtableRecord as raw D1 truth. Immediately before patchRecords, pass each record through encodeRecordForAirtableSchema using the actual mapped table's field types. Legacy Number timestamps remain numbers; provider dateTime fields receive ISO-8601 strings. Legacy numeric boolean values remain 0/1 only when the actual provider field accepts that shape; checkbox fields receive true/false. JSON-backed values receive stable canonical JSON strings when the actual field is text-compatible. Multiple attachments receive only url and optional filename. MIRROR_TABLE_SCHEMA acceptedTypes must include safe legacy writable shapes as well as the preferred provider-legal shapes used by new provisioning. New provisioning declares the preferred provider-legal shape, but an existing old-shaped connected base continues draining without re-adoption by adapting to its live schema. Re-adoption remains an available explicit operation for moving an old base to preferred shapes; it is not required for continuity and must not be used as a hidden switch.

   Before any PATCH, validate the complete operation against the server-owned profile. Computed/read-only fields, unknown fields, and shape-incompatible fields return a named recovery conflict and cause no PATCH. A readBaseSchema failure leaves claimed outbox work retryable and undrained, with no false success or silent discard. Pin tests proving: an existing old-shaped connected base drains without re-adoption; a newly provisioned base receives preferred conversions; exactly one schema read is shared by each drain/reconcile operation and that read is rate-limited; a field-type drift is detected by the next operation and adapts when compatible or returns named recovery when not; and no webhook, mirror_state on-switch, encoder-version marker, or migration is introduced by this compatibility path.

4. Make the attachment hedge exact. The new provider encoder emits each multiple attachment as an object containing url and, only when present, filename; it strips content_type and every other local attachment key. The original unknown-provider-key risk is resolved by this deliberate write-shape hedge and is recorded as resolved in the focused evidence. Live scratch-base validation remains an accepted deferred operator-validation candidate, not a merge prerequisite and not part of this hold.

5. Record the new UI as an acknowledged later divergence. The constant progress/report surface belongs in the later SPEC 3.9 consolidation amendment with the marker [beyond the prototype — acknowledged divergence]; do not fold that contract or alter the binding prototype in this ticket cycle. The implementation plan reserves exactly three progress/report rows from idle through completion. Rows stay mounted and in the same order; an empty value renders an em dash placeholder rather than adding or removing a row. Copy uses organizer language and Flight Deck design tokens. Structural render tests prove fixed row count, stable keys/order, and no conditional row insertion/removal or layout-changing branch; they make no browser or computer-use claim.

6. Treat client continuation claims as sequencing hints only. On the final continuation request, the server must re-read provider schema and freshly validate all three mapped tables, including the previously completed tables, against the current MIRROR_TABLE_SCHEMA before registering a webhook or writing mirror_state on-switch state. Client completed/progress keys may select the next request but cannot authorize completion. A hermetic test mutates a previously conformant table after the client claims it complete, then submits the final continuation: the server returns a named table/field conflict and performs neither webhook registration nor mirror_state on-switch. The same ordering applies after provision/adopt retries.

7. Keep retry and throttling truthful. Provider 429 responses map to a safe retryable structured code/copy, never raw provider text, and carry current per-table progress plus a continuation hint so the UI and CLI can resume. A fake transport fails on the mid-table fifth provider createField call; the client-driven continuation retries after re-reading that table, does not duplicate already-created fields, reports current progress, and converges idempotently. This fifth-call proof is a call-count failure scenario, not a rate claim. The actual base-wide MirrorTokenBucket remains 4 req/s with capacity 1, shared across connect-time read/create/re-read and later traffic, with recorded fake timestamps proving serialization.

8. Name exact-name incompatible adoption recovery. When a normalized exact-name candidate has a conflicting field or otherwise fails the declared provider contract, return a named recovery conflict identifying the mirrored role, provider table ID/name, and field. The allowed recovery is to fix the field in place or rename the provider table; never delete it, roll back organizer data, or silently map it. After the organizer renames the conflicting table, a retry no longer treats it as the canonical normalized exact-name match: it creates the canonical table in one full createTable call with all declared fields, preserves the renamed organizer table and its columns, and returns distinct IDs and an explicit Created/Adopted report. Add the fake retry proof for both the conflict and the post-rename fresh creation.

Cycle 2 evidence and hold. The eventual focused MRQ-248 hermetic tests must combine the Cycle 1 checks with the three readiness fixtures, exhaustive per-role key-set equality, all epoch/boolean/JSON/attachment representatives, old-mapping schema-aware continuity and named recovery behavior, explicit re-adoption to preferred shapes, one shared rate-limited schema read per drain/reconcile operation, fixed three-row structural UI proof, stale-client final re-read conflict, safe 429 progress, fifth-call idempotent continuation, schema-drift detection, and exact-name rename recovery. No migration, encoder-version marker, or webhook/state version marker is declared or added. The plan must not claim live Airtable, browser, credentials, network writes, or full-gate evidence.

Cycle 2 is complete only when this authoritative file and the branch copy are byte-identical, the plan-only commit is pushed, local HEAD equals github/mrq-248-mirror-schema, the worktree is clean, and the authoritative plan remains planned. Hold for Cycle 3 plan review; do not enter feature implementation or any later lifecycle state before that review.
