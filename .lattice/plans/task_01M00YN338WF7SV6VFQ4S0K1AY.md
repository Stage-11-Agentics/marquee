# MRQ-213: Invite machinery completions: QR code and the committee door

Completes the one-invite-machinery ruling (2026-08-14; design contract prototypes/pipeline-v1.1 v1.15 invite modal + evaluation committee card). (1) The invite modal renders the one-time join link as a real, scannable QR (vendor a minimal zero-dependency QR encoder or generate inline SVG server-side — flag before adding any npm dependency) beside the copyable URL and MRQ-207's short join code. (2) The committee door: Evaluation's Program committee card gains + Invite reviewer, which opens the same invite flow preset to role Reviewer scoped to the current conference — same link, same expiry, same pending row. One machinery, two entry points; no duplicated mint path. Depends on MRQ-207's role+scope invite mint landing first.
