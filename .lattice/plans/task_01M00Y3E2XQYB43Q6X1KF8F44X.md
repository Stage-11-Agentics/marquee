# MRQ-204: Delete conference: contract-first cascade, Danger zone, type-name confirm

Contract-first per sequence/sidebar-fold-tickets.md §T2, then build. New organizer-only delete route with the cascade enumerated in SPEC: dies — submissions (both kinds), forms + public links, agenda slots, published pages, portal magic links, queued outbox mail + calendar invites, event embeds; survives — people, notes, tags, org-level attachment subjects (do not deepen the attachments.event_id wart), audit log (deletion audited with actor). Unify with the existing remove-demo verb (SPEC Amendment 19) rather than forking. AC mint: type-the-conference-name confirm gate; dies/stays disclosure in the dialog; post-delete landing (another conference if one exists, else fresh-install landing); irreversibility. Build: Danger zone card at the foot of Conference settings + modal per prototype (wrong name stays locked — tested both ways); CLI verb + skill-file mention for agent-native parity. Design contract: prototypes/pipeline-v1.1 settings view (v1.15).

## Delivery plan

1. **Contract (AC-302–AC-307, US-90–US-91, Amendment 22).** Amend `SPEC.md`, `sequence/USER_STORIES.md`, and `EVALUATION.md` with the organizer-only delete route, the complete dies/survives table, irreversibility and transaction/audit semantics, the unified `remove-demo` relationship, post-delete landing, and the exact prototype copy. Update the shared `Next mint:` line to `AC-308`; an expected rebase conflict with MRQ-207 resolves to the higher value.
2. **Shared deletion seam.** Extract or extend one event-scoped transactional deletion primitive used by both `DELETE /api/v1/events/:id` and `POST /api/v1/admin/remove-demo`. Delete children before the event, remove only queued outbox/calendar work as contracted, preserve organization rows and audit history, leave `attachments.event_id` semantics unchanged, and record the deletion audit row with actor inside the same transaction. Keep demo removal idempotent and scoped to the seeded demo predicate.
3. **API and agent parity.** Add the organizer-only route to the canonical route registry/OpenAPI and authorization path, with a truthful response/error contract. Add a CLI delete-conference verb that calls the same route and document it in the generated `SKILL.md` seam without creating a second operation.
4. **Conference settings UI.** Add the Danger zone card at the foot of Conference settings and reproduce the prototype modal copy and structure. The delete button remains disabled until `value.trim() === event.name`; test mismatch and exact match. On success, navigate to another available conference, or the fresh-install landing when none remains, and state that the operation cannot be undone.
5. **Regression coverage.** Add a transactional integration test that snapshots/counts every event-scoped table before and after, proves all event rows are gone, proves people/notes/tags and other org-scoped rows are byte-identical, and proves the audit row has actor attribution. Add remove-demo parity/idempotence coverage and UI source tests for both confirm arms, landing branches, dies/stays copy, and irreversibility. Add claims for AC-302–AC-307 and keep tests hermetic and fast.
6. **Validation and handoff.** Run focused tests, `trace:ac`, the serialized full gate, local Worker/API proof, and the real running UI flow with insecure local cookies. Commit/push early and at milestones; rebase only when the captain orders after MRQ-203, resolve `Next mint` to `AC-308`, open the GitHub PR without merging or deploying, and report the PR/head and exact check statuses to surface:302.
