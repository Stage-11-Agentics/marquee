Manual code review of exact HEAD 5020ee6b against github/main 1058ed23.\n\nScope and correctness: src/routes/webhooks.routes.ts adds six event-scoped endpoints with canonical WEBHOOK_EVENT_TYPES, HTTPS-only Zod validation, duplicate/allowlist checks, grant policies, and responses that omit secret_hash. Creation hashes a generated whsec_ secret and returns the raw secret only in the 201 response. Test-send verifies the supplied secret before any insert/fetch, signs id.timestamp.body with HMAC-SHA256, records queued then delivered/failed outcome, stores response metadata, and updates last_delivery_at. Delete batches delivery-history and endpoint removal.\n\nUI and truthfulness: route-table and AppShell serve /settings/webhooks; WebhooksPage supports endpoint CRUD, event selection, shown-once secret handling, direct signed test-send, and delivery history. Copy explicitly states that selections are preparatory and that automatic event producers plus the WEBHOOK_QUEUE consumer are out of scope. docs/ROUTES.md was regenerated from the route source, and the stale MRQ-106 negative assertion now requires the generated path.\n\nVerification reviewed: worker/client/test TypeScript, webhook API 3/3, node UI/route tests 4/4, check:api (200 operations), check:routes, check:design, check:shell-truth, git diff --check, and the c11 browser Worker proof all passed. No product findings. Full pr-gate's hermetic suite remains fleet-blocked: unrelated existing 20-second integration timeouts/worker teardown and missing-secret queue warnings under concurrent load; this is documented separately in Lattice.