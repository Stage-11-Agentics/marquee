# Code Review: MRQ-165

## 1. Verdict

**FAIL (implementation-level)** — the bridge fix, undo handling, and tests are correct and well-verified (full suite + pr-gate green, focused tests pass, logic traced through all four fixture personas). But the branch is not ready to open a PR yet: it predates the merge of PR #174, which landed a `LIMITATIONS.md` entry describing this exact bug as an open, deliberately-deferred limitation. The implementation's own plan (`.lattice/plans/task_01KZYBC1P5T5JZ56FKY7CTQSQE.md`, step 6) anticipated this and requires the entry be removed/rewritten during the rebase — that step has not been done. Shipping as-is would merge a fix while the public repo's own docs still claim the bug is unfixed.

## 2. Summary

Reviewed the branch's actual own commits (`d3c97932..3884c18b` in the `mrq-165-import-roster` worktree — 4081522e, e4b20bb7, e83d3ab3, 3884c18b), not the full prompt diff, which is stale and bundles in ~15 already-merged PRs' worth of unrelated history (see note below). The core fix — calling `speakerMembershipStatement` unconditionally for every reconciled speaker row (created, updated, *and* unchanged/skipped) and threading a `membership_created`/`membership_id` marker through the import snapshot for precise undo — is correct and traced cleanly against all four test personas (new person, updated person, pre-existing member, unchanged-data-but-no-membership person). `npm test` (214/214, 41–43s/45s) and `npm run pr-gate` (46s/120s) both pass clean in the worktree as committed. The one real gap is process, not logic: `LIMITATIONS.md`'s SPK-03 entry (merged via #174 after this branch's last main-sync) needs to go before this opens as a PR.

**Diff-base note:** the prompt's diff was computed against a stale/original fork point, not the branch's actual merge-base with current `github/main`. It includes full content from PRs #173, #175, #176, #177, #178, #179, #180 (CLAUDE.md footgun notes, DEPLOY.md freeze protocol, `check-clocks.mjs`→`clock-policy.mjs` refactor, MRQ-160/162 multi-event submitter portal, MRQ-161 docstring fix) — none of which are MRQ-165's own work; all are already merged to `main` independent of this ticket. I did not review those hunks as part of this ticket and none of the findings below concern them. Worth flagging to whoever generates these review diffs so future reviews aren't padded with ~1,700 lines of unrelated, already-shipped code.

## 3. Issues

**[MAJOR] sequence/submission/LIMITATIONS.md — stale SPK-03 entry not removed**
PR #174 ("LIMITATIONS: a speakers-only import creates a person who is not on the roster") merged into `github/main` at 2026-08-13T20:36:45Z, adding a limitation entry (currently at `LIMITATIONS.md:75-90` on `main`) that reads in part: *"A speakers-only CSV import lands people, not roster members... Found by the sbek round-4 evidence (SPK-03)... the fix is one bridge call in the import speaker path plus undo handling, deliberately deferred rather than rushed against a deadline."* This branch is exactly that fix, but its own implementation plan already named this exact contingency: *"If #174 merges before PR creation, remove or rewrite its SPK-03 limitation entry during the rebase."* #174 has merged, the branch has not yet been rebased onto current `main`, and the current worktree diff makes no change to `LIMITATIONS.md`. If this merges without that step, the repo — public, and governed by "every claim in it was verified by the round-4 judge's own evidence" / "write every commit as if it were already public" per project CLAUDE.md — will ship a fixed bug alongside published docs actively asserting it is still broken and "deliberately deferred."
**Fix:** During the rebase onto current `github/main`, remove (or rewrite to reflect the fix, per the plan's own "rewrite" alternative) the SPK-03 entry at `LIMITATIONS.md:75-90`. State the #174 coupling in the PR body as the plan already directs.

**[MINOR] tests/integration/api/sessionize-import.AC-110-113.test.ts — portal-reachability assertion uses a query param the route doesn't read**
The new portal-reachability check (`request(\`/api/v1/me/portal?event_id=${EVENT_ID}\`, {}, ...)`) requests with `event_id` (snake_case). The actual route (`src/routes/portal.routes.ts`) validates only a camelCase `eventId` query param (`z.object({ eventId: z.string().min(1).optional() })`), threaded into `findSpeakerEvent`'s `requestedEventId`. With `event_id`, the param is silently ignored and the handler falls back to its default "first event by `starts_on ASC, id ASC` with a speaker membership" resolution. The assertion still passes here only because Dana has exactly one speaker membership in this fixture — so the test does verify AC4 (import-created speaker reaches the portal), but it does not exercise event-scoped resolution the way it visually appears to.
**Fix:** Use `?eventId=${EVENT_ID}` to match the route's actual param name, or drop the query param entirely since it isn't load-bearing for what this test asserts.

No other issues found in the core fix (`src/lib/sessionize-import.ts`, `src/lib/speaker-membership.ts`) or its test coverage.

## 4. Positive Observations

- **Undo precision is genuinely correct, not just plausible.** The `membership_created`/`membership_id` marker distinguishes "this import created the membership" from "the person already had one," and `removeImportedSpeakerMembership` is invoked *before* the existing person-cleanup/`restoreSnapshot` branch — matching the plan's explicit ordering requirement ("undo already checks memberships before person deletion"). Traced through all four personas (new/created, updated, pre-existing-membership, unchanged-data-but-no-membership) and the behavior is correct in every case, including the non-obvious one: a "skipped" row (no field changes) whose person had no prior membership still gets bridged and still gets un-bridged on undo, via the added `membershipCreatedMarker` bypass of the skip-continue.
- **Broader-than-spec but correctly justified.** The ticket's FIX SHAPE said "for created AND updated speaker rows," but the implementation calls the bridge statement unconditionally for every reconciled row including unchanged/matched ones — necessary because "unchanged person data" and "already a member of this event" are orthogonal facts, and the test suite (`SKIPPED_MEMBERSHIP_PERSON_ID` persona) specifically proves this matters. The `speaker-membership.ts` docstring was updated to name the import as a third bridge caller, keeping the file's own "documented bridge, two/three callers" invariant honest.
- **Idempotency is verified, not assumed.** The test re-runs the same import and asserts `skipped: 4` with an unchanged membership count of 4, actually exercising `ON CONFLICT DO NOTHING` under `uq_memberships_event` rather than trusting it by inspection.
- **Real verification, not just green tests.** Ran `npm test` (214/214 pass, 41–43s/45s budget) and `npm run pr-gate` (46s/120s budget, AC trace clean, 0 uncovered) directly in the worktree as committed — both green, consistent with AC5.
