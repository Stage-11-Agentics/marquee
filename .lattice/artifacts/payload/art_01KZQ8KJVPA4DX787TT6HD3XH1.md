Merged as PR #12 (squash f2efc9ff). **THE AUTH HOLE IS CLOSED.** Hand-reviewed as required: GET /events/:id/submissions now carries auth {kind: grants, grants: [program:read]} — no longer public — and the TODO(MRQ-60) marker is gone (0 occurrences). Its test was flipped, not deleted, and asserts exactly what I required: an unauthenticated caller gets 401/403 AND the body contains neither the submission id nor the title (no-leak, not just a status code); a caller authenticated to a DIFFERENT event gets 403 with the same no-leak assertions — AC-214's cross-event rule finally reaching the API surface. Principal/AuthContext reconciled. pr-gate PASS 23.0s at exact HEAD de5ab25. MRQ-57 (real deploy) can now proceed on this axis.