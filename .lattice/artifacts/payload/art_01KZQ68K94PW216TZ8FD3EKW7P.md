Merged as PR #9 (squash 13c0780a). GUARDRAIL HAND-REVIEWED BY ORCHESTRATOR: AC-231's fail-closed tests assert BOTH a 403 AND zero side effects (attachments count == 0) across four cases — missing Turnstile token, invalid/replayed token, out-of-scope draft/resume-token ownership, and missing auth on the speaker task-upload route. That is fail-closed done properly, not a status-code check. AC-232 covers type/size/magic-byte/rate-limit/origin-isolation. Secret scan: the only credential-shaped strings are local-fake-* placeholders in a .dev.vars example, explicitly annotated that MRQ-57 supplies production values as Wrangler secrets. Deferred and named in the PR body: auth-scoped presigns beyond task_upload self-serve, Images headshot variants, JPEG/WebP dimension checks. Follow-up raised as MRQ-59 (route-manifest naming).