One audit substrate, three lenses — merged as 2f659904 (PR #267). Migration 0019 org-scopes audit_log (event_id nullable, org_id backfilled, CHECK requires at least one scope, MRQ-204's no-FK decision preserved with reasoning in-file). src/lib/org-activity.ts is the recording seam: orgActivityStatement composes into the caller's batch so access changes and their records are atomic; the org lens reads by org_id, never an action vocabulary, so parallel tickets' actions appear without editing the reader. Lenses: /org/activity admin log (organizer authority), person feed merging audit + annotations + mail, submission timeline in the organizer's language. Adversarial pass found four issues post-gate (keyset paging, actor-attribution vs deletion, org-unscoped person feed — a cross-tenant leak, and 'sent' claimed for queued mail); all four fixed at 8956168e per design rulings recorded on this ticket, verified in merged code. Delegator stood down during the merge-captain pause; orchestrator closed lifecycle. Browser evidence for all three lenses recorded earlier at in_validation.