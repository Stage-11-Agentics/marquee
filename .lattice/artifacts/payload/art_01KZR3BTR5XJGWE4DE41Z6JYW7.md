MRQ-49 validation evidence — final HEAD 3116d40, pushed and equal to forgejo/mrq-49-audit-write. Audit base remains cfd7e700fe65a0f153748674d52d6d6bb8dd4973.\n\nOwnership: no auto AC is owned by this ticket; tests/ac-claims/MRQ-49.json was intentionally not created.\n\nFinding 1 — RESOLVED, serious: before the fix, POST /api/v1/public/uploads/sign accepted the same verified token twice. Exact body used twice: {draftId: sub-replay, resumeToken: tok-replay, fieldKey: deck, turnstileToken: one-token-one-presign, filename: deck.pdf, contentType: application/pdf, sizeBytes: 100}. With siteverify stubbed true, the pre-fix runtime probe observed first=200 and second=200 and two distinct pending attachment rows and keys. The contained fix is src/routes/uploads.routes.ts:61-71 and :146-156. Final regression: first=200, its generated URL path matches the attachment r2_key, a positive-control object write at that key lists one object, the second identical request=403, attachment count for evt-replay=1, and R2 list count remains 1. No second row or object exists. The PR must state that MRQ-57 real Cloudflare deployment is gated on this fix.\n\nFinding 2 — KNOWN, UNFIXED, routed to owning ticket: admin create validates only field ownership at src/routes/submission-record.routes.ts:441-450 and writes supplied answers verbatim at :543-546. Reproduction: POST /api/v1/events/{eventId}/submissions as an authorized admin with form fields vendor_content=No and conditional vendor_product hidden unless vendor_content=Yes, body answers [{field_id: field-mrq-49-trigger, value_text: No}, {field_id: field-mrq-49-hidden, value_text: secret product}]. Observed status=201 and persisted rows vendor_content=No plus vendor_product=secret product. Admin draft patch has the same direct-write shape at :622-643. Per operator ruling no product fix was made; the AST guard keeps this known exception visible.\n\nChecked and passed: public draft creation and submit missing/failed Turnstile produce 403 with unchanged row counts; autosave without Turnstile succeeds only with a valid resume token and is per-token rate-limited; public conditional projection persists only applicable answers through src/routes/public-form.shared.ts:309-351 and the public UI uses the shared evaluator at src/ui/public/form/PublicForm.tsx:116. Upload presign missing/invalid/out-of-scope inputs fail closed with no attachment row. Independent filename extension and MIME mismatches were each 400 with zero rows. Magic-byte mismatch was 409 and HEAD returned null after deletion. Valid serve returned attachment disposition and nosniff from the separate media host. Per-submission cap returned 429; a runtime probe returned 20 same-IP presigns=200, the 21st=429, a different IP=200, 21 rows and zero R2 objects. Traversal filename ../../outside/evil.pdf produced a generated uploads/{event}/draft_file/{random}.pdf key; putUrl host was fake-account.r2.cloudflarestorage.com and its path matched only that key, with no outside component.\n\nMachine guard: tests/node/public-write-inventory.test.mjs is an AST inventory with positive controls. It pins four projectApplicableAnswers call sites and six direct submission_answers writers, including the two known admin exceptions, so new direct writers or evaluator bypass surfaces force re-audit.\n\nCommands: node_modules/.bin/vitest run tests/unit/r2/uploads-routes.test.ts -> 9/9; node --test tests/node/public-write-inventory.test.mjs -> 1/1; npm run pr-gate -- --ticket MRQ-49 -> PASS, 17.789s under 45s, build/types/design contract passed, 32 test files and 180 tests passed, AC trace uncovered=0 and errors=0. The gate emitted only the expected missing-current-ticket-manifest warning and local missing-secret warnings.