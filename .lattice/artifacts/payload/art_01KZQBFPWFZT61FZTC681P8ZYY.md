Merged as PR #16 (squash). HAND-REVIEWED (auth-touching): it took the REAL fix, not the allowlist escape hatch — auth.endpoints.ts and admin-ops.endpoints.ts are now auth.routes.ts and admin-ops.routes.ts, inside the manifest glob, with routes declared through defineApiRoute so /api/v1/auth/demo and /api/v1/auth/magic-link now reach the generated manifest and OpenAPI. That disarms the check:api failure that walkthrough step 1 (a demo-login POST) would otherwise have triggered. CRITICAL CHECK: MRQ-3's guardrail tests are byte-for-byte unchanged — git diff of tests/integration/auth-demo.test.ts and auth-tokens.test.ts against master is EMPTY, so AC-2 (demo login 403s outside demo_mode, no cookie) and AC-214 (per-event reviewer scope incl. the schema CHECK) were not weakened to make the port pass. Secret scan clean. src/routes/ now contains only *.routes.ts modules plus _manifest.ts and submissions.queries.ts — the dodge pattern is eliminated fleet-wide.