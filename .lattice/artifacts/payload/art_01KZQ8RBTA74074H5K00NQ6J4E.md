Merged as PR #13 (squash). Verified the thing that mattered: MRQ-2's 0001_init.sql is UNTOUCHED — this is a clean additive 0002_venue_geography.sql, which is the correct handling of a merged, immutable migration. Columns carry real CHECK constraints (lat -90..90, lng -180..180, access_minutes >= 0 NOT NULL DEFAULT 0) rather than bare REALs. Schema mirrored in src/db/schema.ts, seed updated, and a dedicated venue-geography test added. Secret scan clean. No direct AC ownership, correctly declared rather than shipping an empty claims file.