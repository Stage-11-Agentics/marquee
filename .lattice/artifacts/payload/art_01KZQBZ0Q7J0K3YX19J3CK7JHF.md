Merged as PR #17 (squash). AC-246 GUARDRAIL HAND-REVIEWED and it is the right shape: a guessed out-of-scope submission ID returns 403 on BOTH the detail and /files surfaces, and the test asserts the response body contains neither the out-of-scope ID nor its title ('Security only hidden record') — no-metadata-leak proven, not just a status code. The reviewer-scoped export is separately asserted not to contain the hidden record, which is the leak path a status-code-only test would miss entirely. Manager scope editing requires >=1 track (empty set 400s) and preserves completed reviews. Walkthrough step 8's machinery is now live on the seed MRQ-5 laid down. Secret scan clean.