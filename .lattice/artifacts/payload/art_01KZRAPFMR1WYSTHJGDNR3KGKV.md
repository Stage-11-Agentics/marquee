Validation: PASS\nObserved path: tests/integration/reset-demo.test.ts ran the real app.request POST, operations queue consumer, D1 batch, KV job state, mirror queue, and Miniflare R2 binding. The test first dirties the database by accepting/rejecting submissions, completing a speaker task, publishing an agenda item, queueing a demo-safe reminder, creating a saved view, running an import, adding webhook rows, mirror outbox work, an attachment, and an upload; it also inserts an unrelated tenant, DB rows, and R2 object.\nCounts: after reset, every WIPE_ORDER table is queried and compared exactly. Seeded non-zero counts are organizations=1 events=1 formats=4 tracks=8 buildings=3 rooms=10 waves=3 people=1101 submissions=1000 submission_tracks=1153 submission_decisions=680 participations=1025 memberships=159 reviewer_track_scopes=32 evaluation_plans=1 evaluation_rounds=2 rubric_criteria=3 committees=1 committee_members=3 round_assignments=200 evaluations=60 submission_answers=4091 attachments=40 agenda_items=25 speaker_tasks=324 task_templates=6; all other WIPE_ORDER tables are zero. The unrelated tenant contributes the expected +1 in its scoped tables and its R2 object remains; the demo R2 object is gone.\nIdempotence: a second reset from the clean result repeats the exact per-table counts, preserves the unrelated rows/object, and sends exactly one mirror reconcile each time. Organizer and speaker demo login both returned 200 after each reset with the shipped identities.\nContracts: node reset UI, WIPE_ORDER, and MRQ-66 webhook tests passed 4/4. Browser: N/A; the touched reset flow was exercised through the real Worker queue/R2 integration, while the UI contract verifies the rendered action, pending state, error timeout, and reserved slot.