Validation: PASS\n\nExact HEAD: 21a1deda2b7a7257346b38fa7e95bd162d97b9cb.\nRunning-system proof used the repository local-runtime harness, which built this checkout, applied local D1 migrations, seeded scripts/seed/index.ts, started wrangler dev/miniflare, and stopped it in the harness cleanup.\n\ncurl flow observed at the running Worker:\n- GET /health -> 200, status ok.\n- POST /api/v1/auth/demo as organizer -> 200 and mq_session cookie.\n- GET /api/v1/events/evt_aie-ny-2026/files?state=all -> 200 with 153 seeded deliverable rows.\n- POST /api/v1/events/evt_aie-ny-2026/files/export with two selected task IDs and grouping=session -> 200 application/zip; 323-byte archive began with ZIP signature and contained manifest.txt.\n\nThe seeded local R2 state had no readable selected objects, so this probe verified the honest missing-object manifest path. The focused integration tests separately verified latest-pointer bytes, grouping, and lost-object handling. No dev server was left running.