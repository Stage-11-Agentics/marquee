npm run pr-gate -- --ticket MRQ-3 => pass (worker types, client types, test types, production build, design contract, hermetic suite 22/22 vitest + 12/12 node, merged AC trace 0 uncovered/0 errors).

Manual wrangler dev + curl walkthrough: N/A this cycle — cut under the operator's capacity-wall directive (Bravo ~75min runway) to prioritize landing a mergeable PR. Evidence instead comes from tests/integration/*.test.ts, which exercise the real Hono app (app.request), a real D1 instance (miniflare via @cloudflare/vitest-pool-workers), and the real queue-consumer dispatch path (createMessageBatch + worker.queue) rather than mocks — including the AC-2 fail-closed guardrail (403 + no Set-Cookie at demo_mode=0) and the AC-230 reset-restore-within-budget path. Flagged as a residual gap in the self-review, not claimed equivalent.