Validation: PASS\n\nExact HEAD: f10a3acb8557d1ca37adfc5bd0944bd090a93b05.\nThe local-runtime harness built this exact checkout, applied local D1 migrations, seeded scripts/seed/index.ts, started wrangler dev/miniflare, and cleaned up the temporary Worker/persistence.\n\nCurl flow:\n- GET /health -> 200, status ok.\n- POST /api/v1/auth/demo as organizer -> 200 with mq_session.\n- GET /api/v1/events/evt_aie-ny-2026/files?state=all -> 200 with 153 rows.\n- POST /api/v1/events/evt_aie-ny-2026/files/export for two selected task IDs, grouping=session -> 200; archive 323 bytes, ZIP signature PK, manifest.txt present.\n\nThe generated-registry-only delta does not alter runtime source behavior; the exact-head probe still exercised the live export route. The seeded local R2 state intentionally exercised the missing-object manifest path; focused integration tests cover latest-pointer and readable-object paths. No server remains running.