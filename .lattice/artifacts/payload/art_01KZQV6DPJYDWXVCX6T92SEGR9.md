Verdict: PASS-WITH-NITS\n\nReviewed commit: f439533b69a2bf88bb56a0e67b0a5f73da906db1\nPatch identity: the independent MRQ-32 slice from b071fc2, rebased onto forgejo/master b94f099.\n\nAdversarial review:\n- src/jobs/mail/templates.ts: seven trigger keys are materialized for fresh events with enabled=1; event-scoped overrides are used for edits and sends.\n- src/jobs/mail/triggers.ts: automated and bulk paths use enqueueOutbox/demo_safe; duplicate actions rely on the outbox UNIQUE idempotency key and do not pre-check.\n- src/jobs/mail/audience.ts: filters resolve through MRQ-8 selectSubmissionIds, then use a bounded JSON ID set with event scoping and deterministic per-recipient rows.\n- src/jobs/mail/schedule.ts and src/index.ts: pre-close configuration is read-only candidate selection and the hourly cron is wired without a send call at the scheduler edge.\n- src/routes/public-form.routes.ts: submission_confirmation remains enabled when state is absent/unset and only an explicit disabled row suppresses it.\n- src/routes/comms.routes.ts: one comms send route, no messages/send alias, audience read path, and default-template materialization are covered by AC-tagged tests.\n- tests/integration/mail.test.ts: repeat bulk action asserts one outbox row and one provider delivery; cross-event audience guard asserts both status and absence of leaked identifiers with a positive control.\n\nNits/deferred by explicit orchestration ruling:\n1. The current route still has the pre-MRQ-24 local recipient/merge adapter. After MRQ-24 merges, it must be replaced additively with its published recipientsFor/mergeDataForRecipient/renderTemplate/renderAdHocMail/renderMail seam; no second renderer or parallel send path may remain.\n2. Actual templated-send seam adoption and merge-field rendering for the deferred trigger call sites are intentionally pending MRQ-24 merge.\n\nNo independent-slice blocker found.