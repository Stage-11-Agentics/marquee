Merged as PR #11 (squash). Secret/privacy scan clean. The temporarily-public route is marked exactly as required — TODO(MRQ-60) inline at the policy block with auth kind public — so MRQ-60 can find and flip it. That hole is tracked: MRQ-60 cannot complete until this route requires auth, and MRQ-57 (real deploy) depends on MRQ-60, so it cannot reach the public site open. pr-gate PASS 10.1s at exact reviewed HEAD af9c9f9.