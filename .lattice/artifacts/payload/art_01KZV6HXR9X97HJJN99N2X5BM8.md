Validation commit: e9e6db9. PASS. Ran npx vitest run tests/integration/api/bound-form-options.MRQ-126.test.ts: 3 tests passed in 2.23s. This exercises the actual Worker SELF.fetch flow: public anonymous GET receives live format/track rows; authenticated field creation preserves custom options and stores only source for bound fields; invalid source/type returns 422 without a row; settings renames make new names submit successfully to real format_id/primary_track_id; old names reject with field-keyed issues and no submission row. Also passed unit bound-options (8/8), seed assertion, all three TypeScript projects, and check:api. No dev server was left running; browser automation was not used because the covered acceptance path is the local Worker/API flow.