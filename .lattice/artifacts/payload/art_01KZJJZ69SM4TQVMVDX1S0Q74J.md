VERDICT — choose one json_each(?) JSON ID-array binding as M-07's default; keep ≤90-total-binding chunks only as a fallback. Local D1 (Wrangler 4.120.0/workerd 1.20260801.1) changed exactly 150/1,000 rows in 40/40 trials. At 1,000: json_each 1 write query, 7 bindings, 6 ms median/6 ms p95 and 5,000 rows read; batched 84-ID chunks 12 queries, up to 90 bindings, 8.5 ms median/12 ms p95 and 15,000 rows read. 100 bindings succeeded; 101 failed with too many SQL variables. Recommended signature: runBulkByIds<T>(ids: readonly string[], prepare: (idsJson: string) => D1PreparedStatement): Promise<D1Result<T> | null>, with dedupe, empty no-op, one stringify, one run, and WHERE id IN (SELECT CAST(value AS TEXT) FROM json_each(?)). Local verifies SQLite/D1 binding behavior and query counts; production latency, account-plan limit enforcement, and the assembled M-18 transaction remain deploy-verified-later.