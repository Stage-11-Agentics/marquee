Board reconciliation 2026-08-12: verified DONE, having drifted BACKWARDS at 'backlog'. PR #106 ('Seed the autosave limiter instead of racing it: AC-42 could not trip under load') merged 2026-08-12T17:15:46Z, merge commit 72648d63, confirmed an ancestor of github/main. The PR addresses precisely this ticket's mechanism: the fixed window keyed on the wall-clock minute in draftTokenAllowed(). The fix seeds the counter to its limit in both the current and next window rather than bursting 35 PATCHes at it, and exports the limit/window/key-builder from the route so test and production cannot drift. PR reports pr-gate green at 53.7s and the test passing 8/8 concurrent runs. Production limiter behaviour unchanged. Preceding planned/in_progress events are reconciliation bookkeeping.