Validation: PASS\n\nExact HEAD: 19fe323ce8241d63fb54514ea676bc7fc483aa0e.\nThe repository local-runtime harness built this exact checkout, applied local D1 migrations, seeded scripts/seed/index.ts, started wrangler dev/miniflare, and cleaned up the process and temporary persistence.\n\ncurl flow at the running Worker:\n- GET /health -> 200 with status ok.\n- POST /api/v1/auth/demo as organizer -> 200 with mq_session.\n- GET /api/v1/events/evt_aie-ny-2026/files?state=all -> 200 with 153 deliverable rows.\n- POST /api/v1/events/evt_aie-ny-2026/files/export for two selected task IDs with grouping=session -> 200; saved archive was 323 bytes, began with ZIP signature PK, and contained manifest.txt.\n\nSeeded local R2 has no readable selected objects, so the live probe exercised the honest missing-object manifest path. Focused integration tests cover latest-pointer bytes, grouping, and lost-object handling. No dev server remains running.