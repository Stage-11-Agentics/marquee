# Marquee — Tone Run State

**Project:** Marquee — open-source speaker/session management platform for conference organizers (Sessionboard replacement).
**Home:** `~/Projects/Stage11/deployments/Marquee` — canonical remote is **GitHub `Stage-11-Agentics/marquee`, private, branch `main`** (operator ruling 2026-08-11). Forgejo is retired and its remote removed. The public competition repo is a separate curated artifact: the orphan `mrq-42-assembly`, never `main`.
**Context:** Entry into swyx's "$10,000 Kill My SaaS" hackathon (AI Engineer / latent.space team). Deadline: **Wednesday 2026-08-12, 10PM PT**. Judged by AIE team against a walkthrough video, on a deployed site + open-source repo.

## Current stage & phase

- **Stage:** lattice-orchestrator build, post-drain (2026-08-11 evening). **The board is clear — every MRQ ticket through MRQ-83 is merged and `main` is CI-green.** Merging now goes through a human gate (`CODEOWNERS` + the "main: manual merge gate" ruleset); agents open PRs, a human merges. Next work items, both owned and written down rather than folklore: the suite at 145–158s against a 45s objective (lever = fewer files needing a Worker, surface:254), and the real Cloudflare deploy (MRQ-57), which is blocked on `wrangler login` and nothing else.
- **Stage (historical):** tone-prototype v1.4 client review gate / tone-architect contract reconciled (2026-08-09).
- **State:** Pipeline v1.4 is complete in place (legacy path `prototypes/pipeline-v1.1/index.html`): multiple independent forms; complete CFP participant/profile/file/conditional fields; open/closed/at-limit/resumed states; real acknowledge/form/file portal tasks; decision feedback and one-off mail; reviewer full detail plus score-optional Approve/Maybe/Deny and explicit track scopes; saved personal views; configurable columns; and a role-gated Drafts needing attention queue. Program board remains read-only with record-owned actions. `USER_STORIES.md`, `SPEC.md`, `EVALUATION.md`, and `BUILDPLAN.md` now agree through 248 live criteria / AC-249 (AC-239 struck).
- **THE GATE:** Atin drives v1.4 → love/iterate/sign. Then: mint `DESIGN.md` from the signed prototype → adversarial contract pass → lattice-orchestrator kickoff with a **Codex build fleet**. Do not dispatch before that gate.
- **Externals still open:** Discord relay (Sat video, Sun freeze video, Q1/Q2 posts); account checks (Workers Paid, R2 entitlement, Resend tier, Airtable Team ×2 bases); gate materials (Gmail/Outlook/Apple inboxes, real Sessionize export, CF API token, model credential).

## Commission facts (from Atin, 2026-08-08)

- Target to replace: **Sessionboard.com** (speaker/session management for event organizers, >$40k/yr).
- **Organizer-first**, but the product touches all seats (organizer, speaker, reviewer, attendee-facing outputs).
- **Open source** platform, generalizable to many conferences — but it will be used by a **real conference** (the AIE team's).
- End goal beyond the competition: a completely fully functional working product.
- Sequence agreed: understand problem area (user stories, stakeholders, existing-product features) → core product offering → build plan + HTML prototypes → build.

## Competition brief digest (source: `sequence/research/sources/competition-brief.md`)

Six active features (CFP forms w/ conditional logic; speaker portal; automated comms + calendar invites; evaluation/scoring w/ optional AI review; drag-and-drop schedule builder w/ conflict detection + list/day/week/track/room views; onboarding-status dashboard). Three struck: Accelevents integration, wiki/resource pages, embeddable gallery. Walkthrough video = de facto spec; more videos Sat + Sun, then **requirements freeze**. Stack: free choice; mild bonus Cloudflare, bonus Airtable persistence. $500 token reimbursement for valid submissions. Winner: $10k + latent.space writeup.

## Decisions log

- 2026-08-18: **MRQ-281's own harm was closed by follow-up PR #336 (`6e6248c6`), after the original merge was corrected rather than papered over.** PR #333 (`b1346a90`) merged at 18:28:36Z, **91 seconds before** its independent review landed at 18:30:07Z. That review was titled **"Independent review — REQUEST CHANGES at `2bd20455`"** and found the ticket's data loss still reachable through the organizer confirmation-status PATCH: `pending` wrote `invited_at = NULL`, declared `kind:"status"`, emitted no `speaker_roster_linked` ledger claim, and import undo removed the seat so portal `/me` returned 404. The earlier closeout's review-PASS claim was false and was corrected in the run state. The follow-up was frozen at exact head `5f8e9ba0`, received a real **"## Independent review"** comment with **"Verdict: PASS"**, and merged at `6e6248c6` after hosted fast-gate's log reported test status `pass-over-budget` (294 pass, 0 fail). The same reviewer reran the real seeded browser path: pending PATCH → import undo kept the seat (`removed: 0, retained: 2`), the roster stayed at 1 of 1, and `/api/v1/me/portal` returned 200 with `seat:"speaker"` while `invited_at` remained NULL; a plain import with zero ledger rows still removed its own seat (`removed: 1, retained: 0`). The fix removes the non-claim `status` kind and routes all four status values through `kind:"claim"` with `speaker_roster_linked`. The durable design shift is **default-to-claim**: the writer union is now `import | claim`, so a future writer must actively choose import to be silent instead of falling through an unlisted non-claim kind; the undo guard still enumerates its retention signals, and the audit ledger's non-idempotence, person+event scope, and retention dependency remain known safe-direction properties. The stale guard comment was corrected alongside this closeout. Live remains `be03d205`; no deployment was made, and deploy remains held for the ordered follow-ups.

- 2026-08-18: **MRQ-281 merged in PR #333 (`b1346a90`) before its independent review landed.** The actual review comment, posted at 18:30:07Z after the 18:28:36Z merge, is titled **"Independent review — REQUEST CHANGES at `2bd20455`"** and found the ticket's own data-loss harm still reachable through the organizer confirmation-status PATCH: `pending` writes `invited_at = NULL`, declares `kind:"status"`, and therefore emits no `speaker_roster_linked` ledger claim, so import undo can delete the seat and portal `/me` no longer resolves it. The earlier closeout's review-PASS claim was false and has been corrected. MRQ-281 is reopened `in_progress`; follow-up work is on `mrq-281-pending-status` to classify that writer as a claim, test the real PATCH -> undo -> portal path for pending, and cover all four status values. PR #333 is merged, but this follow-up blocks deployment; live remains `be03d205` and no deployment was made.

- 2026-08-18: **MRQ-284 is merged (`bd45f666`, PR #331) — Marquee answers the Model Context Protocol at `POST /mcp`, as a façade over the API it already had.** (An Opus-5 delegator at surface:129 + an independent Opus-5 reviewer) Stateless JSON-RPC 2.0 over Streamable HTTP; no SSE, no OAuth, **bearer only**; `initialize` / `tools/list` / `tools/call` / `ping`, bounded batches, notifications answered 202, `GET` answered 405. **23 curated tools** — 6 public (the published program, one session, one speaker, what a call for speakers asks, sending an abstract to it, starring a session: exactly what a signed-out browser reaches) and 17 that appear once a scoped token is presented. **The one design decision, and the whole reason this is not a second product: a tool dispatches an internal sub-request through the SAME assembled API app the Worker mounts at `/api/*` — not the handler function, the app.** So credential resolution, the rate-limit bucket, `authorize()`, zod validation, ETag/CAS, idempotency, audit rows, demo-safe mail and the one error envelope are the shipped pipeline, byte for byte. A tool declares only an `operationId` plus how its arguments become a path, a query, a body and two headers, and **its tier is read back out of the route registry**, so it cannot claim an authority its route does not have. Tier resolution calls the same `tokenHasGrant` / `roleForEvent` / `membershipAllowsGrant` functions `router.ts`'s own `authorize()` calls, never a copy. Proven live rather than asserted: the official `@modelcontextprotocol/inspector` CLI connected to `npm run dev` and listed 6 tools anonymous / 23 with a token, and `decision_plan` → `apply_decisions` ran end to end against the seeded 1,003-submission conference (plan 23ms, apply 45ms, one outbox row enqueued, a stale fingerprint and a replay both refused in the API's own sentences). **The scope deviation is the durable part.** The ticket named `my_tasks` and `complete_task`; both wrap `/api/v1/me/portal` and `/api/v1/me/tasks/{id}/complete`, which begin with `requireUnscopedSpeakerSession` and refuse anything that is not `kind: "session"` — and this endpoint reads no cookie, deliberately, so that a cross-site POST carrying a browser's session cannot act through it. There is **no credential the endpoint accepts that could ever drive them**, so they were removed rather than shipped as two doors opening onto a wall, per the ticket's own rule that an API-side gap is an API change first or out of scope. **Follow-up candidate (not minted): bind an API token to a speaker seat** the way agent evaluator seats already bind to a reviewer seat via `acts_as_person_id` — that restores both tools and lets a speaker's own agent do its onboarding work. **The independent review is why this entry can be trusted**, and it is the second time in two days that a reviewer denied the author's account and found what the author's own tests could not: it confirmed the central claim ("I could not break it… the sub-request is strictly narrower than a browser's") and then found four real defects underneath it. **The dead tools had shipped and no test called either one** — closed by a smoke pass that now calls *every* listed tool and fails on an argument-mapping fault, a 500, or a listed-then-unknown tool. **A path parameter could move a call onto a different operation than the one whose policy was checked**: `encodeURIComponent` leaves `.` alone and the WHATWG parser removes dot segments, so `review_submission` with `round_id: ".."` was gated as the blinded reviewer route and served by the organizer's *unblinded* record — never an escalation, because every route enforces itself, but the binding this design rests on had quietly stopped being true. **The unreachable-tool refusal ran after argument validation**, handing an anonymous caller the full argument schema of a tool `tools/list` withholds, and the two different sentences were a clean existence oracle for the catalogue; concealment now runs first and is word-for-word what a misspelling gets. **`comms_audience` and `send_reminder` speak disjoint selection languages**, so an agent obeying the "count before you send" habit could count 40 by track and then mail every accepted speaker, because the sender has no track filter — both descriptions now mandate sending with the `person_ids` the count returned. Also fixed: unbounded batches (capped at 32) where one member's thrown header value killed the whole batch as a plain-text 500; tier resolution failing **closed** for an org-wide seat, which is one membership row with `event_id = null` that applies to every conference; and three user-facing surfaces that claimed the tool set widens to "exactly what that token already allows over the API" when eight signed routes carry an `authenticated` policy and check the seat inside the handler — a listing is what the connection reaches, not a promise that every entry answers. **Shipped alongside, because a first read needs somewhere to land: the `agent_score` sort.** The pile already rendered each agent score beside the committee's and already returned `agent_reviews_json`, but `SUBMISSION_SORTS` had no agent key, so the pile could not be *ordered* by it — and ordering the chair's evening is the entire point of a first read. Added through the sort registry, the endpoint's whitelist, saved views and the sort control, taking a chair's override so the column sorts by the number the row displays. `/agents` gained a Connect-over-MCP block (resolved against the instance's own origin, never a baked-in domain) and a worked example — *Let an outside agent do the AI first read* — with the copy-ready brief that mints the seat, pulls the queue, records evaluations with rationales citing the abstract, abstains on a conflict, and decides nothing. **`check:routes` grew a fourth generated source** — the machine doors `src/index.ts` registers itself — so `/mcp`, `/health`, `/SKILL.md`, `/mirror/webhook` and `/__validation/session-cookie` are documented from the code with their methods rather than from memory, and the scan follows an imported `MCP_PATH`-style constant so a renamed endpoint moves with it. **Two house rules bit and are worth remembering:** `SKILL.md` is *generated* (`cli/generate-skill.mjs`), so a hand-edit fails its own contract test, and that test forbids the word "proposal" — the house vocabulary is Abstract and Session — which is why the skill's MCP section describes the tiers and points at `tools/list` instead of reprinting tool names. And `/mcp` is deliberately **not** in the fetchable machine index in `llms.txt`: that list is contract-tested by GETting every link, and this endpoint answers POST only. **The CI story is the other durable finding, and it hands MRQ-280 a measured lever.** `fast-gate` failed three times, every one reporting `timeout` with `fail 0` and zero assertion diffs — the 600s hard-limit kill, not a test. A re-run of the identical sha timed out again, so rather than roll a fourth time the runs were read for numbers: `main` itself runs **555,829ms**, MRQ-279 passed at **596,283ms — by 3.7 seconds**, and this branch's first head passed at 571,279ms before later heads sat at 600,0xx. **The first hypothesis was wrong and CI said so precisely:** merging nineteen test blocks into twelve — seven fewer full `applyMigrations()` wipes, all 92 assertions kept — moved the suite by **83 milliseconds** (600,147 → 600,064). What a workers-pool file actually costs is its **Miniflare isolate**. Folding those twelve blocks into `tests/integration/api/meta.test.ts` under `describe("mcp")` — the machine-surface file, which already owns the OpenAPI document, `/api/docs` and `/SKILL.md`, and which has no top-level `beforeEach` so nothing above them pays for their fixture — took 121 workers-pool files to **120** and the suite to **560,308ms**, green. **So one workers-pool file is worth roughly 40 seconds of CI, and per-test seeding is nearly free.** MRQ-280's lever is file count, not test count. Standing caution: the band is 556–600s, so "re-run once" will eventually go green on any branch, which is exactly how a real `fail` gets waved through as the known ceiling — the flag raised here (cleared once the fold landed) was deliberately *not* resolved by re-rolling. **An admin-merge exception was ruled available and turned out not to be needed:** Atin authorized merging PR #331 past a failing `fast-gate` on 2026-08-18 *if* the fold failed to bring the suite under the ceiling, with a mandated disclosure sentence and an explicit instruction not to generalize it in `CLAUDE.md`. The fold brought it under, so the PR merged on an ordinary green required check and **no exception was used** — the disclosure sentence is deliberately absent because writing it would record a merge-over-timeout that did not happen. **Not deployed** — merging does not ship (`DEPLOY.md`).

- 2026-08-17: **MRQ-277 is merged (`0377b928`, PR #329) — the round-15 defect sweep, and it corrected two of its own ticket's premises rather than coding to them.** (Eval Runner + an Opus-5 implementer at surface:90 + an independent Opus-5 reviewer at surface:92) Ten of eleven defects fixed and driven in a real browser; the eleventh, the routing-rules error banner, was already fixed on the base by MRQ-265 and was **verified rather than re-fixed**. **The two corrections are the durable content of this entry.** *D1 was one reading fault, not the two bugs the ticket asserted.* A levels answer writes **both** columns — `value_text` holds the label, `value_json` holds `{bound_source, id, label}` so routing can resolve the level row without a second lookup — and three readers preferred the JSON, handing a `<select>` an object, which renders as no answer and is then not a valid choice at submit. **Nothing ever enforced "optional"**; the eval judge's reading and the ticket's both had the mechanism wrong. One shared reader (`src/lib/stored-answer.ts`) fixes it and is **retroactive on already-stored data** because `value_text` was always right — a DB-wide scan found zero divergent rows. It also explains what the Runner saw and could not account for: the *required* Format select survived because it is not bound to Conference levels and never takes the JSON path, and re-selecting worked because a fresh selection writes a string. *D2's sort SQL was never broken.* The seed carried **one scored abstract in 1,003**, so both directions returned an identical page and a working control was indistinguishable from a dead one — textbook category (b) in the auto-eval taxonomy, a state-dependent surface the run's own actions hid (round 14 passed it only because the browsing agent had entered three reviews itself). Proven on a controlled fixture by the reviewer with the sort SQL untouched; fixed **in the seed**, with every fifth review left recommendation-only so "Not scored" must still sort last. **The review found real data loss that D3's own fix introduced:** import undo deleted speaker seats it had merely *matched*, destroying a row that gates speaker-portal sign-in. The root cause is worth keeping — `event_attendances` carries `source` **in its unique key**, so an imported attendance row is its own row and scoping its undo by source is safe; `memberships` is unique on `(org, event, person, role)`, so an import's seat and an organizer's are the **same row**, and the shape was copied without the property that made it work. Fixed with an **undo ledger rather than a provenance column**, deliberately: `memberships` upserts with `ON CONFLICT DO UPDATE`, so a column would depend on every future edit remembering not to stamp it on the conflict path — one forgotten line re-arms data loss on a central table. **Known residual, merged deliberately as MRQ-281:** undoing an import still withdraws a seat an organizer has since adopted via Add speaker (`201`, same row, only `invited_at` stamped), leaving the person in People CRM but off the roster and out of the portal — consistent with *reverse your own writes* by authorship, not with a later actor's intent, and the adjacent attendance path disagrees because it kept its multi-claim guard. Also landed: the onboarding board restored to baseline (`portal_invitable` is now a CTE materialized once and joined, 2.74s → 1.68s, all 513 rows byte-identical across both arms). **Process notes that held:** the reviewer was framed on the *symptoms* and explicitly denied the author's account of the causes, which is the only reason the undo data loss was found — the diff reads correctly and nothing in the author's explanation points at it. **Two docs fixed on `main` the same session, both auto-loaded or deploy-critical:** `CLAUDE.md`'s CI table asserted "CI's suite runs ~305s against 600s" when the observed margin is now **under 30 seconds** (`a8d22342`, and **MRQ-280** owns restoring the headroom), and `DEPLOY.md` gained the local-upload recipe — `LOCAL_UPLOAD_SHIM:1` **plus** `UPLOAD_TOKEN_SECRET` on the same line, without which no local browser can complete a required upload (`98968652`). Two agents in one night deferred the same headshot proof as "needs R2 credentials"; it was documented all along in `src/agent-front-door/llms-full.txt`, **the file written for agents to read**. (Atin + Eval Runner surface:85)

- 2026-08-17: **sbek eval round 15 — 85.8% at 96.9% coverage, down 8.2 from round 14; the score floor tripped and the loop is HALTED.** (Eval Runner, run `2026-08-17T01-35-54`, build `cd1d34b86acd`) The round graded the adoption batch's first deployment: the live site had been **252 commits behind** and the whole 19-PR batch had never shipped. Two things had to be discovered before the round could start. **The demo reset was broken on the deployed build** — production D1 had been migrated through `0038` during the batch close-out while the Worker was still pre-`0028`, so `reset-demo` died on the `person_references_remaining` trigger from migration 0035 (`D1_ERROR: SQLITE_CONSTRAINT_TRIGGER`). Schema ahead of code is the one ordering the barrier cannot recover from, since it resets *before* it deploys; the fix was to deploy first and re-run the barrier. **This is also why the guard's rollback advice must be refused here:** reverting to anchor `bbb8e21e3ae3` would restore a build that can no longer reset its own demo. Forward is the only direction. Per-area: call-for-papers **90.5% → 66.7%** (coverage 97.4% → 86.8%), abstract-management 94.6% → 83.9%, speaker-management 92.2% → 85.9%, content-management 91.9% → 88.7%, ai-agenda 93.8% → **94.4% with coverage 88.9% → 100%**, public-widgets held **100%/100%**, speaker-crm 89.5% → 94.1% (coverage fell to 89.5% on an unreached CRM-05, the browsing agent's own recorded call). Three of the four falling areas held coverage flat, so this is product movement, not a thinner run. **Sixteen items moved backward.** The headline defect was reproduced independently on the live site by the Runner and, in doing so, **narrowed**: a saved-draft round-trip silently discards the value of an *optional* select (Audience level) while title, abstract, outcome, tracks and the *required* Format select all survive — and the emptied field is then apparently enforced as required at submit. **That last clause is wrong and MRQ-277 settled it — see the entry above: nothing ever enforced "optional", and there was one fault rather than two.** The run concluded from six reproductions that `/f/cfp` was unsubmittable; the Runner found a recovery path (re-select, resubmit) but could not complete a full submission because the required headshot is a file upload its browser cannot drive. Also new and confirmed: the weighted-score sort is inert (ABS-10, pass → fail — URL and arrow flip, row order byte-identical), and **SPK-03 was finally reached after four rounds** and is broken — CSV import reports "3 created", the people land in org People CRM, and they carry CONFS 0 with the conference roster unchanged. The four never-reached items all landed this round: AIA-05 proven by forcing a real room overlap rather than inferred from a missing drop target, AIA-06 a session moved and surviving reload, plus SPK-03 and CNT-10. **Three tickets minted (single-writer, by the Runner at Atin's direction, deliberately overriding EVAL.md's Runner/Triage split for this batch): MRQ-277** the eleven-defect sweep (critical, an Opus-5 delegator running it at surface:90), **MRQ-278** the content-approval state, and **MRQ-279** the submitter's own home. **Two operator rulings, Atin, 2026-08-17: CNT-12 is ADOPT** — an editorial approval gate distinct from the scheduling gate, closing a question carried open since round 11 — and **the defect sweep stays separate from the two product stories**, because a submitter dashboard is auth plus a new route plus a new page and does not belong in the same diff as eleven small fixes on a build that just regressed 8 points. **Roughly 20 further feature tickets are near merge, and the eval re-runs shortly**, so round 16 will measure the sweep and those features together rather than in isolation; attribution across that boundary will need care. Round-15 housekeeping is done: demo reset, freeze lifted, and the one `submissionNotes` inaccuracy the run found (`/embed/config` offers five output formats, not four) corrected and pushed to Atlas. `sequence/eval-path-to-100.md` still reads "last refreshed: round 14" and needs its round-15 regeneration. (Atin + Eval Runner surface:85)

- 2026-08-16: **MRQ-242's internal-notes contract is consolidated after MRQ-224 landed.** PR #303 merged at `e24e68e843a476c4d9f824e3083edf9ef16cebda`, carrying Amendment 29 and migration 0028. This fold allocates exactly **US-96 / AC-337 – AC-342** to MRQ-242 and keeps migration **0029** unchanged: `submission_notes` is append-only, attributed from the authenticated seat to an organization-scoped people row, with staff-only GET/POST routes, CLI/SKILL parity, and no speaker/public/outbound projection. The shared pointer advances to **US-97 / AC-343** for MRQ-241. Browser/two-seat evidence remains held for the operator.

- 2026-08-15/16: **The eight-ticket plumbing batch is fully landed — PRs #294–#301, every one independently reviewed before merge.** (Plumbing Batch Orchestrator, commissioned by Atin via the Adoption Audit surface) Wave A: MRQ-232 (#294, bulk-publish embed purge), MRQ-240 (#296, 422 focus + portal vocabulary + draft projection; sponsor `courting` ruled "In discussion"), MRQ-231 (#295, importer writes `submission_tracks` + provenance-scoped undo), MRQ-238 (#297, real `smoke:mail`/`smoke:ics` + `tooling/inbox-worker/`), MRQ-226 (#298, idempotency registry + branded entity ids + durable compose keys; Bug A's premise found non-reproducible and honestly reverted; AC-314). Wave B: MRQ-230 (#299, deciding-is-not-publishing invariant SPEC §7 + one-choke-point mirror rejection pipeline; AC-315–318), MRQ-228 (#300, calendar truth: migration 0026 snapshots/ledger/cancellations; Amendment 28, US-94, AC-319–328). Wave C: MRQ-239 (#301, mirror transition guard + Airtable decision rows; migration 0027; merged by Atin directly, post-merge audit CLEAR). Next mint **AC-329**; next migration **0028**. Process notes that held: every PR got a non-author adversarial review plus a rework verification, which caught — among others — a public page still serving after a confirmed deny, a database-global drain contaminating reversal counts, a NULL-snapshot 500 for legacy invites, an idempotency fix for a bug that did not exist, and a silent two-0026 migration collision. **MRQ-238's deployed proof remains operator-only** (needs-human flag standing: Email Routing on `inbox.marquee.stage11.dev`, DNS, inbox D1 + deploys, smoke token, Gmail/Outlook/Apple accept/reschedule/cancel). Cross-cutting follow-up candidates recorded in ticket comments: `PATCH/DELETE /agenda/items/:id` still purge no embed cache (MRQ-232 review); `published_count` response-field overload awaits MRQ-234; the auto-review daemon cannot diff linked worktrees (MRQ-147); a direct `run-test.mjs` invocation was observed printing `status: "fail"` with exit 0 (unconfirmed, invocation-path-specific — the gate itself reds correctly).

- 2026-08-15: **MRQ-215 PR #275's record corrected after post-merge review.** The PR body described a four-commit change and reported validation at `324109ba`, but merged PR #275 contains eight commits (`7249d120`, `42fbde58`, `b4bb9faa`, `8e3fe7ed`, `ed00855c`, `ab1a326a`, `6abb6cf5`, and `f1981896`) and merge SHA `371831d62d4b6f824933bf6692f68ba84134911f`; `324109ba` is not one of those commits. The four omitted commits were `ed00855c` (list-state preservation), `ab1a326a` (the shared roster-board list envelope), `6abb6cf5` (responsive design contracts, including removal of the mobile Playwright project), and `f1981896` (remaining phone-width surfaces). In particular, `ab1a326a` changed `GET /api/v1/events/{eventId}/speakers` and `/onboarding` from `{rows,total}` to the Amendment-7 list envelope and defaulted both to 50 rows per page — a breaking API change that the mobile-sweep record must name. This is a historical correction; the resulting count semantics are recorded in `SPEC.md` Amendment 7.
- 2026-08-15: **The Airtable mirror is finished — cut in August, shipped today, end to end.** (Atin + Airtable intake surface:421) Two tickets, both merged: **MRQ-217** (PR #281, `78094e25`) the outbound change feed, and **MRQ-223** (PR #286, `a3feb61b`) the finisher — connect, inbound, the settings door, and the agent surface. `SPEC.md` Amendments 24 and 25 carry the contract; US-92 / AC-308 – AC-313 minted, `EVALUATION.md` §2.8 owns their rows, next mint **AC-314**. **The ruling that made it possible the second time: the credential was decoupled from the build.** Everything is proven against an injected fake transport, so no criterion ever waited on the Airtable base whose absence killed Amendment 17 — and `check:mirror` went from a stub to a hermetic proof rather than to a gate that needs a human errand. **The build is a case study in what tests do not catch.** MRQ-217 passed every gate while carrying three defects a reader found and no assertion could: `clearMirrorOutbox` called from a global middleware whenever config was absent, so any transient credential loss silently destroyed the pending feed; two independent token buckets against one base, letting 8 req/s through a 5 req/s ceiling while every individual ≤4 req/s assertion stayed honest; and `attempts` incremented but never read, so a poison row would starve every healthy row behind it. All three were composition errors. **Worse, it merged with no on switch at all** — the triggers fire only on a non-empty `mirror_state.airtable_table_id`, nothing in the product wrote one, and every test passed because it inserted `mirror_state` as a fixture. That is the exact failure `CLAUDE.md` names, and it survived two review rounds because both checked the code against §3.9, which had quietly assumed out-of-band configuration. **AC-310 is the durable answer: it forbids a `mirror_state` fixture in its own test, so the connect flow itself must be what turns the mirror on** — the assertion MRQ-217's suite structurally could not make about itself. Also fixed en route: `CLAUDE.md`'s wall-clock rule was stale (MRQ-187 had already given `check:seed` a warn tier), and MRQ-186 was a duplicate of it. **Not deployed** — merging does not ship (`DEPLOY.md`); the mirror is on `main` and dark until someone runs a deploy, and it stays inert until a base is connected. (Atin)
- 2026-08-15: **Board reconciled — seven tickets were finished and still reading in-flight, and one agent's work was stranded off the remote entirely.** (Atin + Airtable intake surface) Asked whether MRQ-204/207 were really in flight, the answer was no: `gh pr list --state open` returned exactly one row across the whole repo. **Seven tickets moved to done against their merged PRs** — MRQ-164 (#182/#184), MRQ-165 (#183), MRQ-183 (#218), MRQ-204 (#256), MRQ-205 (#259), MRQ-207 (#263, whose ticket was last touched 31 seconds *before* its own merge), MRQ-208 (#266). Three needed `--force`: Lattice will not take `in_progress → done` directly, and MRQ-205 tripped a completion policy demanding a review role, **because this fleet's reviews happen in `review-NNN` worktrees and land as ticket comments rather than as GitHub reviews or role-tagged artifacts** — every one of those PRs shows `0 reviews` on GitHub while carrying a full adversarial review in its Lattice comments. Worth fixing at the source: a completion policy that cannot see where the reviews actually are will keep being forced, and a policy that is always forced is not a policy. **The real find was MRQ-157** (`V2-8: agenda grid placement at 15-minute increments`): `agent:codex-v2-8` died 2026-08-12 at 19:27 ET holding four commits of finished-looking work in a local worktree that had **never been pushed and had no PR** — invisible to `gh`, invisible to every other agent, one `git worktree prune` from gone. This is the second time this exact failure has been caught by hand (see the 2026-08-11 entry). Branch pushed to `github` as `v2-8-grid-increments`, ticket returned to backlog and unassigned with the SHAs recorded in a comment. **Nothing now reads in progress, in review, or PR-open except MRQ-215's genuinely open #275** — which is itself inconsistent the other way (ticket says backlog/unassigned while its PR is live), left alone because something is actively pushing to it. Standing lesson: **ticket status is a claim, `gh pr list` plus a worktree check is the evidence**, and the gap between them grows silently every hour a fleet runs. (Atin)
- 2026-08-15: **Portal task button reads `Finish now`/`View`, not `Complete`/`Update` — SPEC § 5.6 divergence marker minted.** (Atin) On the speaker portal the row action was labelled `Complete` while the finished rows directly above and below it carry a `COMPLETE` status chip: one word doing both jobs on one screen, so the filled primary button reads as a badge announcing the task is already done rather than an instruction to go do it. `Complete` stays as the **status**; the button now says what pressing it leads you to do. **Both branches of that label diverge from the contract, not one** — the done row already shipped `View` where SPEC and both prototypes say `Update`, so the marker covers the pair. One change in the shared `TaskRow` (`src/ui/portal/task-machinery.tsx`), so the sponsor portal inherits it; its panel note was changed with it (`Complete the overdue item first` → `Finish the overdue item first`, an instruction whose verb no longer named its control). `prototypes/pipeline-v1.1/` and `prototypes/sponsor-portal/` still say `Complete`/`Update` and are left as the lineage they are — **DESIGN.md makes the prototype binding, so the legal mechanism is the SPEC marker, and it is minted at SPEC.md § 5.6.** This run-state entry is the reasoning, not the authority. Locked by `tests/unit/portal-task-action-label.test.ts` because MRQ-214 builds the sponsor portal from a prototype that still says `Complete`: without the test the ruling reverts on a green gate. (Found by Atin in use; SPEC-marker gap, sponsor-portal note, and the missing lock all found by code review on PR #278.)
- 2026-08-15: **The Airtable mirror is reinstated — SPEC Amendment 24; MRQ-217 + MRQ-218 minted, deliberately not launched.** (Atin + Airtable intake surface) Amendment 17 cut the mirror on 2026-08-11 for exactly one reason — no Airtable base was in hand and the deploy was unshipped — and that condition is gone. **§3.9 is unchanged and re-enters build scope; AC-225 – AC-229 are live again.** The ruling that makes this attempt different: **the credential is decoupled from the build.** The mirror is written against an *injected Airtable transport* (real `fetch` implementation on one side, fake on the other), so `npm test` covers every criterion — round-trip, allowlist, sustained-editing echo suppression — with no token, no base, no network. A missing base now decides when `check:mirror` runs against something real, not whether the code exists; the first attempt died of a track that could not start because a human had not finished an errand, and that does not happen twice. **Absent config disables the mirror cleanly** — feed idle, consumer no-op, webhook 404, Settings says "not connected" — so a Marquee with the mirror off is indistinguishable from today's. Mirrored set is **`submissions`, `speaker_tasks`, `people` and nothing else**; widening it is a new amendment, not a delegator's call, because each added table is an ops-editable write path into D1. Cancelled **MRQ-46**'s isolation audit is folded into MRQ-217 as the import-boundary lint it would have audited for (a guardrail shipping with the code it guards cannot be skipped for capacity); **MRQ-54**'s spike is not reinstated. Every seam survived Amendment 17 intact and is marked in-tree: schema + indexes at `migrations/0001_init.sql:632`, `MIRROR_QUEUE` bound, `MIRROR_RECONCILE_MESSAGE_TYPE` stub-acked at `src/index.ts:249`, the keepalive cron registered at `wrangler.jsonc:121` with no handler, the `mirror:write` scope live, and `changed_in_airtable` already rendering in the decided-not-notified view behind a detail string that says it is theoretical. **Hold lifted the same night: MRQ-217 launched** (codex `gpt-5.6-luna` at `max`, worktree `mrq-217-airtable-outbound` off `github/main`, boot at `.lattice/orchestration/boot/mrq-217.md`, suppressed, reporting to surface:421). MRQ-218 stays backlog behind its `depends_on`. The hold had been written against a board five tickets out of date — see the reconciliation entry above. (Atin)
- 2026-08-14 (evening): **Sponsor portal — DESIGN LOVED, no change requests; MRQ-214 cut and HELD.** (Atin + sponsor-portal design surface) The dedicated portal round (sponsors-design ruling 3) produced `prototypes/sponsor-portal/index.html` (commit `6ab0b7ee`), the binding design contract: the speaker portal's sibling with the sponsorship as center of gravity — tier-stamped company hero with a derived deal line, booth as the speaker location card reused (map as receipt), deliverables with assignee labeling, read-only Session cards filled through the task machinery. Round rulings recorded in `sequence/sponsors-design.md` §5: whole-sponsorship visibility with **anyone-completes + attribution recorded**; **sponsorship hero**; **Sessions read-only, task machinery the single write path**; and the prototype's **Gold/Silver + viewer demo views carry into the build's seed** ("keep those in whatever we're building"). **MRQ-214** minted for the build (backlog, unassigned, high) with ACs and the dependency on the not-yet-ticketed sponsors-module data layer in its description. **Operator hold: do not start a worker — machine overloaded at mint time.** Design and cut only.
- 2026-08-14 (evening): **Attendee schedules — DESIGN SIGNED OFF; v0.3 is the binding visual contract; build running as MRQ-208.** (Atin) Atin drove v0.3 in Chrome and loved it, round-3 additions (identity line, speaker cross-over) included. Five Round-4 build rulings closed the open questions (`sequence/attendee-schedule-design.md` §7, commit de189c0e): **claim is request → verify** — the CRM write, identity flip, and speaker pins happen only when the emailed sync link is first opened, closing the type-a-speaker's-email impersonation hole (pending state is an acknowledged build divergence from the prototype); **retention: codes and webcal feeds persist indefinitely** — no TTL machinery; **the demand board homes in the Agenda module** with the public-counts setting beside it (import prompt on People; the prototype's footer door is demo-only); **speaker pins are derived at render, never stored** — absent from stored `sessionIds` and from the shared read-only link (a friend importing your picks must not inherit "you're speaking"); **one solid ticket, one Opus delegator** (MRQ-132 precedent). **MRQ-208** minted (by the review agent, operator-authorized) and an Opus delegator is building it at surface:308: worktree → PR → review → merge, validation-gated. Claim mail stays launch-gated on the Resend paid tier — build ≠ enable. MRQ-202 (the v0.2 review-fixes ticket) was executed on main and then archived per operator ruling: design work stays out of tickets. The interview thread that produced rounds 2–4 is closed. (Atin + interview surface:257 + review surface + mrq208-opus)
- 2026-08-14 (later): **Attendee schedules — v0.2 review rulings folded; prototype at v0.3 with two mid-drive additions pending love.** (Atin) The v0.2 prototype's adversarial review (`prototypes/attendee-schedule/REVIEW-v0.2.md`, deliberately uncommitted) was ruled finding-by-finding by Atin with the review agent, minted as **MRQ-202**; the prototype+doc portions are executed on main (design doc §7 "Round-2 review rulings"): public count = server aggregate rendered as session metadata beside the chips, never under the star; unlink deletes only what the claim created, with the ruled honesty line; the demand aggregate counts devices **plus agent-created schedule codes** (For-agents discloses it; "via agents" stat on the demand board); demand bar gauges % of room capacity with a 100% tick. Mid-drive, Atin added two round-3 items, prototyped in v0.3 and **pending his love pass** (§7 "Round 3"): an **identity line** (the page says what it knows about you — the answer to "how do I log in" is "you don't," said on the page) and the **speaker cross-over** (claim email matches a speaker → their sessions pin into My Schedule, "You're speaking"; the CRM person-match is the rail, no login). Note: prototype dates were shifted to the real Oct 12–14 before the "demo dates fine as-is" ruling landed; left at 12–14 as the no-op-correct state. (Atin + interview surface:257 + review surface)
- 2026-08-14: **Sidebar reorg round — ruled in prototype (v1.13→v1.15 lineage), build fold NOT yet ticketed.** (Atin + sidebar surface) Comparison mock + ruling record: `prototypes/sidebar-reorg/index.html`; live draft in `pipeline-v1.1` and mirrored in `crm/`. Rulings: the seven-row Pipeline ladder leaves the nav (stages live on the Program-home strip with counts, board columns, list filter); a hover **stage flyout** on the Program pipeline row (Overview row first, then stages 1–7 with live counts) restores direct jumps; "Program home" → **Program pipeline**; "Modules" splits into **Speaker ops / Call for proposals / Public links / Settings**; icons curated (↗ leads in the icon column on external rows; geometry glyphs dropped; 16px column reserved so labels never shift); **People CRM** and **Outreach** are the Organization rows ("Abstracts & sessions" and "Program board" keep full names; Lists folds into People — build PR #224 removed its row); Outreach is org-level with each card naming its target conference (no baked-in year anywhere); conference picker sits under a **Conference** group label, eyebrow removed; **Conference settings gains a Danger zone** (type-name-to-confirm delete; people/notes/tags/outreach stay — org-level); sessions-vs-abstracts keeps the judges' language ("Abstract"/"Session" chips). Environment facts worth keeping: prototypes are served at `127.0.0.1:8123` because **WKWebView silently blocks all page-initiated file://→file:// navigation**, and cross-prototype links carry `?v=` cache-busters. **Round closed 2026-08-14 (later): the love pass is SIGNED.** Everything above plus the toggle/explainer, the + and Embeds rows, the shared `prototypes/shared/sidebar.css` extraction, a conference **Danger zone** (type-name-to-confirm delete), and the centered-page-column fix (crm `.page` lacked `margin:auto`) landed in the prototype; Atin signed the v1.15 state and ruled the minimum ticket set. **Minted: MRQ-203** (sidebar/flyout/Lists-into-People/page column), **MRQ-204** (delete conference, contract-first), **MRQ-205** (Outreach + v1.15 CRM items), **MRQ-206** (sessions legibility), **MRQ-207** (Organization-settings fold per `sequence/org-settings-design.md`). Ticket text: `sequence/sidebar-fold-tickets.md`.
- 2026-08-14: **Attendee schedules, round 2 — no accounts reaffirmed; opt-in email claim; attendees enter the CRM as people; star-count signal; agent-native ticket import.** (Atin) Full rulings + proposed design in `sequence/attendee-schedule-design.md` §7. The anonymous localStorage + short-code model (MRQ-132, live) stays the spine; an opt-in "save your schedule with your email" claim closes the lost-write-key recovery gap and is the sole door to attendee identity ("organizer can see your picks" stated plainly — ruled fine). Attendees become org-scoped `people` + a new event-scoped `event_attendances` join (`participations` is submission-scoped, wrong join; no `memberships` role, no auth change) — a separate attendee DB was considered and ruled against. Per-session star counts via an anonymous device beacon: organizer always sees exact counts (advance room-planning signal); public display is an org setting, shown only at ≥ n stars (default 3). Ticketing import is agent-native: no per-platform connectors — skill-file chapter + a "task for your agent" copy-paste prompt in the web app over the existing people-import rails, idempotent by email. Claim mail launch-gated on a Resend paid tier (100/day free cap vs 1k–3k attendees). Prototype before build; nothing minted yet. (Atin + attendee-schedules interview surface)
- 2026-08-12: **This repository goes public as it stands — history, `sequence/`, and the `.lattice` board.** (Atin) The curated-orphan plan (`mrq-42-assembly`) is superseded: the record of how the product was built is worth more to a reader than a tidy tree, and swyx called the competition "somewhat of a recruiting exercise." **No history rewrite** — a `filter-repo` tonight, across live agent worktrees and an in-flight PR, is a worse risk than what it would fix. gitleaks over all 820 commits found no credential value ever committed (two hits, both the same fabricated `mq_live_…` demo token in the prototype's mock data), so the irreversible question is answered. Two files were deleted from `HEAD` as **tidiness, not remediation** — they remain readable in history and are not to be described as a security fix: `sequence/OPERATOR-PRECONDITIONS.md` (inventoried which credentials sit in which file, plus the account, the 1Password path, and the account-ID prefix — a target list with zero value to a judge) and `sequence/research/sources/competitor-context-doc-2026-08-08.md` (**another entrant's** working document from the Discord our judges run). Everything else in `sources/` stays: swyx's own brief, images, and transcripts are harmless to republish, and they prove the homework. **Deferred, explicitly not tonight:** rotating the R2 token and `RESEND_API_KEY`, reasonable hygiene after the competition since the deleted file inventoried them.

- 2026-08-12: **Cold-start design run complete; contract minted as a post-deadline band; one build ticket cut.** The cold-start interview (rulings D1–D8, `prototypes/cold-start/DECISIONS.md`) produced prototype **v1.11** (agent-run setup · one-time claim link · token handoff · Instance panel · demo side-by-side · organizer invites · second conference via ＋), `docs/GETTING-STARTED.md` (+ README pointer), and the SKILL.md setup-chapter draft — all merged (#59, #63, #65). Contract fold: USER_STORIES Amendment 19 (**US-83 – US-86**, **AC-275 – AC-287**; US-82/AC-270–272 stay reserved; next mint **AC-288**), EVALUATION §2.4 + Amendment 13 (a **post-deadline band outside the Wednesday gate** — no gate count changed; trace:ac verified green at 229 parsed / 0 uncovered, enforcement dormant until the owning claims manifest lands), SPEC Amendment 19 (claim/invite via widened `magic_links`, `POST /api/v1/events`, instance status, org members, remove-demo, CLI setup verbs), BUILDPLAN Amendment 12 (**M-65 / MRQ-105**, one vertical ticket, 16 h). **Operator directives:** one ticket capturing mint+spec+build; executed by a **Claude Opus** delegator (explicit override of the sonnet default for this ticket). (Atin + cold-start designer surface:335)
- 2026-08-11 (evening): **Fleet merge drain — the queue was blocked by three causes, none of them a ticket's code.** Atin ruled "merge now, validate on main" with one merge driver (surface:261). Eleven PRs landed in ~90 minutes on a tree where nothing had merged all afternoon: #14, #16, #11, #12, #15, #18, #19, #17, #20, #21, #23. **The three causes, in order of how much they cost:** (1) **Load average 158 on 16 cores** — five agents each running the full suite, 23 `workerd` processes; tests died at the 5000ms per-test timeout from CPU starvation while three agents independently diagnosed "it's the box, not my change". Compounded by `npm test` spawning **two** vitest processes that each sized a worker pool to the full core count, a 2× self-oversubscription before any second agent existed (fixed, #16). (2) **CI had been red on every PR since the workflow was written** — `node-version: 20.19.0` against an `engines` floor of `>=22.18.0`, so the six `tests/node` files importing `.ts` died with `ERR_UNKNOWN_FILE_EXTENSION`. Invisible for as long as it was because the one-minute `npm test` step timeout killed the suite before it ever reached them; raising that timeout (#8) is what made the Node bug visible at all. (3) **`trace:ac` was red on clean main** — five `REGRESSION ·` titles from #7 that the sanctioned vocabulary rejects, plus a real bug in the checker where `callName()` read the *property* of a property access, so every `/pattern/.test(value)` parsed as a test declaration with a dynamic title (and `test.skip(…)` resolved to `"skip"` and was never title-checked). Fixed in #9. **Three agents fixed the same red-CI symptom independently (#9, #10, #13)** — the tax of leaving a shared gate red is paid by the whole fleet, not by one ticket. **Standing gate during a drain, corrected mid-run by surface:245 and adopted:** three `tsc --noEmit` passes, `vite build`, `check:design`, `check:api`, **`trace:ac`**, plus the diff's own test files — no full `npm test`, no `pr-gate`, documented honestly in each PR body. `trace:ac` belongs in that list precisely because it is static analysis, load-independent, and it caught two real errors the starved suite never reached. (Atin + merge driver surface:261)
- 2026-08-11 (evening): **A regression merged and was recovered; the gap was an untested arm, not a bad review.** MRQ-76 (#17) correctly repointed `can_decide` from the raw `status` column to the derived `stage`, but under `BOARD_STAGE_SQL` a **waitlisted** record matches no arm and falls to `ELSE 'declined'` — so clicking **Maybe**, one of three buttons on the most-used card in the product, permanently removed the Record Action card. Found post-merge by surface:245, which authored the ticket. Verified independently before acting: the derivation is **correct** — `BOARD_STAGES` is a deliberate 8-value vocabulary whose label is literally "Declined / not advancing", the intended catch-all for waitlisted/withdrawn/rejected — so adding `declined` to `can_decide` is the honest fix, not a paper-over, and it closes the open Pass B MAJOR (reconsidering a rejected or withdrawn record) in the same line. Fixing it surfaced worse: `ACTIONABLE_STATUSES` omitted `withdrawn` (the restored button would have rendered and then failed in `canTransition`), and **`can_publish` was not stage-gated at all**, so a withdrawn record that still holds an agenda slot after a reversal could be published to the public site with that speaker's name, title, time and room — through the button or any `program:write` token. Both fixed in #21 with five tests, each verified failing on unfixed main first. **Reversal deliberately does not free the agenda slot** — that is a product call left for Atin; blocking the publish is correct either way. CI could not have caught the original because nothing tested `can_decide` for a waitlisted record: untested branches are invisible to every gate we have. (surface:245 + merge driver)
- 2026-08-11 (evening): **The suite is slow for a measured reason, and the obvious fix does not work.** True runtime is 145–158s on a CI runner against a 45s objective. The per-file tax is the **Miniflare isolate plus the `cloudflare:test` module**, *not* the Worker bundle: probed with four one-line files — bare collect 0.02s, env-only import **18.77s**, whole-app import **18.77s**, migrations 19.05s. Importing the entire application on top of `cloudflare:test` costs approximately nothing, so slimming or tree-shaking the bundle buys nearly nothing. Relocating files to the node pool is also unavailable: of 38 integration files, **zero** touch neither `SELF`, nor an `env.*` binding, nor `cloudflare:test`, nor `apply-migrations` (29 use `SELF`, 35 use a binding, 31 import `cloudflare:test`). **The only lever is fewer files needing a Worker at all.** surface:254 and surface:231 reached this from opposite directions and surface:254 ran the count that settled it against its own hypothesis. Owned by surface:254. Also logged: `reset-demo` asserts `toBeLessThan(20_000)` against a `testTimeout` of exactly 20_000 so it can never fire; and `scanTestSource` cannot read `test.each` — the inner `test.each([…])` call is matched and its *table* read as the title, so every table-driven test fails the gate (third distinct bug in that 40-line scanner in one day; it gates every PR and nobody owns it). (surface:254, surface:231, merge driver)
- 2026-08-11 (evening): **Timeouts are hang detectors, not speed gates — and one that fires on a healthy suite is a defect (#23).** `HARD_LIMIT_MS` was 240s against a suite running 145–158s: ~80s of headroom, about four Worker-backed test files at ~19s each. Two PRs tripped it in one evening with **zero failing tests**, on branches with nothing in common (one added a single Worker test file; the other was a plain-HTTP cookie fix adding no Worker test at all), and both passed on rerun. The cost is not the minutes: a tripped detector reports `status: "timeout"`, deliberately indistinguishable from a wedged process, so the correct response is to re-run rather than read the log — training exactly the habit that makes a real hang invisible. Raised to ten minutes; `BUDGET_MS` still measures against 45s and reports loudly every run, so suite speed stays the number worth arguing about. Same reasoning already applied to CI's step timeouts (#8) and to taking wall-clock budgets out of the hermetic suite entirely (#14, after `AC-62`'s 300ms median assertion failed at 337ms under load — a wall-clock latency assertion inside a hermetic parallel suite is flaky by construction). (merge driver)
- 2026-08-11 (evening): **Two process rules, both paid for in lost work.** (1) **An agent that exits without committing loses real work.** surface:231 found 19 files of a finished change sitting uncommitted in a dead agent's worktree and rescued them (#18); a sweep of `Marquee-worktrees/*` found the rest clean. (2) **Never work in the main checkout** — a genuine fix was found uncommitted and on no branch *in the main checkout itself*, invisible because nothing watches an untracked `main`: session cookies are `Secure`, and **Safari and WKWebView drop a `Secure` cookie on an `http://` origin** (they do not grant localhost the trustworthy-origin exception curl and Chrome do), so under the plain-HTTP local recipe demo login returns 200 and every subsequent request 401s **in the browser only**. c11's embedded browser is WKWebView, so every agent doing local browser validation has been hitting this while curl and the CLI passed. It cannot be sniffed — `wrangler dev` rewrites the request to the `custom_domain` route, so URL, Host *and* Origin all arrive as production even on a loopback http listener — hence the explicit `INSECURE_LOCAL_COOKIES` flag, defaulted to `"0"` in `wrangler.jsonc` and supplied as `--var INSECURE_LOCAL_COOKIES:1` on the dev command. Recovered as #20; it had never been committed because it failed the worker typecheck (`secure` narrows to the literal `true` under `Object.freeze`). **Commit early, commit broken — a branch costs nothing.** (merge driver)
- 2026-08-11 (evening): **Merge control returned to a human gate.** Atin added `CODEOWNERS` (@BenevolentFutures owns every path) and the **"main: manual merge gate"** ruleset on `~DEFAULT_BRANCH`: one approving review, code-owner review required, stale reviews dismissed on push, last-push approval required, plus deletion / non-fast-forward / update protection. Org admins and the admin repository role retain always-bypass. Autonomous fleet merging into `main` ends here; agents open PRs and a human merges. (Atin)
- 2026-08-11: **Collaborator Signal notes investigated; public widgets widened (USER_STORIES Amendment 18).** Note 1 (rubric weights "Public Widgets" joint-heaviest vs embeds tiered cuttable): ruled protect-and-widen — prototype v1.10 embed dialog grows to four formats (Agenda | Sessions | Speakers | Call for speakers) with track filter and speakers cards/list layout in the snippet URL; AC-273/AC-274 minted (AC-270–272 stay reserved; next mint AC-275); US-16/AC-217/AC-218 promoted to live in-scope; AC-87–90 leave the cuttable band. MRQ-75 carries the build plus the SPEC/EVALUATION/BUILDPLAN folds in one PR; Claude Sonnet xhigh delegator launched (worktree mrq-75-widgets off github/main @ 030a3a7). Note 2 (speaker double-booking coverage): verified already covered — AC-77 all-roles, merged `person` conflict class in the one getConflicts path, seed-guaranteed live double-bookings; no action. AI-agenda SKIP reconfirmed (R27). Rubric ingestion itself deferred by Atin; the Sunday-freeze delta triage remains an open exposure. (Atin + gap-investigation surface:177)
- 2026-08-11: **GitHub is canonical for Marquee; Forgejo is retired.** (Atin) `Stage-11-Agentics/marquee`, **private**, branch `main`. A collaborator works through GitHub, so the work has to live there. The local checkout renamed `master` → `main` and **removed the `forgejo` remote entirely** so a stale command fails loudly rather than writing to a repo nobody reads. Delegator contract (`COMMON.md`), `ORCHESTRATOR.md`, `WATCHDOG.md`, and project `CLAUDE.md` all repointed; PRs now go through `gh pr create`. Historical `.lattice` artifacts and plans still name Forgejo — they are the record of what happened and were deliberately left alone. The `forgejo.stage11.ai` strings in `scripts/checks/repo-policy.mjs` are **leak-detection markers for the public assembly** and must stay. Public submission remains a separate curated artifact: the orphan `mrq-42-assembly`, never `main`.

- 2026-08-11: **Resend stays on the free/demo tier — no upgrade.** (Atin) Consequence: the **100/day** cap is a hard constraint rather than a cushion, so demo-safe mode and the outbox are load-bearing rather than conveniences. The "exactly two `always_live` write sites" count becomes a guardrail worth auditing (MRQ-45), and any bulk decision mail must land in the outbox — a rejection wave to ~900 submitters would exceed the daily cap nine times over. `marquee@stage11.systems` is verified and sending-enabled since 2026-03-11, so nothing needs provisioning. Recorded with the rest of the human-only account work in `sequence/OPERATOR-PRECONDITIONS.md` (deleted 2026-08-12 before the repository went public; see the top of this log).
- 2026-08-11: **Operator preconditions collected for a collaborator** → `sequence/OPERATOR-PRECONDITIONS.md`. Everything no agent can do (Workers Paid, R2 entitlement proven by a real fetch, `wrangler login`, Airtable Team + two bases, the Outlook/Apple ICS inboxes, Sessionize fixture), each with why it matters, how to check, and what done looks like. **The file named accounts and was deleted from `HEAD` on 2026-08-12 before the public flip** — see the top of this log; it remains readable in history, which is why nothing in it was ever a secret value. (watchdog)

- 2026-08-10: **State-model gaps found, ruled, and folded — SPEC Amendment 15 / BUILDPLAN Amendment 10.** Dossier: `sequence/research/state-model-gaps.md` (six issues, each verified against the shipped schema rather than inferred). **Three ruled contract-now and built into the prototype:** (1) **task cancellation had no state** — `speaker_tasks` shipped `open|done` while AC-123 already *graded* the reversal dialog's cancel/retain choice and AC-125 puts `task overdue` in the minimum trigger set, so open tasks on a cancelled talk would keep mailing a real speaker; resolved by a nullable `cancelled_at`, one idempotent `reconcileTaskSet` on every acceptance path, and a single `owes = neither done nor cancelled` predicate. (2) **Outbound webhooks existed in story, AC, ticket, queue binding and validation row but had no tables, no §4.2 routes and no screen** — `check:api`'s registry parity would have failed M-54 on its own PR; contract holes filled, build unchanged in rank and hours. (3) **"Decided but not told" had no answer** — derived, zero schema, three named producers (Airtable inbound skips the cascade by design, suppressed/failed outbox, no valid address). **Two client rulings:** the reversal dialog's branches are now `Cancel open tasks` / **`Keep tasks active`** (the old "Retain for records" described what cancelling already did), and re-acceptance **restores** rather than reassigns — implemented as one idempotent reconciliation with no separate restore branch. **Ruled if-capacity:** Speaker Handbook (AC-233 — renders in the prototype, still has no table or authoring surface). **Ruled extra credit:** the submitter/speaker split, named as a known limitation in SPEC §10 and enforced by new **gate 19b**, citing the already-specified AC-223/AC-224; criteria drafted and held unminted. Minted: US-80/US-81, AC-264–269 (203 live in-scope / 267 through AC-269). Tickets MRQ-66 (migration 0003) → MRQ-67 (cancellation + reconciliation) and MRQ-68 (decided·not-notified), 7 agent-hours; MRQ-25 and MRQ-30 both commented with what changed under them. **Also fixed, pre-existing:** the chase board described the portal speaker as owing nothing while her portal showed 3 outstanding — two screens contradicting each other on the seat-flip path that impersonation makes one click. `STATEMAP.md` gained the rule that caused all of this: *every machine draws its reversal edge or says it has none*. Prototype v1.8 verified in-browser across 27 routes, zero JS errors. (Atin + state-gaps surface)
- 2026-08-10: **Sessionboard gap analysis validated and scope ruled** (`sequence/research/sessionboard-gap-analysis.md`, Decided-scope section). Six rulings: rich text ships as a markdown-backed toolbar (no contenteditable HTML, no image insert); taxonomy = Tags + Level, Language cut under R39; copy-from-event skipped for competition (→ ROADMAP.md); drafts count toward the submission limit; `max_sponsors` control dropped from the builder UI (schema column stays); adoption gate = contract-now band (the six + two one-liners + cheap adds) / if-capacity band (after MRQ-20/21 green) / parked. Next: fold contract-now items into the v1.7 prototype, then SPEC/AC mint, then tickets. (Atin + gap-review surface)
- 2026-08-10: **Venue map — geography as a product primitive; prototype v1.7 shipped.** Scope signed T1+T2+T3 (T4 public-site map and non-session places deferred, not struck). Design reasoning in `sequence/venue-map-ux.md`: five zooms of attention, with most surfaces taking text (relation, instruction) rather than a map — the map is the receipt that makes the text trustworthy, not the interface. Landed in the prototype: speaker-portal location card with a leave-by computed from the speaker's own previous session; five place merge fields plus a "Where to be" template; real ICS `LOCATION`+`GEO` replacing a hardcoded bare room name; `#settings/venues` overview; building coordinate/entrance authoring; and a building band over the agenda room columns. Rendered as an OSM tile mosaic — plain `<img>`, no library, no CDN, no key — degrading to pins over the graph-paper grid. Two model corrections found while building: the **Travel** conflict class is renamed **Transit** (it collided with the "Travel and accommodation" speaker task, aimed at the same person), and **entrance instructions moved from the room to the building** (`accessNote`), so every room inherits them. Disclosure ruled by Atin: venue surfaces show for every conference but **fold** when there is one building — zoom 2 (address, entrance, access minutes) is independent of building count, so only the comparison folds. Verified in-browser across all 21 routes. **Contract fold complete 2026-08-10:** SPEC Amendment 14, USER_STORIES Amendment 14 (US-77 – US-79, AC-255 – AC-263), EVALUATION §2.3 extended to AC-263 with counts reconciled (206 live in-scope / 261 through AC-263), BUILDPLAN Amendment 9 (M-57 – M-60, 17 agent-hours), and Lattice MRQ-62 – MRQ-65 created with dependencies wired onto merged MRQ-58. (Atin + map-design surface)
- 2026-08-10: **Build fleet switches Kimi → Claude.** Kimi's output is not meeting the bar; remaining tickets move to Claude. Handover state recorded as a comment on each live ticket (MRQ-3, MRQ-4, MRQ-8, MRQ-14): exact untracked file inventory with line counts, the contract findings each branch must re-verify against, and the `1507bff` rebase requirement. **Two facts a successor needs:** (1) **none of the four delegators committed anything** — ~1,900 lines exist only as untracked working-tree state, with no PR and no history; (2) **zero test files across all four**, while `trace:ac` blocks merge on uncovered `auto` ACs. Also latent: MRQ-8 and MRQ-14 both edit `package.json` + `package-lock.json`, which `BUILDPLAN.md` §7 reserves to M-06 through the orchestrator. (Atin, recorded by Adversary)
- 2026-08-10: **Adversarial pass fully closed — 8/8 BLOCKING · 22/22 FIX · 8/8 NOTE.** The NOTE band was re-verified against current files and N-2 – N-7 applied (N-1/N-8 were already folded into B-1/B-6). Load-bearing one was **N-7**: `check:api` asserted CLI-registry parity on every PR from Wave 0 while the CLI is rank 19 — it now skips that half with a printed notice until `cli/` exists (M-38), which unblocks the live delegators' PRs. Also: AC-233's stale no-waiver sentence corrected, the three non-`/api/v1` calendar/feed URLs named as an allowlist, an e2e split-do-not-delete rule added, desktop-only admin SPA recorded as a non-goal, and Airtable row counts given a refresh owner + "as of last sync" labelling. (Adversary)
- 2026-08-09: **Skin picked: Flight Deck** (skin-c). Skins agent reskinning the prototype in place -> v1.5; holds exclusive file lock on prototypes/pipeline-v1.1/index.html until it reports done. DESIGN.md minted. (Atin via Skins agent)
- 2026-08-09: **F-13 ruling: NO unattended reset cron.** Reset stays manual (button + `npm run reset:demo`, AC-230); during the judging window the team resets morning/evening by hand. A judge watching state vanish mid-read is a worse failure than inheriting a predecessor's edits. (Mabel, decide-and-log)
- 2026-08-09: **F-10 ruling: acknowledged divergence** — SPEC §5.5's on-screen portal magic link is built per spec though absent from the v1.4 prototype; tagged so the fidelity audit passes it. (Mabel, decide-and-log)
- 2026-08-09: **Adversarial cycle complete**: 8/8 BLOCKING + 21/22 FIX applied (e35b32a, 6aed313); arithmetic reconciled (194 in-scope live, next mint AC-251). (Editor)
- 2026-08-09: **Context-coverage closure approved** — add full form/portal/reviewer demonstrations plus saved views, configurable columns, and Draft queue; make all pre-build contract repairs. AC-245–249 minted; downstream contract reconciled. (Atin)
- 2026-08-09: **No Month view and no generalized CMS** — Month was only a reference-image label; generalized CMS was optional/R8-skipped. Narrow Handbook and embeds remain. (Atin)
- 2026-08-09: **Speed budgets signed as OBJECTIVES** — the seven proposed §1.3 numbers report + warn, never fail CI; AC-sourced budgets remain binding. (Atin)
- 2026-08-09: **Board ruling (client): NO drag** — AC-239 struck, AC-243 ratified; consequential actions live on the detail screen. (Atin)
- 2026-08-09: **F-2 CLOSED — seed A′ confirmed** (real Feb-2025 sessions + CODE-2025 roster → ~150 accepted, zero fabricated accepted people). (Atin)
- 2026-08-09: **API comparison folded** (Amendments 6–7): four pre-kickoff API gaps + pinned semantics + webhooks behind Tier A (AC-241) + scoped tokens (AC-242); reviewer full-detail AC-244 minted from client feedback; v1.4 in flight. (Mabel)
- 2026-08-08: **Board interaction refined (canonical AC-238/AC-243; AC-239 struck)** — no board drag or card actions; filters are expanded and composable, cards open exact records, and lifecycle buttons live on record pages. Agenda drag remains. Earlier notes that called these AC-241/242 were an ID-allocation defect and are superseded. (Atin + Codex)
- 2026-08-08: **Pipeline v1.3 completed** — program board, scheduled/public slot legibility, publish affordances, clarifying pipeline labels, and ≥15% multi-track seed coverage are implemented and Playwright-verified at full mock scale. (codex)
- 2026-08-08: **Pipeline v1.2 completed** — all 24 specified screens/states represented, no stub-toast affordances, five finished agenda views, live Event Settings collections, Sessionize import, comms/outbox, reversal cascade, Airtable/API surfaces, multi-track AC-234, and portal talk editing AC-237. Playwright gate passed; client drive remains the love gate. (codex)
- 2026-08-08: Project named Marquee, homed at `deployments/Marquee`. (Atin)
- 2026-08-08: Anchor competitive research on Sessionboard as the reference product. (Atin)
- 2026-08-08: Timeline compresses the Tone arc to ~4 days end-to-end; depth stays proportional to stakes but phases run fast and overlapping. (logged, Mabel)
- 2026-08-08: **Philosophy signed** (PRODUCT-DEFINITION decision 1): one-thing = "fantastic conferences, effortlessly"; respect-the-operator principles; plus **agent-native by design** — real API, `marquee` CLI, shipped skill file; agents are first-class operators. `PHILOSOPHY.md` minted. (Atin)
- 2026-08-08: Clone target reconfirmed explicitly: **Sessionboard's Program module** per the brief; Sessionize is competitive context (judges' daily tool), not the target. (Atin + brief)
- 2026-08-08: **Rounds (decision 2 part):** minimal 2-round funnel UI committed high in Tier B; round-aware schema from first migration. (Atin, interview)
- 2026-08-08: **Stack (decision 3):** Cloudflare Workers + D1 source of truth + R2/Queues/Turnstile, genuine two-way Airtable mirror off the request path, Resend, ICS METHOD:REQUEST, roll-our-own magic-link auth. Committed without waiting for the Q1 Discord ruling (we'd build the mirror under either answer). (Atin, interview)
- 2026-08-08: **Repo/license (decision 4):** develop private on Forgejo; public GitHub `marquee` repo pushed near submission (~Tuesday); **Apache-2.0**. Research/strategy docs curated before the public push. (Atin, interview)
- 2026-08-08: **Domain (decision 6):** stage11 infrastructure only — app at `marquee.stage11.dev`, outbound mail from `marquee@stage11.systems`. No fresh domain; deadline-trap 5 closed, action item 4 dropped. (Atin, interview)
- 2026-08-08: **Strategic intent:** one-off competition play (hosted-for-fee possible later, not designed for now). Optimize purely for the judges' Wednesday experience. (Atin, interview)
- 2026-08-08: **Demo seeding:** faithful AIE replica, grounded in the real 2025 NYC event's public program (Seed Source agent capturing); ~1,000 fabricated submissions shaped like the real distribution. (Atin, interview)
- 2026-08-08: **Comparison-mode triage: in scope**, Tier B (simple win-count aggregation). (Atin, interview)
- 2026-08-08: **Name confirmed: Marquee** — collisions with npm/GitHub explicitly don't matter; this is a competition entry. (Atin, interview)
- 2026-08-08: **No entrant video required** — submission = form + repo + deployed site; the walkthrough videos are the organizers' test script. (brief, confirmed to Atin)
- 2026-08-08: **Timeline set by Atin:** HTML prototypes done TONIGHT → build kicked off overnight (tone-architect contract compressed into late tonight, then lattice-orchestrator) → QA tomorrow (Sun), Sunday video may add requirements → polish Mon/Tue → public push ~Tue → submit Wed.
- 2026-08-08 (night): **Build fleet runs on Codex agents** — Atin directive: implementation work in the orchestrated build uses codex workers (as the prototypes did). Research/drafting agents may remain Opus. (Atin)
- 2026-08-08 (night): **Contract flags F-1/F-5/F-6/F-3 resolved by AC appendix** — AC-225..233 dispatched to the stories agent (mirror US-72, reset-demo US-73, Turnstile/upload-safety on US-14/US-41, handbook AC-233 below cut line). **F-2 (seed A′) and F-4 (v1.1 toast affordances) await Atin's v1.1 review**, bundled with the seven proposed speed budgets. (Mabel, full-ahead)
- 2026-08-08: **Surfaces note:** Atin closed the Brief Research (was surface:130) and Landscape Features (was surface:132) tabs post-completion. Their dossiers are committed; dossier maintenance against the Sat/Sun videos needs a fresh agent when the videos land. Discord pastes now come to the orchestrator (surface:128).

## Historical research agents (completed; none currently active)

- **Brief Research** (surface:130): competition-requirements authority → `sequence/research/competition-requirements.md`. **First pass complete 2026-08-08** (R1–R50). Stays alive for Discord pastes + Sat/Sun videos. Prompt: `sequence/research/sources/AGENT-BRIEF-competition-research.md`.
- **Landscape Features** (surface:132): **First pass complete** — feature matrix keyed to R-numbers, deep profiles (Sessionize corrected: $499/event, has collision detection + undocumented "calendar placeholders"), D1–D15 differentiator ranking, pretalx threat read (5 structural misses), vocabulary crib, L1–L4 new open questions.
- **Stakeholder Stories** (surface:133): **First pass + follow-up complete** — 15 seats, 67 stories (US-66 Sessionize mid-CFP migration, US-67 quick-search added), 45-story MVP. Sessionize API is accepted-only by default → importer eats Export spreadsheets, idempotent.
- **Seed Source** (this surface): **Complete** → `sequence/research/seed-source-2025.md` + machine-readable `sequence/research/sources/aie-summit-2025-program.json`. Chose **AI Engineer Summit NYC, Feb 19–22 2025** as the seed source (AIE's own site calls it "the flagship AI Engineer Summit held in NYC"; it's the ~150-speaker "AIE New York" budget line; its 2025 theme was already finance-forward — Jane Street/BlackRock/Bloomberg/Morgan Stanley — matching NYC 2026's "Where AI Engineering Meets Wall Street"; and it is the **only** AIE NYC event with a published session-level grid). Full 76-item grid recovered from the page's embedded JSON: 60 real speaker-bearing sessions, 75 speakers, 58 companies, 8 rooms across 4 buildings, exact timing grid. CODE Summit Nov 2025 captured as secondary (89-name roster, category taxonomy, 4-day shape). Recommends 1,000 submissions / 60 accepted = **6.0%**, inside AIE's published 5–15%.
- **Seams Feasibility** (surface:134): **First pass complete** — Airtable/Cloudflare/email/ICS/auth with hard numbers, 16 deadline traps, 33–54h seams estimate. Q1 bet: D1 source of truth + genuine two-way Airtable mirror (never read Airtable on a request path). Q9 confirmed: ICS METHOD:REQUEST; OAuth infeasible by deadline.

## Key findings so far (from Brief Research first pass)

- Speed is a graded feature (swyx complains 3x unprompted); AI review is explicitly de-prioritized ("I don't care about the AI workflow thing", 09:23) — a trap for other entrants.
- Real deployment target: **AIE NYC 2026** (Oct 12–14, ~150 speakers, 1k–3k submissions, 5–15% acceptance). AIE runs on **Sessionize** today, not Sessionboard. Competitive frame: "Sessionize's scope, self-hosted, owned, faster, plus post-acceptance workflow."
- Judge is likely AIE's program/ops person. Evaluation = driving the deployed site through the walkthrough loop; seeded demo data essential; 647 entrants registered.
- Core data model: Abstracts (apply to speak) vs Sessions (guaranteed, e.g. sponsors) — video-only, easy to get wrong.

## Retro notes (for run-retro, not action items)

- Monitor false positive ×2 (2026-08-09): a completion-phrase monitor on a builder's screen matched the orchestrator's *own instruction text* echoed in the terminal; the `grep -v "print exactly"` exclusion then ALSO failed because terminal line-wrapping split the exclusion phrase onto a different line than the match. Real fix: **never watch a terminal for a phrase the orchestrator itself sent** — watch the artifact (file markers) or a side-channel (c11 send, metadata key) instead. Generalize into the c11 orchestration reference at retro.

## Human action items (updated from EVALUATION.md §1.6, 2026-08-08 night)

New (for the oracle smokes and gate — needed before Tuesday):
- **S-2 ICS oracle (MRQ-55, `needs_human` since Sun) — deferred by Atin 2026-08-10, plan agreed.** The Gmail triplet is already delivered to `benevolent.futures@gmail.com` (search `[S-2 spike]`). **Gmail row: the orchestrator drives it via browser automation once on Alpha** — screenshot whether 1/3 renders Accept/Decline and whether 2/3 replaces 1/3 without a 15:00 duplicate. **Do not click Accept without a fresh go-ahead** — it writes to Atin's real calendar. **Operator-only remainder: an Outlook.com address and an iCloud-backed address** (one `node send.mjs <address>` run each). MRQ-25 is written against this verdict; Gmail alone is a defensible partial.
- One real Sessionize export file (any event) to validate the importer fixture (AC-109).
- A Cloudflare API token for CI (`check:readme` scratch deploy) + a model credential for `check:skill-agent`.

## Earlier action items (from Seams first pass, 2026-08-08)

1. Verify/enable **Workers Paid** ($5/mo) on the Cloudflare account (Free's 10ms CPU cap breaks SSR; fails at deploy, not in dev).
2. **Create an R2 bucket and fetch a public object now** — Stage 11's R2 entitlement has silently lapsed account-wide before (403s every public URL; dashboard-only fix).
3. Check **Resend plan tier** (30s in dashboard) — Free's 100/day cap decides how urgent the outbox/demo-safe mode is.
4. If a bespoke sending domain (e.g. a marquee.* domain) is wanted: register + verify **Saturday or not at all**. Default: send as `marquee@stage11.systems` (verified since March).
5. Airtable needs **Team plan or above** and now **two bases**: the demo base (seed mirror, judge-visible) and a dedicated `check:mirror` test base (the suite writes destructively — EVALUATION precondition 9). ⚠️ Seed Source confirms the target is **exactly 1,000 submissions** — dead on the Free cap, before speakers/sessions/evaluations are counted. Treat the plan upgrade as required, not optional.

## Blocking defect found in merged code (2026-08-10, map-design surface)

**The seed makes the Transit conflict unreachable.** `scripts/seed/event.ts:151-152` seeds *Sheraton New York Times Square* and *Workshop Annex* at **identical coordinates** (`40.7625188, -73.9814528`) with `access_minutes` 0 on both. MRQ-58's migration is correct and the seed is faithful to SPEC §6 as it was written — and the consequence is that the Transit conflict class can never fire in the shipped product and the site map stacks two pins on a single point. It would pass every unit test and be inert in the demo: green tests, dead feature.

SPEC Amendment 14 supersedes §6's "without inventing a second real venue" clause, and AC-259 makes `check:seed` fail until the seed can produce a live Transit conflict. **MRQ-62 owns the fix and blocks MRQ-63/MRQ-64.**

**Open question for Atin:** what the second venue should be. The conference record is a 2026 Sheraton event; the prototype seeds the real 2025 multi-building set (Times Center · Jay Suites · AWS JFK27), which is geographically real but chronologically wrong for this event, and it pairs Sheraton-native room names (Metropolitan Ballroom, Empire East/West, Central Park) with 2025 buildings — incoherent in the other direction. Prototype and build must land on the same answer, since the prototype is the binding design contract.

## Touchpoints

- **OPEN (urgent, relayed to Atin 2026-08-08):** Discord relay needed — both clarification videos (Sat + Sun) are unlisted, announced only in Discord; requirements freeze after Sunday's. Also Q1 (Airtable primary vs mirror) and Q2 (embed gallery: struck in brief vs described in video) need rulings.
- Pending: stories review (Phase 4) — after fan-out consolidation.
- Pending decision with Atin: public GitHub repo (competition requirement) vs Forgejo-private default.

## Stats

- Human touchpoints: 2 (commission Q&A; brief handoff)
- Agents spawned: 4
